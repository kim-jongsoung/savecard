<!-- 모바일 상단 네비게이션 -->
<nav class="mobile-nav d-md-none">
    <div class="mobile-nav-container">
        <div class="mobile-nav-tabs">
            <a href="<% if (typeof partnerAgency !== 'undefined' && partnerAgency) { %>/partner/<%= partnerAgency.code %><% } else { %>/<% } %>" class="mobile-nav-tab <%= currentPage === 'home' ? 'active' : '' %>">
                <i class="fas fa-home"></i>
                <span>메인</span>
            </a>
            <a href="<% if (typeof partnerAgency !== 'undefined' && partnerAgency) { %>/register?agency=<%= partnerAgency.code %><% } else { %>/register<% } %>" class="mobile-nav-tab <%= currentPage === 'register' ? 'active' : '' %>">
                <i class="fas fa-id-card"></i>
                <span>카드발급</span>
            </a>
            <a href="<% if (typeof partnerAgency !== 'undefined' && partnerAgency) { %>/login?agency=<%= partnerAgency.code %><% } else { %>/login<% } %>" class="mobile-nav-tab <%= currentPage === 'my-card' ? 'active' : '' %>">
                <i class="fas fa-credit-card"></i>
                <span>내카드</span>
            </a>
            <a href="<% if (typeof partnerAgency !== 'undefined' && partnerAgency) { %>/stores?agency=<%= partnerAgency.code %><% } else { %>/stores<% } %>" class="mobile-nav-tab <%= currentPage === 'stores' ? 'active' : '' %>">
                <i class="fas fa-store"></i>
                <span>제휴업체</span>
            </a>
        </div>
    </div>
</nav>

<style>
:root {
    --max-width: 1240px;
    --primary-color: #4FC3F7;
    --secondary-color: #667eea;
    --accent-color: #87CEEB;
    --nav-bg: rgba(255, 255, 255, 0.95);
    --nav-shadow: 0 2px 20px rgba(0,0,0,0.1);
}

.mobile-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: var(--nav-bg);
    backdrop-filter: blur(10px);
    box-shadow: var(--nav-shadow);
    border-bottom: 1px solid rgba(0,0,0,0.05);
}

.mobile-nav-container {
    padding: 10px 15px;
}

.mobile-nav-tabs {
    display: flex;
    justify-content: space-around;
    align-items: center;
}

.mobile-nav-tab {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
    color: var(--secondary-color);
    padding: 8px 12px;
    border-radius: 10px;
    transition: all 0.3s ease;
    min-width: 60px;
    font-family: "Noto Sans KR", -apple-system, BlinkMacSystemFont, sans-serif;
}

.mobile-nav-tab:hover,
.mobile-nav-tab.active {
    color: white;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(79, 195, 247, 0.3);
}

.mobile-nav-tab i {
    font-size: 18px;
    margin-bottom: 4px;
}

.mobile-nav-tab span {
    font-size: 11px;
    font-weight: 500;
    white-space: nowrap;
}

/* 모바일에서 body에 상단 여백 추가 */
@media (max-width: 767.98px) {
    body {
        padding-top: 70px;
    }
    
    .container {
        padding-top: 10px;
    }
}

/* 데스크톱에서는 숨김 */
@media (min-width: 768px) {
    .mobile-nav {
        display: none !important;
    }
}
</style>
