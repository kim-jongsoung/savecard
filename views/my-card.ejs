<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - 괌세이브카드</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body { background: linear-gradient(135deg,#87CEEB 0%,#4682B4 50%,#1E90FF 100%); min-height: 100vh; }
    .container-narrow { max-width: 720px; margin: 0 auto; padding: 16px; }
    .glass { background: rgba(255,255,255,0.15); border: 1px solid rgba(255,255,255,0.2); backdrop-filter: blur(16px); border-radius: 18px; color:#fff; }
    .section { padding: 20px; margin: 12px 0; }
    .usage-item { background:#fff; color:#333; border-radius:10px; padding:12px; margin:8px 0; border-left: 4px solid #667eea; }
    .badge-soft { background: rgba(255,255,255,0.25); border: 1px solid rgba(255,255,255,0.35); }
  </style>
</head>
<body>
  <div class="container-narrow">
    <div class="glass section">
      <div class="d-flex justify-content-between align-items-center">
        <h4 class="mb-0"><i class="fas fa-id-card me-2"></i>내 카드</h4>
        <form method="POST" action="/logout" class="m-0">
          <button class="btn btn-sm btn-light"><i class="fas fa-right-from-bracket me-1"></i>로그아웃</button>
        </form>
      </div>
    </div>

    <div class="glass section">
      <div class="row g-3">
        <div class="col-12 col-md-6">
          <div><span class="badge badge-soft rounded-pill me-2">이름</span> <strong><%= user.customer_name %></strong></div>
          <div class="mt-2"><span class="badge badge-soft rounded-pill me-2">이메일</span> <%= user.email %></div>
        </div>
        <div class="col-12 col-md-6">
          <div><span class="badge badge-soft rounded-pill me-2">여행사</span> <%= user.agency_name %></div>
          <div class="mt-2"><span class="badge badge-soft rounded-pill me-2">유효기간</span> <%= user.expiration_text %></div>
        </div>
      </div>
      <div class="mt-3 small" style="opacity:0.9">카드 토큰: <code><%= user.token %></code></div>
      <div class="mt-3">
        <img src="<%= user.qr_image_path %>" alt="QR" style="background:#fff;border-radius:12px;padding:8px;max-width:180px;width:100%">
      </div>
    </div>

    <div class="bg-white section" style="border-radius:18px;">
      <h5 class="mb-3"><i class="fas fa-clock-rotate-left me-2 text-primary"></i>Recent Usage History</h5>
      <% if (usages && usages.length) { %>
        <% usages.forEach(u => { %>
          <div class="usage-item">
            <div><strong><%= u.store_name || u.store_code %></strong></div>
            <div class="text-muted small"><i class="fas fa-calendar-alt me-1"></i><%= new Date(u.used_at).toLocaleString('ko-KR') %></div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="text-muted"><i class="fas fa-info-circle me-1"></i>최근 사용 내역이 없습니다.</div>
      <% } %>
    </div>

    <div class="text-center mt-3">
      <a href="/stores" class="btn btn-light"><i class="fas fa-store me-1"></i>제휴업체 보기</a>
      <a href="/" class="btn btn-outline-light ms-2"><i class="fas fa-home me-1"></i>메인으로</a>
    </div>
  </div>

  <footer style="text-align:center;padding:16px 0;font-size:14px;color:#ffffffa8">&copy; 2025 luxfind. All rights reserved.</footer>
</body>
</html>
