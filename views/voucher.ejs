<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>바우처 - 괌세이브카드</title>
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px 0;
        }
        .voucher-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .voucher-header {
            background: linear-gradient(135deg, #4FC3F7 0%, #29B6F6 100%);
            color: white;
            padding: 30px;
            text-align: center;
            position: relative;
        }
        .voucher-header::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 0;
            border-left: 20px solid transparent;
            border-right: 20px solid transparent;
            border-top: 20px solid #29B6F6;
        }
        .voucher-body {
            padding: 40px;
        }
        .voucher-info {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .info-label {
            font-weight: 600;
            color: #495057;
            min-width: 120px;
        }
        .info-value {
            color: #212529;
            text-align: right;
            flex: 1;
        }
        .savecard-section {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border-radius: 15px;
            padding: 25px;
            text-align: center;
            margin-bottom: 30px;
        }
        .savecard-code {
            font-size: 2rem;
            font-weight: bold;
            letter-spacing: 3px;
            margin: 15px 0;
            padding: 15px;
            background: rgba(255,255,255,0.2);
            border-radius: 10px;
            border: 2px dashed rgba(255,255,255,0.5);
        }
        .qr-code {
            background: white;
            padding: 20px;
            border-radius: 15px;
            display: inline-block;
            margin: 20px 0;
        }
        .instructions {
            background: #e3f2fd;
            border-left: 5px solid #2196f3;
            padding: 20px;
            border-radius: 0 10px 10px 0;
            margin-bottom: 20px;
        }
        .footer-info {
            text-align: center;
            color: #6c757d;
            font-size: 0.9rem;
            border-top: 1px solid #e9ecef;
            padding-top: 20px;
        }
        .status-badge {
            font-size: 1.1rem;
            padding: 8px 16px;
        }
        @media print {
            body {
                background: white !important;
                padding: 0 !important;
            }
            .voucher-container {
                box-shadow: none !important;
                border: 1px solid #ddd !important;
            }
        }
    </style>
</head>
<body>
    <div class="voucher-container">
        <!-- 바우처 헤더 -->
        <div class="voucher-header">
            <h1 class="mb-2"><i class="bi bi-ticket-perforated me-2"></i>여행 바우처</h1>
            <p class="mb-0">GUAM SAVE CARD TRAVEL VOUCHER</p>
            <div class="mt-3">
                <span class="badge bg-success status-badge">확정완료</span>
            </div>
        </div>

        <!-- 바우처 본문 -->
        <div class="voucher-body">
            <!-- 예약 정보 -->
            <div class="voucher-info">
                <h5 class="text-primary mb-3"><i class="bi bi-info-circle me-2"></i>예약 정보</h5>
                <div class="info-row">
                    <span class="info-label">바우처 번호</span>
                    <span class="info-value fw-bold text-primary"><%= voucher.voucher_token || '-' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">예약자명</span>
                    <span class="info-value"><%= reservation.korean_name || '-' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">연락처</span>
                    <span class="info-value"><%= reservation.phone || '-' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">상품명</span>
                    <span class="info-value fw-bold"><%= reservation.product_name || '-' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">패키지</span>
                    <span class="info-value"><%= reservation.package_type || '-' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">이용일자</span>
                    <span class="info-value fw-bold text-success">
                        <%= reservation.usage_date ? new Date(reservation.usage_date).toLocaleDateString('ko-KR') : '-' %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="info-label">이용시간</span>
                    <span class="info-value"><%= reservation.usage_time || '-' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">인원</span>
                    <span class="info-value">
                        성인 <%= reservation.people_adult || 0 %>명
                        <% if (reservation.people_child && reservation.people_child > 0) { %>
                            , 아동 <%= reservation.people_child %>명
                        <% } %>
                        <% if (reservation.people_infant && reservation.people_infant > 0) { %>
                            , 유아 <%= reservation.people_infant %>명
                        <% } %>
                    </span>
                </div>
                <% if (reservation.memo) { %>
                <div class="info-row">
                    <span class="info-label">특별요청</span>
                    <span class="info-value"><%= reservation.memo %></span>
                </div>
                <% } %>
            </div>

            <!-- 괌세이브카드 발급 섹션 -->
            <div class="savecard-section">
                <h4><i class="bi bi-credit-card me-2"></i>괌세이브카드 무료 발급</h4>
                <p class="mb-2">아래 코드로 30만원 상당의 괌세이브카드를 무료로 발급받으세요!</p>
                
                <div class="savecard-code">
                    <%= voucher.savecard_code || 'LOADING...' %>
                </div>
                
                <!-- QR 코드 (향후 구현) -->
                <div class="qr-code">
                    <div id="qr-placeholder" style="width: 150px; height: 150px; background: #f8f9fa; display: flex; align-items: center; justify-content: center; border-radius: 10px;">
                        <i class="bi bi-qr-code" style="font-size: 3rem; color: #6c757d;"></i>
                    </div>
                </div>
                
                <div class="mt-3">
                    <a href="https://www.guamsavecard.com/register" target="_blank" class="btn btn-light btn-lg">
                        <i class="bi bi-arrow-right-circle me-2"></i>지금 발급받기
                    </a>
                </div>
            </div>

            <!-- 이용 안내 -->
            <div class="instructions">
                <h6 class="text-primary"><i class="bi bi-exclamation-triangle me-2"></i>이용 안내사항</h6>
                <ul class="mb-0">
                    <li>바우처는 예약 확정 후 발급되며, 현지에서 제시하시면 됩니다.</li>
                    <li>이용일 변경이나 취소는 최소 24시간 전에 연락주시기 바랍니다.</li>
                    <li>날씨나 현지 사정으로 인한 일정 변경이 있을 수 있습니다.</li>
                    <li>괌세이브카드는 괌 현지 가맹점에서 할인 혜택을 받을 수 있습니다.</li>
                    <li>분실 시 재발급이 어려우니 안전하게 보관해 주세요.</li>
                </ul>
            </div>

            <!-- 연락처 정보 -->
            <div class="row">
                <div class="col-md-6">
                    <div class="card border-primary">
                        <div class="card-header bg-primary text-white">
                            <h6 class="mb-0"><i class="bi bi-telephone me-2"></i>긴급 연락처</h6>
                        </div>
                        <div class="card-body">
                            <p class="mb-1"><strong>한국 사무소:</strong> +82-2-1234-5678</p>
                            <p class="mb-1"><strong>괌 현지:</strong> +1-671-123-4567</p>
                            <p class="mb-0"><strong>24시간 핫라인:</strong> +1-671-999-0000</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card border-info">
                        <div class="card-header bg-info text-white">
                            <h6 class="mb-0"><i class="bi bi-geo-alt me-2"></i>현지 업체 정보</h6>
                        </div>
                        <div class="card-body">
                            <p class="mb-1"><strong>업체명:</strong> <%= reservation.vendor_name || '현지 파트너' %></p>
                            <p class="mb-1"><strong>위치:</strong> 괌 투몬 지역</p>
                            <p class="mb-0"><strong>운영시간:</strong> 09:00 - 18:00</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 푸터 정보 -->
            <div class="footer-info">
                <p class="mb-1">
                    <strong>괌세이브카드</strong> | 
                    <a href="https://www.guamsavecard.com" target="_blank">www.guamsavecard.com</a>
                </p>
                <p class="mb-0">
                    발급일시: <%= new Date().toLocaleString('ko-KR') %> | 
                    바우처 유효기간: <%= reservation.usage_date ? new Date(new Date(reservation.usage_date).getTime() + 30*24*60*60*1000).toLocaleDateString('ko-KR') : '무제한' %>
                </p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // 페이지 로드 시 조회 기록
        document.addEventListener('DOMContentLoaded', function() {
            // 바우처 조회 기록 남기기
            const voucherToken = '<%= voucher.voucher_token %>';
            if (voucherToken) {
                fetch(`/api/voucher/${voucherToken}/view`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' }
                }).catch(err => console.log('조회 기록 실패:', err));
            }
        });

        // 인쇄 함수
        function printVoucher() {
            window.print();
        }

        // 저장 함수
        function saveVoucher() {
            const voucherElement = document.querySelector('.voucher-container');
            if (voucherElement) {
                // HTML을 이미지로 변환하는 기능 (향후 구현)
                alert('바우처 저장 기능은 준비 중입니다. 현재는 인쇄(Ctrl+P)를 이용해주세요.');
            }
        }
    </script>
</body>
</html>
