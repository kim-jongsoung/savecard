<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 괌세이브카드</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px 0;
        }
        
        .register-card {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            max-width: 500px;
            margin: 0 auto;
        }
        
        .register-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .register-icon {
            font-size: 4rem;
            color: #667eea;
            margin-bottom: 20px;
        }
        
        .form-control, .form-select {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            padding: 15px;
            font-size: 16px;
            margin-bottom: 20px;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .btn-register {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 25px;
            padding: 15px 30px;
            font-weight: 600;
            width: 100%;
            color: white;
            font-size: 18px;
        }
        
        .btn-register:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }
        
        .alert {
            border-radius: 10px;
            border: none;
            margin-bottom: 20px;
        }
        
        .back-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
        }
        
        .back-link:hover {
            color: #5a6fd8;
        }
        
        @media (max-width: 768px) {
            .register-card {
                margin: 10px;
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="register-card">
            <div class="register-header">
                <div class="register-icon">
                    <i class="fas fa-credit-card"></i>
                </div>
                <h2><%= title %></h2>
                <p class="text-muted">여행사 정보와 영문명을 입력하여 할인카드를 발급받으세요</p>
            </div>

            <% if (error) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle me-2"></i><%= error %>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle me-2"></i><%= success %>
                </div>
            <% } %>
            <form method="POST" action="/issue">
                <div class="mb-3">
                    <label for="customer_name" class="form-label">
                        <i class="fas fa-user me-2"></i>고객명 (영문)
                    </label>
                    <input type="text" 
                           class="form-control" 
                           id="customer_name" 
                           name="name" 
                           placeholder="Hong Gil Dong"
                           required>
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">
                        <i class="fas fa-envelope me-2"></i>이메일 (로그인 ID)
                    </label>
                    <input type="email"
                           class="form-control"
                           id="email"
                           name="email"
                           placeholder="you@example.com"
                           required>
                    <div class="form-text">
                        <i class="fas fa-info-circle me-1"></i>
                        발급 후 로그인할 때 사용하는 아이디입니다.
                    </div>
                </div>

                <div class="mb-3">
                    <label for="phone" class="form-label">
                        <i class="fas fa-phone me-2"></i>전화번호
                    </label>
                    <input type="text"
                           class="form-control"
                           id="phone"
                           name="phone"
                           placeholder="010-1234-5678"
                           required>
                </div>

                <div class="mb-4">
                    <label for="agency_code" class="form-label">
                        <i class="fas fa-building me-2"></i>여행사
                    </label>
                    <select class="form-select" id="agency_code" name="agency_code" required <% if (selectedAgency) { %>disabled<% } %>>
                        <% if (!selectedAgency) { %>
                            <option value="">여행사를 선택해주세요</option>
                        <% } %>
                        <% agencies.forEach(agency => { %>
                            <option value="<%= agency.code %>" <% if (selectedAgency && (selectedAgency.code === agency.code || selectedAgency.agency_code === agency.code)) { %>selected<% } %>>
                                <%= agency.name %> (<%= agency.code %>)
                            </option>
                        <% }); %>
                    </select>
                    <% if (selectedAgency) { %>
                        <!-- 제휴 여행사가 선택된 경우 숨겨진 필드로 값 전송 -->
                        <input type="hidden" name="agency_code" value="<%= selectedAgency.code || selectedAgency.agency_code %>">
                        <div class="form-text text-success">
                            <i class="fas fa-check-circle me-1"></i>
                            <strong><%= selectedAgency.name %></strong> 제휴 여행사로 자동 선택되었습니다.
                        </div>
                    <% } %>
                </div>

                <button type="submit" class="btn btn-register">
                    <i class="fas fa-plus me-2"></i>할인카드 발급받기
                </button>
            </form>

            <div class="text-center mt-4">
                <a href="/" class="back-link">
                    <i class="fas fa-arrow-left me-2"></i>메인 페이지로 돌아가기
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // 영문명 검증 함수
        function validateEnglishName(name) {
            // 영문자와 공백만 허용
            const englishNameRegex = /^[a-zA-Z\s]+$/;
            return englishNameRegex.test(name.trim());
        }
        
        // 폼 제출 시 검증 및 로딩 표시
        document.querySelector('form').addEventListener('submit', function(e) {
            const customerNameInput = document.getElementById('customer_name');
            const customerName = customerNameInput.value.trim();
            const phoneInput = document.getElementById('phone');
            const phone = phoneInput.value.trim();
            
            // 영문명 검증
            if (customerName && !validateEnglishName(customerName)) {
                e.preventDefault();
                alert('고객명은 영문으로만 입력해주세요 (예: Hong Gil Dong)');
                customerNameInput.focus();
                return;
            }
            
            // 전화번호 간단 검증
            const phoneRegex = /^[0-9\-+()\s]{6,20}$/;
            if (!phoneRegex.test(phone)) {
                e.preventDefault();
                alert('전화번호를 올바르게 입력해주세요.');
                phoneInput.focus();
                return;
            }
            
            const submitBtn = document.querySelector('.btn-register');
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>발급 중...';
            submitBtn.disabled = true;
        });
        
        // 실시간 입력 검증
        document.getElementById('customer_name').addEventListener('input', function() {
            const name = this.value.trim();
            const isValid = name === '' || validateEnglishName(name);
            
            if (!isValid) {
                this.style.borderColor = '#dc3545';
                if (!document.getElementById('name-warning')) {
                    const warning = document.createElement('small');
                    warning.id = 'name-warning';
                    warning.className = 'text-danger';
                    warning.innerHTML = '<i class="fas fa-exclamation-triangle me-1"></i>영문자만 입력해주세요';
                    this.parentNode.appendChild(warning);
                }
            } else {
                this.style.borderColor = '';
                const warning = document.getElementById('name-warning');
                if (warning) {
                    warning.remove();
                }
            }
        });
    </script>

    
    <footer style="text-align: center; padding: 20px 0; font-size: 14px; color: #ffffff; background: rgba(0,0,0,0.1); margin-top: 30px; border-top: 1px solid rgba(255,255,255,0.2); text-shadow: 1px 1px 2px rgba(0,0,0,0.5);">
        &copy; 2025 luxfind. All rights reserved. Developed by Kim Jongsoung.
    </footer>
</body>
</html>
