<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 괌세이브카드</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px 0;
        }
        
        .register-card {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            max-width: 500px;
            margin: 0 auto;
        }
        
        .register-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .register-icon {
            font-size: 4rem;
            color: #667eea;
            margin-bottom: 20px;
        }
        
        .form-control, .form-select {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            padding: 15px;
            font-size: 16px;
            margin-bottom: 20px;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .btn-register {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 25px;
            padding: 15px 30px;
            font-weight: 600;
            width: 100%;
            color: white;
            font-size: 18px;
        }
        
        .btn-register:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }
        
        .alert {
            border-radius: 10px;
            border: none;
            margin-bottom: 20px;
        }
        
        .back-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
        }
        
        .back-link:hover {
            color: #5a6fd8;
        }
        
        @media (max-width: 768px) {
            .register-card {
                margin: 10px;
                padding: 30px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="register-card">
            <div class="register-header">
                <div class="register-icon">
                    <i class="fas fa-credit-card"></i>
                </div>
                <h2><%= title %></h2>
                <p class="text-muted">여행사 정보와 영문명을 입력하여 할인카드를 발급받으세요</p>
            </div>

            <% if (error) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle me-2"></i><%= error %>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle me-2"></i><%= success %>
                </div>
            <% } %>

            <form method="POST" action="/register">
                <div class="mb-3">
                    <label for="customer_name" class="form-label">
                        <i class="fas fa-user me-2"></i>고객명 (영문)
                    </label>
                    <input type="text" 
                           class="form-control" 
                           id="customer_name" 
                           name="customer_name" 
                           placeholder="Hong Gil Dong"
                           required>
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">
                        <i class="fas fa-envelope me-2"></i>이메일 (로그인 ID)
                    </label>
                    <input type="email"
                           class="form-control"
                           id="email"
                           name="email"
                           placeholder="you@example.com"
                           required>
                    <div class="form-text">
                        <i class="fas fa-info-circle me-1"></i>
                        발급 후 로그인할 때 사용하는 아이디입니다.
                    </div>
                </div>

                <div class="mb-4">
                    <label for="agency_code" class="form-label">
                        <i class="fas fa-building me-2"></i>여행사
                    </label>
                    <select class="form-select" id="agency_code" name="agency_code" required>
                        <option value="">여행사를 선택해주세요</option>
                        <% agencies.forEach(agency => { %>
                            <option value="<%= agency.agency_code %>">
                                <%= agency.name %> (<%= agency.agency_code %>)
                            </option>
                        <% }); %>
                    </select>
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label">
                        <i class="fas fa-lock me-2"></i>카드 비밀번호 (4자리 숫자)
                    </label>
                    <input type="number" 
                           class="form-control" 
                           id="password" 
                           name="password" 
                           placeholder="4자리 숫자 입력 (예: 1234)"
                           maxlength="4"
                           pattern="[0-9]{4}"
                           title="4자리 숫자를 입력하세요"
                           required>
                    <div class="form-text">
                        <i class="fas fa-info-circle me-1"></i>
                        카드 사용 시 필요한 4자리 숫자 비밀번호를 입력하세요 (예: 1234, 5678)
                    </div>
                    <div id="passwordError" class="text-danger mt-1" style="display: none;">
                        <i class="fas fa-exclamation-triangle me-1"></i>
                        4자리 숫자만 입력 가능합니다
                    </div>
                </div>

                <div class="mb-4">
                    <label for="password_confirm" class="form-label">
                        <i class="fas fa-lock me-2"></i>비밀번호 확인
                    </label>
                    <input type="number" 
                           class="form-control" 
                           id="password_confirm" 
                           name="password_confirm" 
                           placeholder="위에서 입력한 4자리 숫자 다시 입력"
                           maxlength="4"
                           pattern="[0-9]{4}"
                           title="위에서 입력한 비밀번호와 동일하게 입력하세요"
                           required>
                    <div id="passwordMatchError" class="text-danger mt-1" style="display: none;">
                        <i class="fas fa-exclamation-triangle me-1"></i>
                        비밀번호가 일치하지 않습니다
                    </div>
                </div>

                <button type="submit" class="btn btn-register">
                    <i class="fas fa-plus me-2"></i>할인카드 발급받기
                </button>
            </form>

            <div class="text-center mt-4">
                <a href="/" class="back-link">
                    <i class="fas fa-arrow-left me-2"></i>메인 페이지로 돌아가기
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // 영문명 검증 함수
        function validateEnglishName(name) {
            // 영문자와 공백만 허용
            const englishNameRegex = /^[a-zA-Z\s]+$/;
            return englishNameRegex.test(name.trim());
        }
        
        // 4자리 숫자 비밀번호 검증 함수
        function validatePassword(password) {
            // 4자리 숫자만 허용
            const passwordRegex = /^[0-9]{4}$/;
            return passwordRegex.test(password);
        }
        
        // 비밀번호 일치 검증 함수
        function validatePasswordMatch(password, confirmPassword) {
            return password === confirmPassword;
        }
        
        // 폼 제출 시 검증 및 로딩 표시
        document.querySelector('form').addEventListener('submit', function(e) {
            const customerNameInput = document.getElementById('customer_name');
            const customerName = customerNameInput.value.trim();
            const passwordInput = document.getElementById('password');
            const passwordConfirmInput = document.getElementById('password_confirm');
            const password = passwordInput.value.trim();
            const passwordConfirm = passwordConfirmInput.value.trim();
            
            // 영문명 검증
            if (customerName && !validateEnglishName(customerName)) {
                e.preventDefault();
                alert('고객명은 영문으로만 입력해주세요 (예: Hong Gil Dong)');
                customerNameInput.focus();
                return;
            }
            
            // 비밀번호 형식 검증
            if (!validatePassword(password)) {
                e.preventDefault();
                alert('비밀번호는 4자리 숫자로 입력해주세요 (예: 1234)');
                passwordInput.focus();
                return;
            }
            
            // 비밀번호 일치 검증
            if (!validatePasswordMatch(password, passwordConfirm)) {
                e.preventDefault();
                alert('비밀번호가 일치하지 않습니다. 다시 확인해주세요.');
                passwordConfirmInput.focus();
                return;
            }
            
            const submitBtn = document.querySelector('.btn-register');
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>발급 중...';
            submitBtn.disabled = true;
        });
        
        // 실시간 비밀번호 검증
        document.getElementById('password').addEventListener('input', function(e) {
            const password = e.target.value;
            const errorDiv = document.getElementById('passwordError');
            
            if (password.length > 0 && !validatePassword(password)) {
                errorDiv.style.display = 'block';
                e.target.classList.add('is-invalid');
            } else {
                errorDiv.style.display = 'none';
                e.target.classList.remove('is-invalid');
                
                // 비밀번호 확인 필드도 다시 검증
                const confirmInput = document.getElementById('password_confirm');
                const confirmPassword = confirmInput.value;
                if (confirmPassword.length > 0) {
                    validatePasswordConfirmField();
                }
            }
        });
        
        // 실시간 비밀번호 확인 검증
        document.getElementById('password_confirm').addEventListener('input', function(e) {
            validatePasswordConfirmField();
        });
        
        function validatePasswordConfirmField() {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('password_confirm').value;
            const errorDiv = document.getElementById('passwordMatchError');
            const confirmInput = document.getElementById('password_confirm');
            
            if (confirmPassword.length > 0 && !validatePasswordMatch(password, confirmPassword)) {
                errorDiv.style.display = 'block';
                confirmInput.classList.add('is-invalid');
            } else {
                errorDiv.style.display = 'none';
                confirmInput.classList.remove('is-invalid');
            }
        }
        
        // 실시간 입력 검증
        document.getElementById('customer_name').addEventListener('input', function() {
            const name = this.value.trim();
            const isValid = name === '' || validateEnglishName(name);
            
            if (!isValid) {
                this.style.borderColor = '#dc3545';
                if (!document.getElementById('name-warning')) {
                    const warning = document.createElement('small');
                    warning.id = 'name-warning';
                    warning.className = 'text-danger';
                    warning.innerHTML = '<i class="fas fa-exclamation-triangle me-1"></i>영문자만 입력해주세요';
                    this.parentNode.appendChild(warning);
                }
            } else {
                this.style.borderColor = '';
                const warning = document.getElementById('name-warning');
                if (warning) {
                    warning.remove();
                }
            }
        });
    </script>

    
    <footer style="text-align: center; padding: 20px 0; font-size: 14px; color: #ffffff; background: rgba(0,0,0,0.1); margin-top: 30px; border-top: 1px solid rgba(255,255,255,0.2); text-shadow: 1px 1px 2px rgba(0,0,0,0.5);">
        &copy; 2025 luxfind. All rights reserved. Developed by Kim Jongsoung.
    </footer>
</body>
</html>
