<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 괌세이브카드</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #87CEEB 0%, #4FC3F7 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        .hero-section {
            text-align: center;
            padding: 30px 15px 40px;
            color: white;
            position: relative;
        }
        
        .travel-icons {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        
        .travel-icon {
            font-size: 1.8rem;
            color: rgba(255, 255, 255, 0.8);
            animation: float 3s ease-in-out infinite;
        }
        
        /* Desktop styles */
        @media (min-width: 768px) {
            .hero-section {
                padding: 60px 20px 80px;
            }
            
            .travel-icons {
                gap: 30px;
                margin-bottom: 30px;
            }
            
            .travel-icon {
                font-size: 2.5rem;
            }
        }
        
        .travel-icon:nth-child(2) { animation-delay: 0.5s; }
        .travel-icon:nth-child(3) { animation-delay: 1s; }
        .travel-icon:nth-child(4) { animation-delay: 1.5s; }
        .travel-icon:nth-child(5) { animation-delay: 2s; }
        .travel-icon:nth-child(6) { animation-delay: 2.5s; }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        
        .hero-title {
            font-size: 2.2rem;
            font-weight: 900;
            margin-bottom: 15px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            letter-spacing: 3px;
            font-family: 'Arial Black', Arial, sans-serif;
            line-height: 1.2;
        }
        
        .hero-subtitle {
            font-size: 1rem;
            margin-bottom: 25px;
            opacity: 0.95;
            font-weight: 300;
            line-height: 1.4;
            padding: 0 10px;
        }
        
        /* Desktop styles */
        @media (min-width: 768px) {
            .hero-title {
                font-size: 4rem;
                margin-bottom: 20px;
                letter-spacing: 8px;
            }
            
            .hero-subtitle {
                font-size: 1.4rem;
                margin-bottom: 40px;
                padding: 0;
            }
        }
        
        .feature-card {
            background: white;
            border-radius: 15px;
            padding: 20px;
            margin: 15px 10px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
        }
        
        .feature-icon {
            font-size: 2.2rem;
            color: #667eea;
            margin-bottom: 15px;
        }
        
        .btn-hero {
            background: white;
            color: #667eea;
            border: none;
            border-radius: 25px;
            padding: 12px 25px;
            font-size: 1rem;
            font-weight: 600;
            margin: 8px 5px;
            transition: all 0.3s ease;
            min-height: 44px;
            width: 100%;
            max-width: 280px;
        }
        
        .cta-buttons .btn {
            padding: 12px 20px;
            font-size: 1rem;
            border-radius: 25px;
            margin: 8px 5px;
            transition: all 0.3s ease;
            border: 2px solid white;
            min-height: 44px;
            width: 100%;
            max-width: 280px;
        }
        
        /* Desktop styles */
        @media (min-width: 768px) {
            .feature-card {
                border-radius: 20px;
                padding: 30px;
                margin: 20px 0;
                box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            }
            
            .feature-card:hover {
                transform: translateY(-10px);
            }
            
            .feature-icon {
                font-size: 3rem;
                margin-bottom: 20px;
            }
            
            .btn-hero {
                border-radius: 50px;
                padding: 15px 40px;
                font-size: 1.2rem;
                margin: 10px;
                width: auto;
            }
            
            .cta-buttons .btn {
                padding: 15px 30px;
                font-size: 1.1rem;
                border-radius: 50px;
                margin: 10px;
                width: auto;
            }
        }
        
        .cta-buttons .btn-primary {
            background: rgba(255, 255, 255, 0.2);
            border-color: white;
            color: white;
            backdrop-filter: blur(10px);
        }
        
        .cta-buttons .btn-primary:hover {
            background: white;
            color: #4FC3F7;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }
        
        .cta-buttons .btn-outline-light {
            background: transparent;
            border-color: white;
            color: white;
        }
        
        .cta-buttons .btn-outline-light:hover {
            background: white;
            color: #4FC3F7;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }
        
        .stats-section {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            margin: 40px 0;
            text-align: center;
        }
        
        .stat-item {
            color: white;
            margin: 20px 0;
        }
        
        .stat-number {
            font-size: 2.5rem;
            font-weight: bold;
            display: block;
        }
        
        .stat-label {
            font-size: 1rem;
            opacity: 0.8;
        }
        
        .banner-section {
            margin: 40px 0;
        }
        
        .banner-item {
            margin-bottom: 20px;
        }
        
        .banner-card {
            background: rgba(255,255,255,0.95);
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.3s ease;
            cursor: pointer;
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
            border: 2px solid rgba(255,255,255,0.3);
            height: 100%;
            display: flex;
            flex-direction: column;
        }
        
        .banner-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.25);
            border-color: #4FC3F7;
        }
        
        .banner-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            transition: all 0.3s ease;
            flex-shrink: 0;
        }
        
        .banner-card:hover .banner-image {
            transform: scale(1.05);
        }
        
        .banner-content {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }
        
        .banner-name {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 10px;
            color: #333;
            line-height: 1.3;
        }
        
        .banner-description {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 15px;
            line-height: 1.4;
            flex-grow: 1;
        }
        
        .banner-action {
            font-size: 0.9rem;
            font-weight: 600;
            color: #4FC3F7;
            display: flex;
            align-items: center;
            margin-top: auto;
            padding-top: 10px;
            border-top: 1px solid rgba(0,0,0,0.1);
        }
        
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
                letter-spacing: 4px;
            }
            
            .hero-subtitle {
                font-size: 1.1rem;
            }
            
            .travel-icons {
                gap: 20px;
                margin-bottom: 20px;
            }
            
            .travel-icon {
                font-size: 2rem;
            }
            
            .banner-image {
                max-height: 100px;
            }
            
            .feature-card {
                margin: 10px 0;
                padding: 20px;
            }
            
            .cta-buttons {
                display: flex;
                flex-direction: column;
                align-items: center; /* 가운데 정렬 */
                gap: 12px;
            }
            .cta-buttons .btn {
                display: block;
                width: 90%;            /* 동일한 가로폭 */
                max-width: 340px;     /* 최대폭 제한으로 일관성 */
                margin: 0 auto;       /* 좌우 동일 여백 */
            }
            
            .banner-image {
                height: 160px;
            }
            
            .banner-content {
                padding: 15px;
            }
            
            .banner-name {
                font-size: 1.1rem;
            }
        }
        
        /* 제휴업체 신청 배너 스타일 */
        .partner-application-banner {
            margin: 30px 0;
        }
        
        .partner-banner-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 20px;
            padding: 30px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
            border: 2px solid rgba(255,255,255,0.2);
            color: white;
        }
        
        .partner-banner-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
            border-color: rgba(255,255,255,0.3);
        }
        
        .btn-partner-apply {
            background: rgba(255,255,255,0.2);
            color: white;
            border: 2px solid rgba(255,255,255,0.3);
            padding: 12px 24px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .btn-partner-apply:hover {
            background: white;
            color: #667eea;
            border-color: white;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Hero Section -->
        <div class="hero-section">
            <!-- Travel Icons Row -->
            <div class="travel-icons">
                <div class="travel-icon"><i class="fas fa-plane"></i></div>
                <div class="travel-icon"><i class="fas fa-suitcase-rolling"></i></div>
                <div class="travel-icon"><i class="fas fa-fish"></i></div>
                <div class="travel-icon"><i class="fas fa-shopping-bag"></i></div>
                <div class="travel-icon"><i class="fas fa-palm-tree"></i></div>
                <div class="travel-icon"><i class="fas fa-sailboat"></i></div>
            </div>
            
            <% if (partnerAgency) { %>
                <!-- 제휴 여행사 로고만 표시 -->
                <div class="partner-branding mb-3">
                    <% if (partnerAgency.logo_url) { %>
                        <img src="<%= partnerAgency.logo_url %>" alt="<%= partnerAgency.name %> 로고" 
                             style="max-height: 80px; margin-bottom: 20px;" class="partner-logo">
                    <% } %>
                </div>
            <% } %>
            
            <h1 class="hero-title">
                GUAM SAVE
                <% if (partnerAgency) { %>
                    <small class="text-light opacity-75 d-block" style="font-size: 0.5em; margin-top: 10px;">
                        (<%= partnerAgency.name %>)
                    </small>
                <% } %>
            </h1>
            <p class="hero-subtitle">괌 여행의 모든 할인 혜택을 한 번에!</p>
            <div class="cta-buttons">
                <a href="<% if (partnerAgency) { %>/register?agency=<%= partnerAgency.code %><% } else { %>/register<% } %>" class="btn btn-primary btn-lg me-3">
                    <i class="fas fa-credit-card me-2"></i>할인카드 발급받기
                </a>
                <a href="/my-card" class="btn btn-outline-light btn-lg me-3">
                    <i class="fas fa-id-card me-2"></i>내 카드 보기
                </a>
                <a href="/stores" class="btn btn-outline-light btn-lg me-3">
                    <i class="fas fa-store me-2"></i>제휴업체 보기
                </a>
                <a href="#features" class="btn btn-outline-light btn-lg">
                    <i class="fas fa-info-circle me-2"></i>자세히 알아보기
                </a>
            </div>
        </div>

        <!-- Features Section -->
        <div id="features" class="row">
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-qrcode"></i>
                    </div>
                    <h4>간편한 QR 할인</h4>
                    <p>QR 코드 하나로 괌 전역의 제휴처에서 할인 혜택을 받으세요. 복잡한 쿠폰은 이제 그만!</p>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-store"></i>
                    </div>
                    <h5>다양한 제휴업체</h5>
                    <p>쇼핑, 레스토랑, 관광지 등 괌의 모든 주요 업체에서 할인 혜택을 제공합니다.</p>
                </div>
            </div>
            
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h5>간편한 사용</h5>
                    <p>QR 코드 스캔만으로 즉시 할인 혜택을 받을 수 있습니다.</p>
                </div>
            </div>
        </div>

        <!-- Stats Section -->
        <div class="stats-section">
            <div class="row">
                <div class="col-md-3 col-6">
                    <div class="stat-item">
                        <span class="stat-number">50+</span>
                        <span class="stat-label">제휴 매장</span>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="stat-item">
                        <span class="stat-number">1000+</span>
                        <span class="stat-label">발급된 카드</span>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="stat-item">
                        <span class="stat-number">5000+</span>
                        <span class="stat-label">할인 사용</span>
                    </div>
                </div>
                <div class="col-md-3 col-6">
                    <div class="stat-item">
                        <span class="stat-number">98%</span>
                        <span class="stat-label">만족도</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Banner Section -->
        <% if (typeof banners !== 'undefined' && banners && banners.length > 0) { %>
        <div class="banner-section">
            <div class="row">
                <% banners.forEach(function(banner) { %>
                    <div class="col-md-6 col-lg-4 banner-item">
                        <div class="banner-card" data-link="<%= banner.link_url || '' %>" onclick="trackBannerClick(<%= banner.id %>, this.dataset.link)">
                            <img class="banner-image" src="<%= banner.image_url %>" alt="<%= banner.advertiser_name %>">
                            <div class="banner-content">
                                <div class="banner-name"><%= banner.title || banner.advertiser_name %></div>
                                <% if (banner.description) { %>
                                    <div class="banner-description"><%= banner.description %></div>
                                <% } %>
                                <div class="banner-action">
                                    <span>자세히 보기</span>
                                    <i class="fas fa-arrow-right ms-2"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        <% } %>

        <!-- CTA Section -->
        <div class="text-center mb-5">
            <h3 class="text-white mb-4">지금 바로 괌세이브카드를 발급받으세요!</h3>
            <a href="/register" class="btn btn-hero btn-lg">
                <i class="fas fa-rocket me-2"></i>무료 발급 시작하기
            </a>
        </div>
        
        <!-- Footer 제휴업체 신청 배너 -->
        <div class="partner-application-banner mt-5 mb-4">
            <div class="partner-banner-card" data-bs-toggle="modal" data-bs-target="#partnerApplicationModal">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h4 class="mb-2">
                            <i class="fas fa-store me-2"></i>한국인 관광객에게 매장을 알리고 혜택을 제공하세요
                        </h4>
                        <p class="mb-1">괌세이브카드에 입점하면 매출이 달라집니다.</p>
                        <small class="text-light">Promote your store to Korean tourists and increase your sales.<br>Join Guam Save Card and see the difference in your business.</small>
                    </div>
                    <div class="col-md-4 text-end">
                        <button class="btn btn-partner-apply">
                            <i class="fas fa-handshake me-2"></i>Apply for Partnership
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 제휴업체 신청 모달 -->
    <div class="modal fade" id="partnerApplicationModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">
                        <i class="fas fa-handshake me-2"></i>제휴업체 신청
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="text-center mb-4">
                        <h6 class="text-primary">괌세이브카드와 함께 성장할 비즈니스 파트너를 찾고 있습니다</h6>
                        <small class="text-muted">We are looking for business partners to grow with Guam Save Card</small>
                    </div>
                    
                    <form id="partnerApplicationForm">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label">
                                    <i class="fas fa-store me-2"></i>업체명 / Business Name *
                                </label>
                                <input type="text" class="form-control" name="businessName" required 
                                       placeholder="예: ABC Restaurant">
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label">
                                    <i class="fas fa-user me-2"></i>담당자명 / Contact Name *
                                </label>
                                <input type="text" class="form-control" name="contactName" required 
                                       placeholder="예: John Smith">
                            </div>
                        </div>
                        
                        <div class="mb-3">
                            <label class="form-label">
                                <i class="fas fa-envelope me-2"></i>이메일 / Email *
                            </label>
                            <input type="email" class="form-control" name="email" required 
                                   placeholder="예: contact@business.com">
                        </div>
                        
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle me-2"></i>
                            신청서 접수 후 담당자가 직접 연락드리겠습니다.<br>
                            <small>We will contact you directly after receiving your application.</small>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">취소</button>
                    <button type="button" class="btn btn-primary" onclick="submitPartnerApplication()">
                        <i class="fas fa-paper-plane me-2"></i>신청하기
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // 배너 클릭 추적 함수
        async function trackBannerClick(bannerId, linkUrl) {
            try {
                // 클릭 수 증가 API 호출
                await fetch(`/banner/click/${bannerId}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                // 링크가 있으면 새 탭에서 열기
                if (linkUrl && linkUrl.trim() !== '') {
                    window.open(linkUrl, '_blank');
                }
            } catch (error) {
                console.error('배너 클릭 추적 오류:', error);
                // 오류가 발생해도 링크는 열기
                if (linkUrl && linkUrl.trim() !== '') {
                    window.open(linkUrl, '_blank');
                }
            }
        }

        // 부드러운 스크롤
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
        
        // 제휴업체 신청 제출 함수
        async function submitPartnerApplication() {
            const form = document.getElementById('partnerApplicationForm');
            const formData = new FormData(form);
            
            const data = {
                businessName: formData.get('businessName'),
                email: formData.get('email'),
                contactName: formData.get('contactName')
            };
            
            // 입력 검증
            if (!data.businessName || !data.email || !data.contactName) {
                alert('모든 필드를 입력해주세요.');
                return;
            }
            
            try {
                const response = await fetch('/partner-application', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(data)
                });
                
                if (response.ok) {
                    alert('신청이 성공적으로 접수되었습니다. 담당자가 연락드릴 예정입니다.');
                    form.reset();
                    const modal = bootstrap.Modal.getInstance(document.getElementById('partnerApplicationModal'));
                    modal.hide();
                } else {
                    throw new Error('신청 접수에 실패했습니다.');
                }
            } catch (error) {
                console.error('신청 오류:', error);
                alert('신청 접수 중 오류가 발생했습니다. 다시 시도해주세요.');
            }
        }
    </script>

    
    <footer style="text-align: center; padding: 20px 0; font-size: 14px; color: #ffffff; background: rgba(0,0,0,0.1); margin-top: 30px; border-top: 1px solid rgba(255,255,255,0.2); text-shadow: 1px 1px 2px rgba(0,0,0,0.5);">
        &copy; 2025 luxfind. All rights reserved. Developed by Kim Jongsoung.
    </footer>
</body>
</html>
