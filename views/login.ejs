<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - 괌세이브카드</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body { 
      background: linear-gradient(135deg, #87CEEB 0%, #4FC3F7 100%);
      min-height: 100vh;
      margin: 0;
      padding: 0;
    }
    .login-card { max-width: 420px; margin: 40px auto; background: #fff; border-radius: 16px; padding: 28px; box-shadow: 0 12px 30px rgba(0,0,0,0.08); }
    .btn-login { width: 100%; border-radius: 12px; padding: 12px 16px; background: linear-gradient(45deg,#667eea,#764ba2); border: 0; }
    .form-control { border-radius: 10px; padding: 12px 14px; }
  </style>
</head>
<body>
  <!-- 네비게이션 포함 -->
  <%- include('partials/mobile-nav') %>
  <%- include('partials/desktop-nav') %>
  <div class="container">
    <div class="login-card">
      <div class="text-center mb-3">
        <i class="fas fa-user-circle fa-3x text-primary mb-2"></i>
        <h4 class="mb-0">내 카드 로그인</h4>
      </div>

      <% if (error) { %>
      <div class="alert alert-danger"><i class="fas fa-exclamation-triangle me-1"></i><%= error %></div>
      <% } %>
      <% if (success) { %>
      <div class="alert alert-success"><i class="fas fa-check-circle me-1"></i><%= success %></div>
      <% } %>

      <form method="POST" action="/login">
        <div class="mb-3">
          <label for="email" class="form-label"><i class="fas fa-envelope me-2"></i>이메일</label>
          <input type="email" id="email" name="email" class="form-control" placeholder="you@example.com" required>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label"><i class="fas fa-lock me-2"></i>비밀번호 (4자리)</label>
          <input type="password" inputmode="numeric" pattern="[0-9]{4}" maxlength="4" id="password" name="password" class="form-control" placeholder="4자리 숫자" required>
          <div class="form-text"><i class="fas fa-info-circle me-1"></i>발급 시 설정한 4자리 숫자 비밀번호</div>
        </div>
        <button class="btn btn-login text-white" type="submit"><i class="fas fa-right-to-bracket me-2"></i>로그인</button>
      </form>

    </div>
  </div>
  <footer style="text-align:center;padding:16px 0;font-size:14px;color:#666">&copy; 2025 luxfind. All rights reserved.</footer>
</body>
</html>
