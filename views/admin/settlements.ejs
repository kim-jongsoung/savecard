<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>정산관리 - 괌세이브카드 관리자</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/erp-common.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* PC 화면에서 가로폭 1300px 고정 */
        @media (min-width: 1200px) {
            .container-fluid {
                max-width: 1300px !important;
                margin: 0 auto;
            }
        }
        
        .status-badge {
            font-size: 0.8rem;
            padding: 0.25rem 0.5rem;
        }
        .status-settled { background-color: #28a745; color: #fff; }
        .status-pending { background-color: #ffc107; color: #000; }
        .status-overdue { background-color: #dc3545; color: #fff; }
        
        .settlement-card {
            border-left: 4px solid #dee2e6;
            transition: all 0.3s ease;
        }
        .settlement-card:hover {
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .settlement-card.settled { border-left-color: #28a745; }
        .settlement-card.pending { border-left-color: #ffc107; }
        .settlement-card.overdue { border-left-color: #dc3545; }
        
        .stats-card {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            color: #333;
            border-radius: 15px;
            border: 1px solid #dee2e6;
        }
        
        .stats-card .card-body {
            padding: 1rem;
        }
        
        .stats-card h3 {
            margin-bottom: 0.25rem;
            font-size: 1.5rem;
        }
        
        .stats-card h5 {
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }
        
        .stats-card .fa-2x {
            font-size: 1.5rem;
        }
        
        .stats-card small {
            font-size: 0.75rem;
        }
        
        .profit-positive { color: #28a745; font-weight: bold; }
        .profit-negative { color: #dc3545; font-weight: bold; }
        .profit-neutral { color: #6c757d; font-weight: bold; }
    </style>
</head>
<body>
    <%- include('../partials/navbar', { currentPage: 'settlements' }) %>
    
    <div class="container-fluid mt-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2><i class="fas fa-calculator me-2"></i>정산관리</h2>
                    <div class="d-flex gap-2">
                        <button class="btn btn-outline-info" onclick="openExchangeRateModal()">
                            <i class="fas fa-exchange-alt me-1"></i>환율 관리
                        </button>
                        <button class="btn btn-outline-primary" onclick="openBulkCalculateModal()">
                            <i class="fas fa-calculator me-1"></i>대량 정산 계산
                        </button>
                    </div>
                </div>

                <!-- 정산 목록 -->
                        <!-- 정산 통계 -->
                <!-- 첫번째 행: 미입금/미송금 -->
                <div class="row mb-3">
                    <div class="col-md-6">
                        <div class="card stats-card border-warning">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-center mb-2">
                                    <h5 class="mb-0"><i class="fas fa-arrow-down text-warning me-2"></i>미입금 거래액</h5>
                                    <small class="text-muted">현재 시점</small>
                                </div>
                                <h3 class="text-warning mb-2" id="unpaidRevenue">₩0</h3>
                                <div id="unpaidByPlatform" class="small text-muted">
                                    <!-- 업체별 미입금액 표시 -->
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card stats-card border-danger">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-center mb-2">
                                    <h5 class="mb-0"><i class="fas fa-arrow-up text-danger me-2"></i>미송금 매입액</h5>
                                    <small class="text-muted">현재 시점</small>
                                </div>
                                <h3 class="text-danger mb-2" id="unpaidCost">₩0</h3>
                                <div id="unpaidByVendor" class="small text-muted">
                                    <!-- 업체별 미송금액 표시 -->
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 정산 상태 탭 -->
                <ul class="nav nav-pills mb-3" id="settlementStatusTabs" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="incomplete-tab" data-bs-toggle="pill" data-bs-target="#incompletePane" type="button" role="tab" onclick="loadSettlementsByStatus('incomplete')">
                            <i class="fas fa-clock me-1"></i>미완료 <span class="badge bg-warning text-dark ms-1" id="incompleteCount">0</span>
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="completed-tab" data-bs-toggle="pill" data-bs-target="#completedPane" type="button" role="tab" onclick="loadSettlementsByStatus('completed')">
                            <i class="fas fa-check-circle me-1"></i>전체완료 <span class="badge bg-success ms-1" id="completedCount">0</span>
                        </button>
                    </li>
                </ul>

                <!-- 필터 및 검색 -->
                <div class="card mb-4">
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-3">
                                <label class="form-label small">기간 조회</label>
                                <div class="input-group">
                                    <input type="date" class="form-control form-control-sm" id="startDate">
                                    <span class="input-group-text">~</span>
                                    <input type="date" class="form-control form-control-sm" id="endDate">
                                </div>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label small">예약업체</label>
                                <select class="form-select form-select-sm" id="platformFilter">
                                    <option value="">전체</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label small">수배업체</label>
                                <select class="form-select form-select-sm" id="vendorFilter">
                                    <option value="">전체</option>
                                </select>
                            </div>
                            <div class="col-md-1">
                                <label class="form-label small">입금</label>
                                <select class="form-select form-select-sm" id="paymentReceivedFilter">
                                    <option value="">전체</option>
                                    <option value="completed">완료</option>
                                    <option value="pending">미완료</option>
                                </select>
                            </div>
                            <div class="col-md-1">
                                <label class="form-label small">송금</label>
                                <select class="form-select form-select-sm" id="paymentSentFilter">
                                    <option value="">전체</option>
                                    <option value="completed">완료</option>
                                    <option value="pending">미완료</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label small">담당직원</label>
                                <select class="form-select form-select-sm" id="staffFilter">
                                    <option value="">전체</option>
                                </select>
                            </div>
                            <div class="col-md-1">
                                <label class="form-label small">&nbsp;</label>
                                <button class="btn btn-outline-primary btn-sm w-100" onclick="searchSettlements()">
                                    <i class="fas fa-search"></i>
                                </button>
                            </div>
                        </div>
                        <div class="row g-3 mt-2">
                            <div class="col-md-10">
                                <label class="form-label small">손님이름/상품명 검색</label>
                                <input type="text" class="form-control form-control-sm" id="searchInput" placeholder="손님이름 또는 상품명 입력...">
                            </div>
                            <div class="col-md-1">
                                <label class="form-label small">&nbsp;</label>
                                <button class="btn btn-outline-secondary btn-sm w-100" onclick="resetFilters()">
                                    <i class="fas fa-undo"></i>
                                </button>
                            </div>
                            <div class="col-md-1">
                                <label class="form-label small">&nbsp;</label>
                                <button class="btn btn-success btn-sm w-100" onclick="exportSettlements()">
                                    <i class="fas fa-download"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 정산 목록 탭 컨텐츠 -->
                <div class="tab-content" id="settlementStatusContent">
                    <!-- 미완료 목록 -->
                    <div class="tab-pane fade show active" id="incompletePane" role="tabpanel">
                        <div id="incompleteList">
                            <!-- 동적으로 로드됨 -->
                        </div>
                    </div>
                    
                    <!-- 전체완료 목록 -->
                    <div class="tab-pane fade" id="completedPane" role="tabpanel">
                        <div id="completedList">
                            <!-- 동적으로 로드됨 -->
                        </div>
                    </div>
                </div>

                <!-- 페이징 -->
                <nav aria-label="정산 목록 페이징">
                    <ul class="pagination justify-content-center" id="pagination">
                        <!-- 동적으로 로드됨 -->
                    </ul>
                </nav>
            </div>
        </div>
    </div>

    <!-- 정산 처리 모달 -->
    <div class="modal fade" id="settlementModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">정산 처리</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="settlementForm">
                        <input type="hidden" id="reservationId">
                        <div class="row">
                            <div class="col-md-6">
                                <label class="form-label">거래액 금액</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input type="number" class="form-control" id="saleAmount" step="0.01" required>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">매입 금액</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input type="number" class="form-control" id="costAmount" step="0.01" required>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-6">
                                <label class="form-label">마진</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input type="number" class="form-control" id="profitAmount" readonly>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">마진율</label>
                                <div class="input-group">
                                    <input type="number" class="form-control" id="profitRate" readonly>
                                    <span class="input-group-text">%</span>
                                </div>
                            </div>
                        </div>
                        <div class="mt-3">
                            <label class="form-label">정산 메모</label>
                            <textarea class="form-control" id="settlementNotes" rows="3" placeholder="정산 관련 메모를 입력하세요..."></textarea>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">취소</button>
                    <button type="button" class="btn btn-success" onclick="processSettlement()">정산 처리</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 환율 관리 모달 -->
    <div class="modal fade" id="exchangeRateModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="fas fa-exchange-alt me-2"></i>환율 관리</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <!-- 환율 등록 폼 -->
                    <div class="card mb-3">
                        <div class="card-header bg-info text-white">
                            <h6 class="mb-0"><i class="fas fa-plus me-1"></i>환율 등록</h6>
                        </div>
                        <div class="card-body">
                            <form id="exchangeRateForm" class="row g-3">
                                <div class="col-md-3">
                                    <label class="form-label">통화 코드</label>
                                    <select class="form-select" id="currencyCode" required>
                                        <option value="">선택하세요</option>
                                        <option value="USD">USD (미국 달러)</option>
                                        <option value="JPY">JPY (일본 엔)</option>
                                        <option value="CNY">CNY (중국 위안)</option>
                                        <option value="EUR">EUR (유로)</option>
                                        <option value="VND">VND (베트남 동)</option>
                                        <option value="THB">THB (태국 바트)</option>
                                    </select>
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label">기준 날짜</label>
                                    <input type="date" class="form-control" id="rateDate" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">기준 시간</label>
                                    <input type="time" class="form-control" id="rateTime" value="16:00" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">환율 (KRW)</label>
                                    <input type="number" class="form-control" id="exchangeRate" step="0.0001" placeholder="1330.50" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">&nbsp;</label>
                                    <button type="submit" class="btn btn-primary w-100">
                                        <i class="fas fa-save me-1"></i>등록
                                    </button>
                                </div>
                            </form>
                            <div class="mt-3 d-flex justify-content-between align-items-center">
                                <small class="text-muted">
                                    <i class="fas fa-info-circle me-1"></i>
                                    환율은 KRW 기준입니다. 예: USD 1 = 1330.50 KRW
                                </small>
                                <button type="button" class="btn btn-success btn-sm" onclick="fetchExchangeRateAuto()">
                                    <i class="fas fa-download me-1"></i>USD 환율 자동으로 가져오기
                                </button>
                            </div>
                            <div class="mt-2">
                                <div class="alert alert-info alert-dismissible fade show mb-0" role="alert">
                                    <i class="fas fa-robot me-1"></i>
                                    <strong>자동 업데이트:</strong> 매일 아침 9시에 자동으로 USD 환율이 등록됩니다.
                                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 환율 목록 -->
                    <div class="card">
                        <div class="card-header bg-light">
                            <h6 class="mb-0"><i class="fas fa-list me-1"></i>최근 환율 이력</h6>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-sm table-hover">
                                    <thead>
                                        <tr>
                                            <th>통화</th>
                                            <th>기준일시</th>
                                            <th>환율 (KRW)</th>
                                            <th>출처</th>
                                            <th>등록일시</th>
                                        </tr>
                                    </thead>
                                    <tbody id="exchangeRateList">
                                        <tr>
                                            <td colspan="5" class="text-center text-muted">
                                                <i class="fas fa-spinner fa-spin me-1"></i>로딩 중...
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">닫기</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 대량 정산 계산 모달 -->
    <div class="modal fade" id="bulkCalculateModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="fas fa-calculator me-2"></i>대량 정산 계산</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-1"></i>
                        <strong>대량 정산 계산 기능</strong><br>
                        여러 예약의 정산 금액을 AI 기반으로 자동 계산합니다. 환율, 수수료 등이 자동으로 적용됩니다.
                    </div>

                    <!-- 선택 옵션 -->
                    <div class="card mb-3">
                        <div class="card-header bg-primary text-white">
                            <h6 class="mb-0"><i class="fas fa-filter me-1"></i>계산 대상 선택</h6>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="form-label">플랫폼 선택 (선택사항)</label>
                                    <select class="form-select" id="bulkPlatformId">
                                        <option value="">전체 플랫폼</option>
                                        <option value="nol">NOL</option>
                                        <option value="klook">KLOOK</option>
                                        <option value="viator">VIATOR</option>
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">공급사 선택 (선택사항)</label>
                                    <select class="form-select" id="bulkSupplierId">
                                        <option value="">전체 공급사</option>
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label">기준 기간</label>
                                    <select class="form-select" id="bulkPeriod">
                                        <option value="current_month">이번 달</option>
                                        <option value="last_month">지난 달</option>
                                        <option value="custom">직접 선택</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 대상 예약 목록 -->
                    <div class="card">
                        <div class="card-header bg-light d-flex justify-content-between align-items-center">
                            <h6 class="mb-0"><i class="fas fa-list-check me-1"></i>계산 대상 예약</h6>
                            <div>
                                <button class="btn btn-sm btn-outline-primary" onclick="selectAllBulkReservations()">
                                    <i class="fas fa-check-square me-1"></i>전체 선택
                                </button>
                                <button class="btn btn-sm btn-outline-secondary" onclick="deselectAllBulkReservations()">
                                    <i class="fas fa-square me-1"></i>전체 해제
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div id="bulkReservationList" style="max-height: 400px; overflow-y: auto;">
                                <p class="text-center text-muted">
                                    <i class="fas fa-search me-1"></i>조건을 선택한 후 "대상 조회" 버튼을 클릭하세요.
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- 계산 결과 -->
                    <div id="bulkCalculateResult" class="mt-3" style="display: none;">
                        <div class="alert alert-success">
                            <h6><i class="fas fa-check-circle me-1"></i>계산 완료</h6>
                            <div id="bulkResultSummary"></div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">닫기</button>
                    <button type="button" class="btn btn-info" onclick="loadBulkReservations()">
                        <i class="fas fa-search me-1"></i>대상 조회
                    </button>
                    <button type="button" class="btn btn-primary" onclick="executeBulkCalculate()" id="bulkCalculateBtn" disabled>
                        <i class="fas fa-calculator me-1"></i>정산 계산 실행
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- 정산 상세/수정 모달 -->
    <div class="modal fade" id="settlementDetailModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title"><i class="fas fa-edit me-2"></i>정산 상세/수정</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <!-- 숨겨진 ID 필드 -->
                    <input type="hidden" id="edit-settlement-id">
                    <input type="hidden" id="edit-reservation-id">
                    
                    <!-- 예약 정보 상세 -->
                    <div class="card mb-3 border-primary">
                        <div class="card-header bg-primary text-white">
                            <h6 class="mb-0"><i class="fas fa-info-circle me-2"></i>예약 상세 정보</h6>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="small text-muted mb-1">예약번호</label>
                                    <div class="fw-bold" id="edit-reservation-number">-</div>
                                </div>
                                <div class="col-md-4">
                                    <label class="small text-muted mb-1">손님명</label>
                                    <div class="fw-bold" id="edit-korean-name">-</div>
                                </div>
                                <div class="col-md-4">
                                    <label class="small text-muted mb-1">이용일</label>
                                    <div class="fw-bold text-primary" id="edit-usage-date">-</div>
                                </div>
                            </div>
                            <hr class="my-3">
                            <div class="row g-3">
                                <div class="col-md-3">
                                    <label class="small text-muted mb-1">예약업체</label>
                                    <div class="fw-bold text-success" id="edit-platform-name">-</div>
                                </div>
                                <div class="col-md-3">
                                    <label class="small text-muted mb-1">수배업체</label>
                                    <div class="fw-bold text-danger" id="edit-vendor-name">-</div>
                                </div>
                                <div class="col-md-4">
                                    <label class="small text-muted mb-1">상품명</label>
                                    <div class="fw-bold" id="edit-product-name">-</div>
                                </div>
                                <div class="col-md-2">
                                    <label class="small text-muted mb-1">패키지명</label>
                                    <div id="edit-package-type">-</div>
                                </div>
                            </div>
                            <hr class="my-3">
                            <div class="row g-3">
                                <div class="col-md-2">
                                    <label class="small text-muted mb-1">성인</label>
                                    <div class="badge bg-primary fs-6" id="edit-people-adult">0</div>명
                                </div>
                                <div class="col-md-2">
                                    <label class="small text-muted mb-1">소아</label>
                                    <div class="badge bg-info fs-6" id="edit-people-child">0</div>명
                                </div>
                                <div class="col-md-2">
                                    <label class="small text-muted mb-1">유아</label>
                                    <div class="badge bg-secondary fs-6" id="edit-people-infant">0</div>명
                                </div>
                                <div class="col-md-6">
                                    <label class="small text-muted mb-1">총 인원</label>
                                    <div class="fw-bold fs-5" id="edit-total-people">0명</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- 거래액 (받을 돈) -->
                    <div class="card mb-3">
                        <div class="card-header bg-success text-white">
                            <h6 class="mb-0"><i class="fas fa-arrow-down me-2"></i>거래액 (받을 돈)</h6>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-2">
                                    <label class="form-label">화폐</label>
                                    <select class="form-select" id="edit-sale-currency" onchange="calculateEditSettlement()">
                                        <option value="KRW">원화(₩)</option>
                                        <option value="USD">달러($)</option>
                                    </select>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">성인 판매가</label>
                                    <input type="number" class="form-control" id="edit-sale-adult" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">소아 판매가</label>
                                    <input type="number" class="form-control" id="edit-sale-child" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">유아 판매가</label>
                                    <input type="number" class="form-control" id="edit-sale-infant" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">총 판매가</label>
                                    <input type="number" class="form-control bg-light" id="edit-total-sale" readonly>
                                </div>
                            </div>
                            <div class="row g-3 mt-2">
                                <div class="col-md-3">
                                    <label class="form-label">수수료율 (%)</label>
                                    <input type="number" class="form-control" id="edit-commission-rate" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label">수수료금액</label>
                                    <input type="number" class="form-control bg-light" id="edit-commission-amount" readonly>
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label"><strong>입금받을금액</strong></label>
                                    <input type="number" class="form-control bg-warning fw-bold" id="edit-net-revenue" readonly>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- 매입 (줄 돈) -->
                    <div class="card mb-3">
                        <div class="card-header bg-danger text-white">
                            <h6 class="mb-0"><i class="fas fa-arrow-up me-2"></i>매입 (줄 돈)</h6>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-2">
                                    <label class="form-label">화폐</label>
                                    <select class="form-select" id="edit-cost-currency" onchange="calculateEditSettlement()">
                                        <option value="USD">달러($)</option>
                                        <option value="KRW">원화(₩)</option>
                                    </select>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">성인 원가</label>
                                    <input type="number" class="form-control" id="edit-cost-adult" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">소아 원가</label>
                                    <input type="number" class="form-control" id="edit-cost-child" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label">유아 원가</label>
                                    <input type="number" class="form-control" id="edit-cost-infant" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label"><strong>총 원가</strong></label>
                                    <input type="number" class="form-control bg-light fw-bold" id="edit-total-cost" readonly>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- 환율 및 마진 정보 -->
                    <div class="card mb-3">
                        <div class="card-header bg-info text-white">
                            <h6 class="mb-0"><i class="fas fa-graph-up me-2"></i>환율 및 마진</h6>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-3">
                                    <label class="form-label">환율 (1 USD = ? KRW)</label>
                                    <input type="number" class="form-control" id="edit-exchange-rate" step="0.01" onchange="calculateEditSettlement()">
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label">매입 원화환산</label>
                                    <input type="number" class="form-control bg-light" id="edit-cost-krw" readonly>
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label"><strong>예상 마진</strong></label>
                                    <input type="number" class="form-control bg-primary text-white fw-bold" id="edit-margin-krw" readonly>
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label"><strong>마진율 (%)</strong></label>
                                    <input type="number" class="form-control bg-primary text-white fw-bold" id="edit-margin-rate" readonly>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- 메모 -->
                    <div class="card">
                        <div class="card-body">
                            <label class="form-label">정산 메모</label>
                            <textarea class="form-control" id="edit-memo" rows="2"></textarea>
                        </div>
                    </div>
                </div>
                
                <div class="modal-footer">
                    <button class="btn btn-danger" onclick="deleteSettlement()">
                        <i class="fas fa-trash me-1"></i>정산 삭제
                    </button>
                    <button class="btn btn-secondary" data-bs-dismiss="modal">취소</button>
                    <button class="btn btn-primary" onclick="updateSettlement()">
                        <i class="fas fa-save me-1"></i>수정 저장
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        let currentPage = 1;
        let currentStatus = '';
        let currentMonth = '';
        let currentSearch = '';
        let selectedSettlements = new Set(); // 선택된 정산 ID 관리

        // 날짜 포맷 함수 (ISO → YYYY-MM-DD)
        function formatDate(dateString) {
            if (!dateString) return '-';
            const date = new Date(dateString);
            const year = date.getFullYear();
            const month = String(date.getMonth() + 1).padStart(2, '0');
            const day = String(date.getDate()).padStart(2, '0');
            return `${year}-${month}-${day}`;
        }

        // 페이지 로드 시 초기화
        document.addEventListener('DOMContentLoaded', function() {
            console.log('정산관리 페이지 로드 시작');
            loadSettlementsByStatus('incomplete');
            loadStats();
            loadStaffList();
            loadPlatformList();
            loadVendorList();
            
            // 금액 입력 시 자동 계산 (요소가 존재할 때만)
            const saleAmountEl = document.getElementById('saleAmount');
            const costAmountEl = document.getElementById('costAmount');
            
            if (saleAmountEl && costAmountEl) {
                saleAmountEl.addEventListener('input', calculateProfit);
                costAmountEl.addEventListener('input', calculateProfit);
            }
        });

        // 직원 목록 로드
        async function loadStaffList() {
            try {
                const response = await fetch('/api/admin/users');
                const data = await response.json();
                
                if (data.success) {
                    const staffFilter = document.getElementById('staffFilter');
                    staffFilter.innerHTML = '<option value="">전체</option>';
                    
                    data.users.forEach(user => {
                        const option = document.createElement('option');
                        option.value = user.username;
                        option.textContent = `${user.full_name} (${user.username})`;
                        staffFilter.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('직원 목록 로드 실패:', error);
            }
        }

        // 예약업체 목록 로드
        async function loadPlatformList() {
            try {
                const response = await fetch('/api/settlements/platforms');
                const data = await response.json();
                
                if (data.success) {
                    const platformFilter = document.getElementById('platformFilter');
                    platformFilter.innerHTML = '<option value="">전체</option>';
                    
                    data.platforms.forEach(platform => {
                        const option = document.createElement('option');
                        option.value = platform;
                        option.textContent = platform;
                        platformFilter.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('예약업체 목록 로드 실패:', error);
            }
        }

        // 수배업체 목록 로드
        async function loadVendorList() {
            try {
                const response = await fetch('/api/settlements/vendors');
                const data = await response.json();
                
                if (data.success) {
                    const vendorFilter = document.getElementById('vendorFilter');
                    vendorFilter.innerHTML = '<option value="">전체</option>';
                    
                    data.vendors.forEach(vendor => {
                        const option = document.createElement('option');
                        option.value = vendor;
                        option.textContent = vendor;
                        vendorFilter.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('수배업체 목록 로드 실패:', error);
            }
        }

        // 정산 통계 로드
        async function loadStats() {
            try {
                console.log('정산 통계 로드 시작');
                const response = await fetch('/api/settlements/stats');
                
                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                }
                
                const data = await response.json();
                console.log('정산 통계 응답:', data);
                
                if (data.success) {
                    const stats = data.data;
                    
                    // 1. 미입금 거래액
                    const unpaidRevenueEl = document.getElementById('unpaidRevenue');
                    if (unpaidRevenueEl) {
                        unpaidRevenueEl.textContent = `₩${Math.round(stats.unpaidRevenue).toLocaleString()}`;
                    }
                    
                    // 1-1. 예약업체별 미입금 분류
                    const unpaidByPlatformEl = document.getElementById('unpaidByPlatform');
                    if (unpaidByPlatformEl && stats.unpaidByPlatform) {
                        const platformHtml = stats.unpaidByPlatform
                            .filter(p => p.amount > 0)
                            .map(p => `<div class="d-flex justify-content-between mb-1">
                                <span>${p.name}:</span>
                                <strong>₩${Math.round(p.amount).toLocaleString()}</strong>
                            </div>`)
                            .join('');
                        unpaidByPlatformEl.innerHTML = platformHtml || '<div class="text-center text-success">✓ 미입금 없음</div>';
                    }
                    
                    // 2. 미송금 매입액
                    const unpaidCostEl = document.getElementById('unpaidCost');
                    if (unpaidCostEl) {
                        unpaidCostEl.textContent = `₩${Math.round(stats.unpaidCost).toLocaleString()}`;
                    }
                    
                    // 2-1. 수배업체별 미송금 분류
                    const unpaidByVendorEl = document.getElementById('unpaidByVendor');
                    if (unpaidByVendorEl && stats.unpaidByVendor) {
                        const vendorHtml = stats.unpaidByVendor
                            .filter(v => v.amount > 0)
                            .map(v => `<div class="d-flex justify-content-between mb-1">
                                <span>${v.name}:</span>
                                <strong>₩${Math.round(v.amount).toLocaleString()}</strong>
                            </div>`)
                            .join('');
                        unpaidByVendorEl.innerHTML = vendorHtml || '<div class="text-center text-success">✓ 미송금 없음</div>';
                    }
                    
                } else {
                    console.error('통계 API 오류:', data.message);
                    showAlert('통계를 불러오는데 실패했습니다: ' + data.message, 'danger');
                }
            } catch (error) {
                console.error('통계 로드 실패:', error);
                showAlert('통계를 불러오는데 실패했습니다.', 'danger');
            }
        }

        // 정산 목록 로드 (상태별)
        let currentSettlementStatus = 'incomplete';
        
        async function loadSettlementsByStatus(status) {
            currentSettlementStatus = status;
            
            try {
                console.log('💰 정산 목록 로드:', status);
                
                const startDate = document.getElementById('startDate').value;
                const endDate = document.getElementById('endDate').value;
                const searchText = document.getElementById('searchInput').value;
                const platformFilter = document.getElementById('platformFilter').value;
                const vendorFilter = document.getElementById('vendorFilter').value;
                const paymentReceivedFilter = document.getElementById('paymentReceivedFilter').value;
                const paymentSentFilter = document.getElementById('paymentSentFilter').value;
                const staffFilter = document.getElementById('staffFilter').value;
                
                const params = new URLSearchParams({
                    status: status,
                    start_date: startDate || '',
                    end_date: endDate || '',
                    search: searchText || '',
                    platform: platformFilter || '',
                    vendor: vendorFilter || '',
                    payment_received: paymentReceivedFilter || '',
                    payment_sent: paymentSentFilter || '',
                    assigned_to: staffFilter || ''
                });
                
                const response = await fetch(`/api/settlements/list?${params}`);
                const data = await response.json();
                
                if (data.success) {
                    renderSettlementsList(data.data || [], status);
                    updateCounts(data.counts || {});
                } else {
                    showAlert(data.message || '데이터 로드 실패', 'danger');
                }
            } catch (error) {
                console.error('❌ 정산 목록 로드 실패:', error);
                showAlert('정산 목록을 불러오는데 실패했습니다.', 'danger');
            }
        }
        
        // 정산 목록 렌더링
        function renderSettlementsList(settlements, status) {
            const containerId = status === 'incomplete' ? 'incompleteList' : 'completedList';
            const container = document.getElementById(containerId);
            
            if (!settlements || settlements.length === 0) {
                container.innerHTML = `
                    <div class="text-center py-5">
                        <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                        <h5 class="text-muted">${status === 'incomplete' ? '미완료 정산이 없습니다' : '완료된 정산이 없습니다'}</h5>
                    </div>
                `;
                return;
            }
            
            // 총액 계산
            let totalRevenue = 0;
            let totalCost = 0;
            let totalMargin = 0;
            let totalMarginTax = 0;
            let totalCommissionTax = 0;
            let totalTax = 0;
            
            const rows = settlements.map(s => {
                const isPaymentReceived = !!s.payment_received_date;
                const isPaymentSent = !!s.payment_sent_date;
                
                // 거래액 (KRW 환산) - 반올림 적용, 항상 기본 환율 사용
                const revenueKRW = Math.round(s.sale_currency === 'KRW' ? (s.net_revenue || 0) : (s.net_revenue || 0) * (s.exchange_rate || 1330));
                
                // 매입액 (KRW 환산) - 송금 완료 시 송금환율 기준, 미완료 시 기본 환율 기준
                const costKRW = Math.round(
                    isPaymentSent && s.payment_sent_cost_krw 
                    ? s.payment_sent_cost_krw  // 송금 완료: 송금환율로 재계산된 값
                    : (s.cost_krw || 0)        // 미완료: 기본 환율 기준
                );
                
                // 사용된 환율 (표시용)
                const displayExchangeRate = isPaymentSent && s.payment_sent_exchange_rate 
                    ? s.payment_sent_exchange_rate 
                    : (s.exchange_rate || 1330);
                
                // 마진 - 거래액과 매입액 차이로 재계산
                const marginKRW = Math.round(revenueKRW - costKRW);
                
                // 부가세 = 마진*10% - 예약업체수수료*10%
                const marginTax = Math.round(marginKRW * 0.1);  // 마진부가세
                const commissionTax = Math.round((s.commission_amount || 0) * 0.1);  // 수수료부가세
                const tax = marginTax - commissionTax;  // 실제부가세
                
                // 디버깅: 송금 완료 건 환율 정보 확인
                if (isPaymentSent && s.payment_sent_exchange_rate) {
                    console.log('💱 송금 완료 건 환율:', {
                        id: s.id,
                        기본환율: s.exchange_rate,
                        송금환율: s.payment_sent_exchange_rate,
                        기본매입액: s.cost_krw,
                        송금매입액: s.payment_sent_cost_krw,
                        사용된매입액: costKRW
                    });
                }
                
                totalRevenue += revenueKRW;
                totalCost += costKRW;
                totalMargin += marginKRW;
                totalMarginTax += marginTax;
                totalCommissionTax += commissionTax;
                totalTax += tax;
                
                // 거래액 표시 (원화만 입력되면 달러는 0)
                const revenueDisplay = s.sale_currency === 'KRW' 
                    ? formatCurrency(revenueKRW, 'KRW')
                    : `${formatCurrency(revenueKRW, 'KRW')}<br><small class="text-muted">(${formatCurrency(s.net_revenue || 0, 'USD')} × ${s.exchange_rate || 1330})</small>`;
                
                // 매입액 표시 (달러 × 환율) - 송금 완료 시 송금환율 표시
                const costDisplay = s.cost_currency === 'KRW'
                    ? formatCurrency(costKRW, 'KRW')
                    : `${formatCurrency(costKRW, 'KRW')}<br><small class="text-muted">(${formatCurrency(s.total_cost || 0, 'USD')} × ${displayExchangeRate.toFixed(2)})</small>`;
                
                return `
                    <tr style="cursor: pointer;" 
                        onclick="openSettlementDetail(${s.id}, event, '${status}')">
                        <td class="text-center" onclick="event.stopPropagation()">
                            <input type="checkbox" class="form-check-input settlement-checkbox" 
                                   value="${s.id}" 
                                   onchange="toggleSettlementSelection(${s.id})" 
                                   ${(isPaymentReceived && isPaymentSent) ? 'disabled' : ''}>
                        </td>
                        <td><small>${formatDate(s.usage_date)}</small></td>
                        <td><strong>${s.korean_name || '-'}</strong></td>
                        <td><small>${s.product_name || '-'}</small></td>
                        <td><small>${s.platform_name || '-'}</small></td>
                        <td><small>${s.vendor_name || '-'}</small></td>
                        <td><small class="text-primary">${s.staff_name || s.assigned_to || '-'}</small></td>
                        <td class="text-end"><strong>${revenueDisplay}</strong></td>
                        <td class="text-end text-danger"><strong>${costDisplay}</strong></td>
                        <td class="text-end text-primary"><strong>${marginKRW.toLocaleString()}원</strong></td>
                        <td class="text-end" style="background-color: #e0f7ff;"><small>${marginTax.toLocaleString()}원</small></td>
                        <td class="text-end" style="background-color: #e0ffe0;"><small>${commissionTax.toLocaleString()}원</small></td>
                        <td class="text-end" style="background-color: #ffe0cc;"><small>${tax.toLocaleString()}원</small></td>
                        <td class="text-center">
                            ${isPaymentReceived ? `<small class="text-success">${formatDate(s.payment_received_date)}</small>` : `<small class="text-muted">-</small>`}
                        </td>
                        <td class="text-center">
                            ${isPaymentSent ? `<small class="text-success">${formatDate(s.payment_sent_date)}</small>` : `<small class="text-muted">-</small>`}
                        </td>
                    </tr>
                `;
            }).join('');
            
            const bulkActionButtons = status === 'incomplete' ? `
                <div class="d-flex gap-2 mb-3">
                    <button class="btn btn-success btn-sm" onclick="bulkProcessPayment('received')" id="bulkReceivedBtn">
                        <i class="fas fa-arrow-down me-1"></i>선택 항목 일괄 입금처리
                    </button>
                    <button class="btn btn-danger btn-sm" onclick="bulkProcessPayment('sent')" id="bulkSentBtn">
                        <i class="fas fa-arrow-up me-1"></i>선택 항목 일괄 송금처리
                    </button>
                    <button class="btn btn-warning btn-sm" onclick="bulkCancelPayment('received')">
                        <i class="fas fa-undo me-1"></i>선택 항목 일괄 입금 취소
                    </button>
                    <button class="btn btn-info btn-sm" onclick="bulkCancelPayment('sent')">
                        <i class="fas fa-undo me-1"></i>선택 항목 일괄 송금 취소
                    </button>
                    <button class="btn btn-outline-secondary btn-sm" onclick="clearAllSelections()">
                        <i class="fas fa-times me-1"></i>선택 해제
                    </button>
                </div>
            ` : '';
            
            const html = `
                ${bulkActionButtons}
                <div class="table-responsive">
                    <table class="table table-hover table-sm" style="font-size: 0.65rem;">
                        <thead class="table-light">
                            <tr>
                                <th class="text-center">
                                    <input type="checkbox" class="form-check-input" id="selectAll" 
                                           onchange="toggleSelectAll()" ${status === 'completed' ? 'style="display:none;"' : ''}>
                                </th>
                                <th>이용일</th>
                                <th>손님이름</th>
                                <th>상품명</th>
                                <th>예약업체</th>
                                <th>수배업체</th>
                                <th>담당직원</th>
                                <th class="text-end">거래액 (KRW)</th>
                                <th class="text-end">매입액 (KRW)</th>
                                <th class="text-end">마진</th>
                                <th class="text-end" style="background-color: #e0f7ff;">마진부가세</th>
                                <th class="text-end" style="background-color: #e0ffe0;">수수료부가세</th>
                                <th class="text-end" style="background-color: #ffe0cc;">실제부가세</th>
                                <th class="text-center">입금일</th>
                                <th class="text-center">송금일</th>
                            </tr>
                        </thead>
                        <tbody>
                            ${rows}
                        </tbody>
                        <tfoot class="table-secondary">
                            <tr>
                                <th colspan="7" class="text-end">총액</th>
                                <th class="text-end">${formatCurrency(totalRevenue, 'KRW')}</th>
                                <th class="text-end text-danger">${formatCurrency(totalCost, 'KRW')}</th>
                                <th class="text-end text-primary">${formatCurrency(totalMargin, 'KRW')}</th>
                                <th class="text-end" style="background-color: #e0f7ff;">${formatCurrency(totalMarginTax, 'KRW')}</th>
                                <th class="text-end" style="background-color: #e0ffe0;">${formatCurrency(totalCommissionTax, 'KRW')}</th>
                                <th class="text-end" style="background-color: #ffe0cc;">${formatCurrency(totalTax, 'KRW')}</th>
                                <th colspan="2"></th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            `;
            
            container.innerHTML = html;
        }
        
        // 카운트 업데이트
        function updateCounts(counts) {
            document.getElementById('incompleteCount').textContent = counts.incomplete || 0;
            document.getElementById('completedCount').textContent = counts.completed || 0;
        }
        
        // 화폐 포맷 (소수점 반올림)
        function formatCurrency(amount, currency) {
            if (!amount) return '0';
            const roundedAmount = Math.round(parseFloat(amount));
            const symbol = currency === 'KRW' ? '₩' : '$';
            return `${symbol}${roundedAmount.toLocaleString()}`;
        }
        
        // 체크박스 선택/해제
        function toggleSettlementSelection(settlementId) {
            if (selectedSettlements.has(settlementId)) {
                selectedSettlements.delete(settlementId);
            } else {
                selectedSettlements.add(settlementId);
            }
            console.log('선택된 정산:', Array.from(selectedSettlements));
        }
        
        // 전체 선택/해제
        function toggleSelectAll() {
            const selectAllCheckbox = document.getElementById('selectAll');
            const checkboxes = document.querySelectorAll('.settlement-checkbox:not(:disabled)');
            
            selectedSettlements.clear();
            
            if (selectAllCheckbox.checked) {
                checkboxes.forEach(cb => {
                    cb.checked = true;
                    selectedSettlements.add(parseInt(cb.value));
                });
            } else {
                checkboxes.forEach(cb => cb.checked = false);
            }
            
            console.log('전체 선택:', Array.from(selectedSettlements));
        }
        
        // 선택 해제
        function clearAllSelections() {
            selectedSettlements.clear();
            const checkboxes = document.querySelectorAll('.settlement-checkbox');
            checkboxes.forEach(cb => cb.checked = false);
            const selectAll = document.getElementById('selectAll');
            if (selectAll) selectAll.checked = false;
        }
        
        // 일괄 입금/송금 처리
        async function bulkProcessPayment(type) {
            if (selectedSettlements.size === 0) {
                alert('처리할 항목을 선택해주세요.');
                return;
            }
            
            const typeText = type === 'received' ? '입금' : '송금';
            const today = new Date().toISOString().split('T')[0];
            
            const dateInput = prompt(`${typeText} 날짜를 입력하세요 (YYYY-MM-DD):`, today);
            
            if (!dateInput) return;
            
            let exchangeRate = null;
            if (type === 'sent') {
                const rateInput = prompt('송금 시 적용된 환율을 입력하세요 (예: 1330):', '1330');
                if (!rateInput) return;
                exchangeRate = parseFloat(rateInput);
                if (isNaN(exchangeRate) || exchangeRate <= 0) {
                    alert('올바른 환율을 입력해주세요.');
                    return;
                }
            }
            
            if (!confirm(`선택한 ${selectedSettlements.size}개 항목을 ${typeText} 처리하시겠습니까?`)) {
                return;
            }
            
            try {
                console.log(`💰 일괄 ${typeText} 처리:`, Array.from(selectedSettlements), dateInput, exchangeRate);
                
                const response = await fetch('/api/settlements/bulk-payment', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        settlement_ids: Array.from(selectedSettlements),
                        type: type,
                        date: dateInput,
                        exchange_rate: exchangeRate
                    })
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert(`✅ ${selectedSettlements.size}개 항목의 ${typeText} 처리가 완료되었습니다.`);
                    clearAllSelections();
                    loadSettlementsByStatus(currentSettlementStatus);
                } else {
                    alert(`❌ ${typeText} 처리 실패: ` + (result.message || '알 수 없는 오류'));
                }
            } catch (error) {
                console.error(`❌ 일괄 ${typeText} 처리 오류:`, error);
                alert(`일괄 ${typeText} 처리 중 오류가 발생했습니다.`);
            }
        }
        
        // 일괄 입금/송금 취소 처리
        async function bulkCancelPayment(type) {
            if (selectedSettlements.size === 0) {
                alert('취소할 항목을 선택해주세요.');
                return;
            }
            
            const typeText = type === 'received' ? '입금' : '송금';
            
            if (!confirm(`선택한 ${selectedSettlements.size}개 항목의 ${typeText}을 취소하시겠습니까?\n\n${typeText} 날짜가 삭제되고 미완료 상태로 되돌아갑니다.`)) {
                return;
            }
            
            try {
                console.log(`🔄 일괄 ${typeText} 취소:`, Array.from(selectedSettlements));
                
                const response = await fetch('/api/settlements/bulk-cancel-payment', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        settlement_ids: Array.from(selectedSettlements),
                        type: type
                    })
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert(`✅ ${selectedSettlements.size}개 항목의 ${typeText}이 취소되었습니다.\n\n미완료 상태로 되돌아갑니다.`);
                    clearAllSelections();
                    loadSettlementsByStatus(currentSettlementStatus);
                } else {
                    alert(`❌ ${typeText} 취소 실패: ` + (result.message || '알 수 없는 오류'));
                }
            } catch (error) {
                console.error(`❌ 일괄 ${typeText} 취소 오류:`, error);
                alert(`일괄 ${typeText} 취소 중 오류가 발생했습니다.`);
            }
        }
        
        // 입금/송금 처리 (단일)
        async function processPayment(settlementId, type) {
            const typeText = type === 'received' ? '입금' : '송금';
            const today = new Date().toISOString().split('T')[0];
            
            const dateInput = prompt(`${typeText} 날짜를 입력하세요 (YYYY-MM-DD):`, today);
            
            if (!dateInput) return;
            
            let exchangeRate = null;
            if (type === 'sent') {
                const rateInput = prompt('송금 시 적용된 환율을 입력하세요 (예: 1330):', '1330');
                if (!rateInput) return;
                exchangeRate = parseFloat(rateInput);
                if (isNaN(exchangeRate) || exchangeRate <= 0) {
                    alert('올바른 환율을 입력해주세요.');
                    return;
                }
            }
            
            try {
                console.log(`💰 ${typeText} 처리:`, settlementId, dateInput, exchangeRate);
                
                const response = await fetch(`/api/settlements/${settlementId}/payment`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        type: type,
                        date: dateInput,
                        exchange_rate: exchangeRate
                    })
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert(`✅ ${typeText} 처리가 완료되었습니다.`);
                    loadSettlementsByStatus(currentSettlementStatus);
                } else {
                    alert(`❌ ${typeText} 처리 실패: ` + (result.message || '알 수 없는 오류'));
                }
            } catch (error) {
                console.error(`❌ ${typeText} 처리 오류:`, error);
                alert(`${typeText} 처리 중 오류가 발생했습니다.`);
            }
        }

        // 정산 상태 텍스트 변환
        function getSettlementStatusText(status) {
            const statusMap = {
                'pending': '정산 대기',
                'settled': '정산 완료',
                'overdue': '연체'
            };
            return statusMap[status] || '정산 대기';
        }

        // 정산 상태 CSS 클래스 변환
        function getSettlementStatusClass(status) {
            const classMap = {
                'pending': 'pending',
                'settled': 'settled',
                'overdue': 'overdue'
            };
            return classMap[status] || 'pending';
        }

        // 마진 색상 클래스
        function getProfitClass(profit) {
            if (profit > 0) return 'profit-positive';
            if (profit < 0) return 'profit-negative';
            return 'profit-neutral';
        }

        // 정산 상세 모달 열기
        async function openSettlementDetail(settlementId, event, status = 'incomplete') {
            if (event) event.stopPropagation();
            
            try {
                console.log('💰 정산 상세 조회:', settlementId);
                
                const response = await fetch(`/api/settlements/${settlementId}`);
                const data = await response.json();
                
                if (!data.success) {
                    alert('정산 정보를 불러오는데 실패했습니다.');
                    return;
                }
                
                const settlement = data.data;
                const isCompleted = status === 'completed';
                
                // 숨겨진 필드 설정
                document.getElementById('edit-settlement-id').value = settlement.id;
                document.getElementById('edit-reservation-id').value = settlement.reservation_id;
                
                // 예약 정보 표시
                document.getElementById('edit-reservation-number').textContent = settlement.reservation_number || '-';
                document.getElementById('edit-korean-name').textContent = settlement.korean_name || '-';
                document.getElementById('edit-product-name').textContent = settlement.product_name || '-';
                document.getElementById('edit-usage-date').textContent = formatDate(settlement.usage_date);
                document.getElementById('edit-platform-name').textContent = settlement.platform_name || '-';
                document.getElementById('edit-vendor-name').textContent = settlement.vendor_name || '-';
                document.getElementById('edit-package-type').textContent = settlement.package_type || '-';
                
                // 인원 정보 표시
                const peopleAdult = settlement.people_adult || 0;
                const peopleChild = settlement.people_child || 0;
                const peopleInfant = settlement.people_infant || 0;
                const totalPeople = peopleAdult + peopleChild + peopleInfant;
                
                document.getElementById('edit-people-adult').textContent = peopleAdult;
                document.getElementById('edit-people-child').textContent = peopleChild;
                document.getElementById('edit-people-infant').textContent = peopleInfant;
                document.getElementById('edit-total-people').textContent = totalPeople + '명';
                
                // 거래액 정보
                document.getElementById('edit-sale-currency').value = settlement.sale_currency || 'KRW';
                document.getElementById('edit-sale-adult').value = settlement.sale_adult_price || 0;
                document.getElementById('edit-sale-child').value = settlement.sale_child_price || 0;
                document.getElementById('edit-sale-infant').value = settlement.sale_infant_price || 0;
                document.getElementById('edit-commission-rate').value = settlement.commission_rate || 0;
                
                // 매입 정보
                document.getElementById('edit-cost-currency').value = settlement.cost_currency || 'USD';
                document.getElementById('edit-cost-adult').value = settlement.cost_adult_price || 0;
                document.getElementById('edit-cost-child').value = settlement.cost_child_price || 0;
                document.getElementById('edit-cost-infant').value = settlement.cost_infant_price || 0;
                
                // 환율
                document.getElementById('edit-exchange-rate').value = settlement.exchange_rate || 1330;
                
                // 메모
                document.getElementById('edit-memo').value = settlement.memo || '';
                
                // 계산 실행
                calculateEditSettlement();
                
                // 관리자 권한 확인
                const isAdmin = '<%= adminRole %>' === 'admin';
                
                // 완료된 항목은 읽기 전용으로 설정 (단, 관리자는 수정 가능)
                const formElements = document.querySelectorAll('#settlementDetailModal input, #settlementDetailModal select, #settlementDetailModal textarea');
                formElements.forEach(el => {
                    if (isCompleted && !isAdmin) {
                        el.setAttribute('readonly', true);
                        el.setAttribute('disabled', true);
                    } else {
                        el.removeAttribute('readonly');
                        el.removeAttribute('disabled');
                    }
                });
                
                // 모달 제목 변경
                const modalTitle = document.querySelector('#settlementDetailModal .modal-title');
                if (isCompleted && !isAdmin) {
                    modalTitle.innerHTML = '<i class="fas fa-file-invoice me-2"></i>정산서 상세';
                } else {
                    modalTitle.innerHTML = '<i class="fas fa-edit me-2"></i>정산 상세/수정';
                }
                
                // 버튼 표시/숨김
                const deleteBtn = document.querySelector('#settlementDetailModal .btn-danger');
                const saveBtn = document.querySelector('#settlementDetailModal .btn-primary');
                
                if (isCompleted && !isAdmin) {
                    // 일반 직원은 완료된 정산 수정 불가
                    deleteBtn.style.display = 'none';
                    saveBtn.style.display = 'none';
                    
                    // 입출금 날짜 정보 추가
                    if (settlement.payment_received_date || settlement.payment_sent_date) {
                        const paymentInfo = document.createElement('div');
                        paymentInfo.className = 'alert alert-info mt-3';
                        paymentInfo.innerHTML = `
                            <strong><i class="fas fa-check-circle me-2"></i>입출금 완료 정보</strong><br>
                            ${settlement.payment_received_date ? `<span class="me-3">입금일: <strong>${formatDate(settlement.payment_received_date)}</strong></span>` : ''}
                            ${settlement.payment_sent_date ? `<span>송금일: <strong>${formatDate(settlement.payment_sent_date)}</strong></span>` : ''}
                        `;
                        
                        const modalBody = document.querySelector('#settlementDetailModal .modal-body');
                        const existingInfo = modalBody.querySelector('.alert-info');
                        if (existingInfo) existingInfo.remove();
                        modalBody.insertBefore(paymentInfo, modalBody.firstChild);
                    }
                } else {
                    // 미완료 정산 또는 관리자는 수정 가능
                    deleteBtn.style.display = 'inline-block';
                    saveBtn.style.display = 'inline-block';
                    
                    // 관리자가 완료된 정산을 수정하는 경우 경고 메시지 표시
                    if (isCompleted && isAdmin) {
                        const warningInfo = document.createElement('div');
                        warningInfo.className = 'alert alert-warning mt-3';
                        warningInfo.innerHTML = `
                            <strong><i class="fas fa-exclamation-triangle me-2"></i>관리자 권한으로 완료된 정산을 수정합니다</strong><br>
                            <small>이미 입출금이 완료된 정산입니다. 신중하게 수정해주세요.</small>
                        `;
                        
                        const modalBody = document.querySelector('#settlementDetailModal .modal-body');
                        const existingWarning = modalBody.querySelector('.alert-warning');
                        if (existingWarning) existingWarning.remove();
                        modalBody.insertBefore(warningInfo, modalBody.firstChild);
                    } else {
                        // 입출금 정보 및 경고 메시지 제거
                        const modalBody = document.querySelector('#settlementDetailModal .modal-body');
                        const existingInfo = modalBody.querySelector('.alert-info');
                        const existingWarning = modalBody.querySelector('.alert-warning');
                        if (existingInfo) existingInfo.remove();
                        if (existingWarning) existingWarning.remove();
                    }
                }
                
                // 모달 열기
                const modal = new bootstrap.Modal(document.getElementById('settlementDetailModal'));
                modal.show();
                
            } catch (error) {
                console.error('❌ 정산 상세 조회 실패:', error);
                alert('정산 정보를 불러오는데 실패했습니다.');
            }
        }
        
        // 정산 수정 모달 계산 함수
        function calculateEditSettlement() {
            // 인원수 가져오기 (화면에 표시된 실제 예약 인원수 사용)
            const adultCount = parseInt(document.getElementById('edit-people-adult').textContent) || 0;
            const childCount = parseInt(document.getElementById('edit-people-child').textContent) || 0;
            const infantCount = parseInt(document.getElementById('edit-people-infant').textContent) || 0;
            
            // 거래액 계산
            const saleCurrency = document.getElementById('edit-sale-currency').value;
            const saleAdult = parseFloat(document.getElementById('edit-sale-adult').value) || 0;
            const saleChild = parseFloat(document.getElementById('edit-sale-child').value) || 0;
            const saleInfant = parseFloat(document.getElementById('edit-sale-infant').value) || 0;
            const totalSale = Math.round((saleAdult * adultCount) + (saleChild * childCount) + (saleInfant * infantCount));
            
            const commissionRate = parseFloat(document.getElementById('edit-commission-rate').value) || 0;
            const commissionAmount = Math.round(totalSale * (commissionRate / 100));
            const netRevenue = Math.round(totalSale - commissionAmount);
            
            document.getElementById('edit-total-sale').value = totalSale;
            document.getElementById('edit-commission-amount').value = commissionAmount;
            document.getElementById('edit-net-revenue').value = netRevenue;
            
            // 매입액 계산
            const costCurrency = document.getElementById('edit-cost-currency').value;
            const costAdult = parseFloat(document.getElementById('edit-cost-adult').value) || 0;
            const costChild = parseFloat(document.getElementById('edit-cost-child').value) || 0;
            const costInfant = parseFloat(document.getElementById('edit-cost-infant').value) || 0;
            const totalCost = Math.round((costAdult * adultCount) + (costChild * childCount) + (costInfant * infantCount));
            
            document.getElementById('edit-total-cost').value = totalCost;
            
            // 환율 및 마진 계산
            const exchangeRate = parseFloat(document.getElementById('edit-exchange-rate').value) || 1330;
            
            // 원화 환산
            const revenueKRW = saleCurrency === 'KRW' ? netRevenue : Math.round(netRevenue * exchangeRate);
            const costKRW = costCurrency === 'KRW' ? totalCost : Math.round(totalCost * exchangeRate);
            
            const marginKRW = Math.round(revenueKRW - costKRW);
            const marginRate = revenueKRW > 0 ? ((marginKRW / revenueKRW) * 100).toFixed(2) : 0;
            
            document.getElementById('edit-cost-krw').value = costKRW;
            document.getElementById('edit-margin-krw').value = marginKRW;
            document.getElementById('edit-margin-rate').value = marginRate;
        }
        
        // 정산 수정 저장
        async function updateSettlement() {
            const settlementId = document.getElementById('edit-settlement-id').value;
            
            if (!confirm('정산 정보를 수정하시겠습니까?')) {
                return;
            }
            
            try {
                const updateData = {
                    sale_currency: document.getElementById('edit-sale-currency').value,
                    sale_adult_price: parseFloat(document.getElementById('edit-sale-adult').value) || 0,
                    sale_child_price: parseFloat(document.getElementById('edit-sale-child').value) || 0,
                    sale_infant_price: parseFloat(document.getElementById('edit-sale-infant').value) || 0,
                    total_sale: parseFloat(document.getElementById('edit-total-sale').value) || 0,
                    commission_rate: parseFloat(document.getElementById('edit-commission-rate').value) || 0,
                    commission_amount: parseFloat(document.getElementById('edit-commission-amount').value) || 0,
                    net_revenue: parseFloat(document.getElementById('edit-net-revenue').value) || 0,
                    
                    cost_currency: document.getElementById('edit-cost-currency').value,
                    cost_adult_price: parseFloat(document.getElementById('edit-cost-adult').value) || 0,
                    cost_child_price: parseFloat(document.getElementById('edit-cost-child').value) || 0,
                    cost_infant_price: parseFloat(document.getElementById('edit-cost-infant').value) || 0,
                    total_cost: parseFloat(document.getElementById('edit-total-cost').value) || 0,
                    
                    exchange_rate: parseFloat(document.getElementById('edit-exchange-rate').value) || 1330,
                    cost_krw: parseFloat(document.getElementById('edit-cost-krw').value) || 0,
                    margin_krw: parseFloat(document.getElementById('edit-margin-krw').value) || 0,
                    
                    memo: document.getElementById('edit-memo').value
                };
                
                console.log('💾 정산 수정:', settlementId, updateData);
                
                const response = await fetch(`/api/settlements/${settlementId}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(updateData)
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert('✅ 정산 정보가 수정되었습니다.');
                    bootstrap.Modal.getInstance(document.getElementById('settlementDetailModal')).hide();
                    loadSettlementsByStatus(currentSettlementStatus);
                } else {
                    alert('❌ 정산 수정 실패: ' + (result.message || '알 수 없는 오류'));
                }
            } catch (error) {
                console.error('❌ 정산 수정 오류:', error);
                alert('정산 수정 중 오류가 발생했습니다.');
            }
        }
        
        // 정산 삭제
        async function deleteSettlement() {
            const settlementId = document.getElementById('edit-settlement-id').value;
            
            if (!confirm('정산을 삭제하시겠습니까? 이 작업은 취소할 수 없습니다.')) {
                return;
            }
            
            try {
                console.log('🗑️ 정산 삭제:', settlementId);
                
                const response = await fetch(`/api/settlements/${settlementId}`, {
                    method: 'DELETE'
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert('✅ 정산이 삭제되었습니다.');
                    bootstrap.Modal.getInstance(document.getElementById('settlementDetailModal')).hide();
                    loadSettlementsByStatus(currentSettlementStatus);
                } else {
                    alert('❌ 정산 삭제 실패: ' + (result.message || '알 수 없는 오류'));
                }
            } catch (error) {
                console.error('❌ 정산 삭제 오류:', error);
                alert('정산 삭제 중 오류가 발생했습니다.');
            }
        }
        
        // 마진 자동 계산
        function calculateProfit() {
            const saleAmount = parseFloat(document.getElementById('saleAmount').value) || 0;
            const costAmount = parseFloat(document.getElementById('costAmount').value) || 0;
            const profit = saleAmount - costAmount;
            const profitRate = saleAmount > 0 ? (profit / saleAmount * 100) : 0;
            
            document.getElementById('profitAmount').value = profit.toFixed(2);
            document.getElementById('profitRate').value = profitRate.toFixed(1);
        }

        // 정산 모달 열기
        function openSettlementModal(reservationId) {
            document.getElementById('reservationId').value = reservationId;
            document.getElementById('settlementForm').reset();
            
            // 기존 예약 정보로 초기값 설정 (필요시)
            
            const modal = new bootstrap.Modal(document.getElementById('settlementModal'));
            modal.show();
        }

        // 정산 처리
        async function processSettlement() {
            const reservationId = document.getElementById('reservationId').value;
            const saleAmount = document.getElementById('saleAmount').value;
            const costAmount = document.getElementById('costAmount').value;
            const settlementNotes = document.getElementById('settlementNotes').value;
            
            if (!saleAmount || !costAmount) {
                showAlert('거래액 금액과 매입 금액을 입력해주세요.', 'warning');
                return;
            }
            
            try {
                const response = await fetch(`/api/settlements/${reservationId}/process`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        sale_amount: parseFloat(saleAmount),
                        cost_amount: parseFloat(costAmount),
                        settlement_notes: settlementNotes
                    })
                });

                const result = await response.json();
                
                if (result.success) {
                    showAlert('정산이 완료되었습니다.', 'success');
                    bootstrap.Modal.getInstance(document.getElementById('settlementModal')).hide();
                    loadSettlements();
                    loadStats();
                } else {
                    showAlert(result.message || '정산 처리에 실패했습니다.', 'danger');
                }
            } catch (error) {
                console.error('정산 처리 실패:', error);
                showAlert('정산 처리 중 오류가 발생했습니다.', 'danger');
            }
        }

        // 검색 및 필터링
        function searchSettlements() {
            // 현재 선택된 탭(미완료/완료)의 정산 목록을 다시 로드
            loadSettlementsByStatus(currentSettlementStatus);
        }

        function resetFilters() {
            // 모든 필터 초기화
            document.getElementById('startDate').value = '';
            document.getElementById('endDate').value = '';
            document.getElementById('searchInput').value = '';
            document.getElementById('platformFilter').value = '';
            document.getElementById('vendorFilter').value = '';
            document.getElementById('paymentReceivedFilter').value = '';
            document.getElementById('paymentSentFilter').value = '';
            document.getElementById('staffFilter').value = '';
            
            // 현재 탭의 정산 목록 다시 로드
            loadSettlementsByStatus(currentSettlementStatus);
        }

        // 정산 내보내기 (엑셀)
        function exportSettlements() {
            try {
                // 현재 필터 조건 가져오기
                const startDate = document.getElementById('startDate').value;
                const endDate = document.getElementById('endDate').value;
                const searchText = document.getElementById('searchInput').value;
                const platformFilter = document.getElementById('platformFilter').value;
                const vendorFilter = document.getElementById('vendorFilter').value;
                const paymentReceivedFilter = document.getElementById('paymentReceivedFilter').value;
                const paymentSentFilter = document.getElementById('paymentSentFilter').value;
                const staffFilter = document.getElementById('staffFilter').value;
                
                const params = new URLSearchParams({
                    status: currentSettlementStatus,
                    start_date: startDate || '',
                    end_date: endDate || '',
                    search: searchText || '',
                    platform: platformFilter || '',
                    vendor: vendorFilter || '',
                    payment_received: paymentReceivedFilter || '',
                    payment_sent: paymentSentFilter || '',
                    assigned_to: staffFilter || ''
                });
                
                // 엑셀 다운로드
                window.location.href = `/api/settlements/export?${params}`;
                
                showAlert('엑셀 파일을 다운로드합니다.', 'success');
                
            } catch (error) {
                console.error('엑셀 내보내기 오류:', error);
                showAlert('엑셀 내보내기 중 오류가 발생했습니다.', 'danger');
            }
        }

        // 기타 함수들
        function viewSettlementDetail(id) {
            showAlert('정산 상세 보기 기능은 곧 추가됩니다.', 'info');
        }

        function printSettlement(id) {
            showAlert('정산서 출력 기능은 곧 추가됩니다.', 'info');
        }

        function renderPagination(pagination) {
            const container = document.getElementById('pagination');
            if (!pagination || pagination.totalPages <= 1) {
                container.innerHTML = '';
                return;
            }

            let html = '';
            
            if (pagination.currentPage > 1) {
                html += `<li class="page-item"><a class="page-link" href="#" onclick="loadSettlements(${pagination.currentPage - 1})">이전</a></li>`;
            }

            for (let i = Math.max(1, pagination.currentPage - 2); i <= Math.min(pagination.totalPages, pagination.currentPage + 2); i++) {
                html += `<li class="page-item ${i === pagination.currentPage ? 'active' : ''}">
                    <a class="page-link" href="#" onclick="loadSettlements(${i})">${i}</a>
                </li>`;
            }

            if (pagination.currentPage < pagination.totalPages) {
                html += `<li class="page-item"><a class="page-link" href="#" onclick="loadSettlements(${pagination.currentPage + 1})">다음</a></li>`;
            }

            container.innerHTML = html;
        }

        function showAlert(message, type = 'info') {
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert alert-${type} alert-dismissible fade show position-fixed`;
            alertDiv.style.cssText = 'top: 20px; right: 20px; z-index: 9999; min-width: 300px;';
            alertDiv.innerHTML = `
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;
            document.body.appendChild(alertDiv);
            
            setTimeout(() => {
                if (alertDiv.parentNode) {
                    alertDiv.remove();
                }
            }, 5000);
        }

        // ==================== 환율 관리 기능 ====================

        // 환율 관리 모달 열기
        function openExchangeRateModal() {
            const modal = new bootstrap.Modal(document.getElementById('exchangeRateModal'));
            modal.show();
            loadExchangeRates();
            
            // 오늘 날짜를 기본값으로 설정
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('rateDate').value = today;
        }

        // 환율 목록 로드
        async function loadExchangeRates() {
            try {
                const response = await fetch('/api/exchange-rates');
                const data = await response.json();
                
                if (data.success) {
                    renderExchangeRates(data.data);
                } else {
                    throw new Error(data.message);
                }
            } catch (error) {
                console.error('환율 목록 로드 실패:', error);
                document.getElementById('exchangeRateList').innerHTML = `
                    <tr>
                        <td colspan="5" class="text-center text-danger">
                            <i class="fas fa-exclamation-triangle me-1"></i>환율 목록을 불러올 수 없습니다.
                        </td>
                    </tr>
                `;
            }
        }

        // 환율 목록 렌더링
        function renderExchangeRates(rates) {
            const tbody = document.getElementById('exchangeRateList');
            
            if (rates.length === 0) {
                tbody.innerHTML = `
                    <tr>
                        <td colspan="5" class="text-center text-muted">
                            <i class="fas fa-inbox me-1"></i>등록된 환율이 없습니다.
                        </td>
                    </tr>
                `;
                return;
            }

            tbody.innerHTML = rates.map(rate => {
                // source 표시 개선
                let sourceLabel = '수동';
                let sourceBadge = 'secondary';
                if (rate.source === 'auto_api') {
                    sourceLabel = '자동 (API)';
                    sourceBadge = 'success';
                } else if (rate.source === 'korea_eximbank_api') {
                    sourceLabel = '한국수출입은행';
                    sourceBadge = 'primary';
                }
                
                return `
                    <tr>
                        <td><strong>${rate.currency_code}</strong></td>
                        <td>${rate.rate_date} ${rate.rate_time || '16:00'}</td>
                        <td><strong class="text-primary">₩${parseFloat(rate.rate).toLocaleString('ko-KR', {minimumFractionDigits: 2})}</strong></td>
                        <td><span class="badge bg-${sourceBadge}">${sourceLabel}</span></td>
                        <td><small class="text-muted">${formatDate(rate.created_at)}</small></td>
                    </tr>
                `;
            }).join('');
        }

        // 환율 자동으로 가져오기 (수동 실행)
        async function fetchExchangeRateAuto() {
            try {
                showAlert('환율을 가져오는 중입니다...', 'info');
                
                const response = await fetch('/api/exchange-rates/fetch', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' }
                });
                
                const result = await response.json();
                
                if (result.success) {
                    showAlert(
                        `✅ USD 환율이 자동으로 등록되었습니다: 1 USD = ₩${result.data.rate.toFixed(2)} KRW`, 
                        'success'
                    );
                    loadExchangeRates(); // 목록 새로고침
                } else {
                    showAlert('❌ ' + (result.message || '환율 가져오기 실패'), 'danger');
                }
            } catch (error) {
                console.error('환율 자동 가져오기 실패:', error);
                showAlert('환율 가져오기 중 오류가 발생했습니다.', 'danger');
            }
        }

        // 환율 폼 제출 이벤트
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('exchangeRateForm');
            if (form) {
                form.addEventListener('submit', async function(e) {
                    e.preventDefault();
                    
                    const formData = {
                        currency_code: document.getElementById('currencyCode').value,
                        rate_date: document.getElementById('rateDate').value,
                        rate_time: document.getElementById('rateTime').value,
                        rate: parseFloat(document.getElementById('exchangeRate').value),
                        source: 'manual'
                    };
                    
                    try {
                        const response = await fetch('/api/exchange-rates', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(formData)
                        });
                        
                        const result = await response.json();
                        
                        if (result.success) {
                            showAlert('환율이 등록되었습니다.', 'success');
                            form.reset();
                            document.getElementById('rateTime').value = '16:00';
                            const today = new Date().toISOString().split('T')[0];
                            document.getElementById('rateDate').value = today;
                            loadExchangeRates();
                        } else {
                            showAlert(result.message || '환율 등록에 실패했습니다.', 'danger');
                        }
                    } catch (error) {
                        console.error('환율 등록 실패:', error);
                        showAlert('환율 등록 중 오류가 발생했습니다.', 'danger');
                    }
                });
            }
        });

        // ==================== 대량 정산 계산 기능 ====================

        let selectedReservations = [];

        // 대량 정산 모달 열기
        function openBulkCalculateModal() {
            const modal = new bootstrap.Modal(document.getElementById('bulkCalculateModal'));
            modal.show();
            selectedReservations = [];
            document.getElementById('bulkCalculateResult').style.display = 'none';
        }

        // 대상 예약 조회
        async function loadBulkReservations() {
            try {
                const platformId = document.getElementById('bulkPlatformId').value;
                const supplierId = document.getElementById('bulkSupplierId').value;
                const period = document.getElementById('bulkPeriod').value;

                // 바우처 전송 완료된 예약 조회 (정산 대상)
                const params = new URLSearchParams({
                    status: 'voucher_sent',
                    limit: 100
                });

                if (platformId) params.append('platform', platformId);
                if (supplierId) params.append('supplier', supplierId);

                const response = await fetch(`/api/assignments?${params}`);
                const data = await response.json();

                if (data.success && data.data) {
                    renderBulkReservations(data.data);
                } else {
                    throw new Error(data.message || '예약 목록을 불러올 수 없습니다.');
                }
            } catch (error) {
                console.error('대상 예약 조회 실패:', error);
                showAlert('대상 예약을 불러오는데 실패했습니다.', 'danger');
            }
        }

        // 대상 예약 렌더링
        function renderBulkReservations(reservations) {
            const container = document.getElementById('bulkReservationList');
            
            if (!reservations || reservations.length === 0) {
                container.innerHTML = `
                    <p class="text-center text-muted">
                        <i class="fas fa-inbox me-1"></i>조건에 맞는 예약이 없습니다.
                    </p>
                `;
                document.getElementById('bulkCalculateBtn').disabled = true;
                return;
            }

            container.innerHTML = reservations.map(res => `
                <div class="form-check border-bottom py-2">
                    <input class="form-check-input" type="checkbox" value="${res.id}" id="bulk_${res.id}" 
                           onchange="updateBulkSelection()">
                    <label class="form-check-label w-100" for="bulk_${res.id}">
                        <div class="d-flex justify-content-between">
                            <div>
                                <strong>${res.reservation_number || 'N/A'}</strong> - ${res.product_name || '상품명 없음'}
                                <br>
                                <small class="text-muted">
                                    <i class="fas fa-user me-1"></i>${res.korean_name || 'N/A'}
                                    <i class="fas fa-calendar ms-2 me-1"></i>${res.usage_date || 'N/A'}
                                </small>
                            </div>
                            <div class="text-end">
                                <strong class="text-primary">₩${(res.total_amount || 0).toLocaleString()}</strong>
                            </div>
                        </div>
                    </label>
                </div>
            `).join('');

            document.getElementById('bulkCalculateBtn').disabled = false;
        }

        // 선택 업데이트
        function updateBulkSelection() {
            const checkboxes = document.querySelectorAll('#bulkReservationList input[type="checkbox"]:checked');
            selectedReservations = Array.from(checkboxes).map(cb => parseInt(cb.value));
            
            const btn = document.getElementById('bulkCalculateBtn');
            btn.disabled = selectedReservations.length === 0;
            btn.innerHTML = `<i class="fas fa-calculator me-1"></i>정산 계산 실행 (${selectedReservations.length}건)`;
        }

        // 전체 선택
        function selectAllBulkReservations() {
            document.querySelectorAll('#bulkReservationList input[type="checkbox"]').forEach(cb => {
                cb.checked = true;
            });
            updateBulkSelection();
        }

        // 전체 해제
        function deselectAllBulkReservations() {
            document.querySelectorAll('#bulkReservationList input[type="checkbox"]').forEach(cb => {
                cb.checked = false;
            });
            updateBulkSelection();
        }

        // 대량 정산 계산 실행
        async function executeBulkCalculate() {
            if (selectedReservations.length === 0) {
                showAlert('계산할 예약을 선택해주세요.', 'warning');
                return;
            }

            const btn = document.getElementById('bulkCalculateBtn');
            const originalHTML = btn.innerHTML;
            btn.disabled = true;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin me-1"></i>계산 중...';

            try {
                const response = await fetch('/api/settlements/bulk-calculate', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        reservation_ids: selectedReservations,
                        platform_id: document.getElementById('bulkPlatformId').value || null,
                        supplier_id: document.getElementById('bulkSupplierId').value || null
                    })
                });

                const result = await response.json();

                if (result.success) {
                    showAlert(`${result.data.processed}건의 정산이 계산되었습니다.`, 'success');
                    
                    // 결과 표시
                    const resultDiv = document.getElementById('bulkCalculateResult');
                    const summaryDiv = document.getElementById('bulkResultSummary');
                    
                    summaryDiv.innerHTML = `
                        <p class="mb-1"><strong>처리 완료:</strong> ${result.data.processed}건</p>
                        ${result.data.errors && result.data.errors.length > 0 ? 
                            `<p class="mb-1 text-danger"><strong>실패:</strong> ${result.data.errors.length}건</p>` : 
                            ''}
                        <p class="mb-0"><small>정산 목록 페이지를 새로고침하여 결과를 확인하세요.</small></p>
                    `;
                    resultDiv.style.display = 'block';

                    // 3초 후 목록 새로고침
                    setTimeout(() => {
                        loadSettlements();
                        loadStats();
                    }, 3000);
                } else {
                    showAlert(result.message || '정산 계산에 실패했습니다.', 'danger');
                }
            } catch (error) {
                console.error('대량 정산 계산 실패:', error);
                showAlert('정산 계산 중 오류가 발생했습니다: ' + error.message, 'danger');
            } finally {
                btn.disabled = false;
                btn.innerHTML = originalHTML;
            }
        }

        // ==================== 요금 RAG 문서 관리 기능 ====================

        // 요금 RAG 문서 목록 로드
        async function loadPriceRagDocuments() {
            try {
                console.log('💰 요금 RAG 문서 목록 로드 시작');
                const tbody = document.getElementById('priceRagDocumentList');
                const searchTerm = document.getElementById('priceSearchInput').value;
                
                const response = await fetch(`/api/price-rag/documents?search=${encodeURIComponent(searchTerm)}`);
                const data = await response.json();
                
                if (data.success && data.data && data.data.length > 0) {
                    renderPriceRagDocuments(data.data);
                } else {
                    tbody.innerHTML = `
                        <tr>
                            <td colspan="8" class="text-center text-muted py-4">
                                <i class="fas fa-inbox fa-2x mb-2 d-block"></i>
                                등록된 요금 정보가 없습니다.
                            </td>
                        </tr>
                    `;
                }
            } catch (error) {
                console.error('❌ 요금 RAG 문서 목록 로드 실패:', error);
                const tbody = document.getElementById('priceRagDocumentList');
                tbody.innerHTML = `
                    <tr>
                        <td colspan="8" class="text-center text-danger py-3">
                            <i class="fas fa-exclamation-triangle me-1"></i>
                            데이터를 불러오는데 실패했습니다.
                        </td>
                    </tr>
                `;
            }
        }
        
        // 요금 RAG 문서 렌더링
        function renderPriceRagDocuments(documents) {
            const tbody = document.getElementById('priceRagDocumentList');
            
            tbody.innerHTML = documents.map(doc => {
                const salePrice = `${doc.sale_adult_price || 0}/${doc.sale_child_price || 0}/${doc.sale_infant_price || 0} ${doc.sale_currency}`;
                const costPrice = `${doc.cost_adult_price || 0}/${doc.cost_child_price || 0}/${doc.cost_infant_price || 0} ${doc.cost_currency}`;
                
                return `
                    <tr>
                        <td><strong>${doc.product_name}</strong></td>
                        <td>${doc.package_name || '-'}</td>
                        <td>${doc.supplier_name || '-'}</td>
                        <td>
                            <small class="text-success">${salePrice}</small>
                        </td>
                        <td>
                            <small class="text-danger">${costPrice}</small>
                        </td>
                        <td>${doc.commission_rate || 0}%</td>
                        <td><small class="text-muted">${formatDate(doc.created_at)}</small></td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <button class="btn btn-outline-primary" onclick="editPriceRag(${doc.id})" title="수정">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button class="btn btn-outline-danger" onclick="deletePriceRag(${doc.id})" title="삭제">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                `;
            }).join('');
        }
        
        // 요금 RAG 폼 초기화
        function resetPriceForm() {
            document.getElementById('priceRagForm').reset();
        }
        
        // 요금 RAG 수정
        async function editPriceRag(id) {
            try {
                const response = await fetch(`/api/price-rag/documents/${id}`);
                const data = await response.json();
                
                if (data.success && data.data) {
                    const doc = data.data;
                    
                    document.getElementById('productName').value = doc.product_name || '';
                    document.getElementById('packageName').value = doc.package_name || '';
                    document.getElementById('supplierName').value = doc.supplier_name || '';
                    
                    document.getElementById('saleCurrency').value = doc.sale_currency || 'KRW';
                    document.getElementById('saleAdultPrice').value = doc.sale_adult_price || 0;
                    document.getElementById('saleChildPrice').value = doc.sale_child_price || 0;
                    document.getElementById('saleInfantPrice').value = doc.sale_infant_price || 0;
                    document.getElementById('commissionRate').value = doc.commission_rate || 0;
                    
                    document.getElementById('costCurrency').value = doc.cost_currency || 'USD';
                    document.getElementById('costAdultPrice').value = doc.cost_adult_price || 0;
                    document.getElementById('costChildPrice').value = doc.cost_child_price || 0;
                    document.getElementById('costInfantPrice').value = doc.cost_infant_price || 0;
                    
                    // 폼에 수정 ID 저장
                    document.getElementById('priceRagForm').dataset.editId = id;
                    
                    // 스크롤 이동
                    document.querySelector('#priceRagForm').scrollIntoView({ behavior: 'smooth', block: 'start' });
                    showAlert('수정하려는 데이터를 불러왔습니다.', 'info');
                } else {
                    showAlert('데이터를 불러오는데 실패했습니다.', 'danger');
                }
            } catch (error) {
                console.error('❌ 요금 RAG 조회 실패:', error);
                showAlert('데이터를 불러오는데 실패했습니다.', 'danger');
            }
        }
        
        // 요금 RAG 삭제
        async function deletePriceRag(id) {
            if (!confirm('이 요금 정보를 삭제하시겠습니까?')) {
                return;
            }
            
            try {
                const response = await fetch(`/api/price-rag/documents/${id}`, {
                    method: 'DELETE'
                });
                
                const result = await response.json();
                
                if (result.success) {
                    showAlert('✅ 요금 정보가 삭제되었습니다.', 'success');
                    loadPriceRagDocuments();
                } else {
                    showAlert(result.message || '삭제에 실패했습니다.', 'danger');
                }
            } catch (error) {
                console.error('❌ 요금 RAG 삭제 실패:', error);
                showAlert('삭제 중 오류가 발생했습니다.', 'danger');
            }
        }

        // RAG 문서 렌더링
        function renderRagDocuments(documents) {
            const tbody = document.getElementById('ragDocumentList');
            
            tbody.innerHTML = documents.map(doc => {
                const typeLabels = {
                    'platform_policy': '플랫폼 정책',
                    'supplier_policy': '공급사 정책',
                    'settlement_guide': '정산 가이드',
                    'exchange_rate_policy': '환율 정책',
                    'other': '기타'
                };
                
                return `
                    <tr>
                        <td><span class="badge bg-info">${typeLabels[doc.document_type] || doc.document_type}</span></td>
                        <td><strong>${doc.document_name}</strong></td>
                        <td>${doc.platform_name || doc.supplier_name || '-'}</td>
                        <td><small class="text-muted">${doc.content ? doc.content.substring(0, 50) + '...' : '-'}</small></td>
                        <td><small class="text-muted">${formatDate(doc.created_at)}</small></td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <button class="btn btn-outline-primary" onclick="viewRagDocument(${doc.id})">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button class="btn btn-outline-danger" onclick="deleteRagDocument(${doc.id})">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                `;
            }).join('');
        }

        // 예약업체 및 수배업체 목록 로드
        async function loadFilterOptions() {
            try {
                // 예약업체 목록 로드
                const platformsResponse = await fetch('/api/settlements/platforms');
                if (platformsResponse.ok) {
                    const platformsData = await platformsResponse.json();
                    const platformSelect = document.getElementById('platformFilter');
                    if (platformsData.success && platformsData.data) {
                        platformsData.data.forEach(platform => {
                            const option = document.createElement('option');
                            option.value = platform;
                            option.textContent = platform;
                            platformSelect.appendChild(option);
                        });
                    }
                }
                
                // 수배업체 목록 로드
                const vendorsResponse = await fetch('/api/settlements/vendors');
                if (vendorsResponse.ok) {
                    const vendorsData = await vendorsResponse.json();
                    const vendorSelect = document.getElementById('vendorFilter');
                    if (vendorsData.success && vendorsData.data) {
                        vendorsData.data.forEach(vendor => {
                            const option = document.createElement('option');
                            option.value = vendor;
                            option.textContent = vendor;
                            vendorSelect.appendChild(option);
                        });
                    }
                }
            } catch (error) {
                console.error('❌ 필터 옵션 로드 실패:', error);
            }
        }
        
        // 요금 RAG 폼 제출 이벤트
        document.addEventListener('DOMContentLoaded', function() {
            // 필터 옵션 로드
            loadFilterOptions();
            
            const priceRagForm = document.getElementById('priceRagForm');
            if (priceRagForm) {
                priceRagForm.addEventListener('submit', async function(e) {
                    e.preventDefault();
                    
                    const editId = priceRagForm.dataset.editId;
                    const formData = {
                        product_name: document.getElementById('productName').value,
                        package_name: document.getElementById('packageName').value || null,
                        supplier_name: document.getElementById('supplierName').value || null,
                        
                        sale_currency: document.getElementById('saleCurrency').value,
                        sale_adult_price: parseFloat(document.getElementById('saleAdultPrice').value) || 0,
                        sale_child_price: parseFloat(document.getElementById('saleChildPrice').value) || 0,
                        sale_infant_price: parseFloat(document.getElementById('saleInfantPrice').value) || 0,
                        commission_rate: parseFloat(document.getElementById('commissionRate').value) || 0,
                        
                        cost_currency: document.getElementById('costCurrency').value,
                        cost_adult_price: parseFloat(document.getElementById('costAdultPrice').value) || 0,
                        cost_child_price: parseFloat(document.getElementById('costChildPrice').value) || 0,
                        cost_infant_price: parseFloat(document.getElementById('costInfantPrice').value) || 0
                    };
                    
                    try {
                        const url = editId 
                            ? `/api/price-rag/documents/${editId}` 
                            : '/api/price-rag/documents';
                        const method = editId ? 'PUT' : 'POST';
                        
                        const response = await fetch(url, {
                            method: method,
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(formData)
                        });
                        
                        const result = await response.json();
                        
                        if (result.success) {
                            showAlert(`✅ 요금 정보가 ${editId ? '수정' : '등록'}되었습니다.`, 'success');
                            priceRagForm.reset();
                            delete priceRagForm.dataset.editId;
                            loadPriceRagDocuments();
                        } else {
                            showAlert(result.message || `요금 정보 ${editId ? '수정' : '등록'}에 실패했습니다.`, 'danger');
                        }
                    } catch (error) {
                        console.error('❌ 요금 RAG 등록/수정 실패:', error);
                        showAlert('요금 정보 저장 중 오류가 발생했습니다.', 'danger');
                    }
                });
            }
            
            // 요금 RAG 탭 클릭 시 문서 로드
            const ragTab = document.getElementById('rag-tab');
            if (ragTab) {
                ragTab.addEventListener('shown.bs.tab', function() {
                    loadPriceRagDocuments();
                });
            }
            
            // 검색 입력 시 실시간 검색
            const searchInput = document.getElementById('priceSearchInput');
            if (searchInput) {
                let searchTimeout;
                searchInput.addEventListener('input', function() {
                    clearTimeout(searchTimeout);
                    searchTimeout = setTimeout(() => {
                        loadPriceRagDocuments();
                    }, 500);
                });
            }
        });
    </script>
</body>
</html>
