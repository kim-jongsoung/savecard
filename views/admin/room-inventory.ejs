<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Room Availability - 괌 세이브카드</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body { font-family: Arial, sans-serif; font-size: 11px; }
    .inventory-header { background: #2c3e50; color: white; padding: 0.5rem; text-align: center; font-weight: 700; margin: 1rem 0; }
    .month-section { margin-bottom: 2rem; page-break-inside: avoid; }
    .month-title { background: #34495e; color: white; padding: 0.4rem; font-weight: 700; text-align: center; margin-bottom: 0.5rem; cursor: pointer; }
    .month-title input[type="checkbox"] { cursor: pointer; }
    .avail-table { width: 100%; border-collapse: collapse; font-size: 10px; }
    .avail-table th { background: #95a5a6; color: white; padding: 0.3rem; text-align: center; font-weight: 600; border: 1px solid #7f8c8d; min-width: 22px; }
    .avail-table th.rt { min-width: 120px; text-align: left; }
    .avail-table td { padding: 0; text-align: center; border: 1px solid #ddd; cursor: pointer; font-weight: 600; min-width: 22px; height: 22px; position: relative; }
    .avail-table td.rt-cell { background: #ecf0f1; font-weight: 700; text-align: left; padding-left: 0.4rem; cursor: pointer; }
    .avail-table td.rt-cell input[type="checkbox"] { cursor: pointer; }
    .cell-O { color: #27ae60; background: #d5f4e6; }
    .cell-X { color: #e74c3c; background: #fadbd8; }
    .cell-V { color: #3498db; background: #d6eaf8; }
    .cell-highlighted { background: #ffeb3b !important; font-weight: 700; }
    .cell-updated { background: #fff59d !important; }
    .cell-selected { background: #bbdefb !important; outline: 2px solid #2196f3; }
    .cell-chk { position: absolute; top: 1px; left: 1px; transform: scale(0.7); }
    .btn-sm { font-size: 10px; padding: 0.2rem 0.4rem; }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>
  <div class="container-fluid mt-3">
    <div class="mb-3">
      <div class="d-flex align-items-center mb-2">
        <h4 class="mb-0 me-3">Room Availability</h4>
        <label class="me-2">Hotel:</label>
        <select class="form-select form-select-sm d-inline-block w-auto me-3" id="hotelSelect" onchange="loadData()">
          <option value="">Select Hotel</option>
        </select>
        <label class="me-2">Start:</label>
        <input type="month" class="form-control form-control-sm d-inline-block w-auto me-3" id="startMonth" value="2025-11" onchange="loadData()">
        <span class="text-muted small">by <%= adminUsername %> | <span id="todayDate"></span></span>
      </div>
      <div class="d-flex align-items-center">
        <button class="btn btn-primary btn-sm me-2" onclick="selectAllCells()">전체선택</button>
        <button class="btn btn-secondary btn-sm me-2" onclick="clearSelection()">선택해제</button>
        <span class="text-muted me-2">|</span>
        <button class="btn btn-success btn-sm me-2" onclick="bulkUpdate('O')">Open</button>
        <button class="btn btn-danger btn-sm me-2" onclick="bulkUpdate('X')">Close</button>
        <button class="btn btn-info btn-sm me-2" onclick="bulkUpdate('V')">Check</button>
        <button class="btn btn-warning btn-sm me-2" onclick="toggleHighlight()" style="color: #000;">🟡 노란색 표시</button>
        <span class="text-muted me-2">|</span>
        <button class="btn btn-warning btn-sm me-3" onclick="saveChanges()">💾 저장</button>
        <span class="text-muted me-2">|</span>
        <a href="/inventory/view" target="_blank" class="btn btn-outline-primary btn-sm me-2">
          <i class="bi bi-box-arrow-up-right"></i> 공개 페이지 보기
        </a>
        <span class="small text-muted" id="inventoryTypeHint"></span>
      </div>
      
      <!-- 마지막 업데이트 시간 -->
      <div id="lastUpdateInfo" class="alert alert-info alert-dismissible fade show" role="alert" style="display: none; padding: 8px 12px; font-size: 11px;">
        <strong>📅 재고 최종 업데이트:</strong> <span id="lastUpdateTime"></span>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close" style="padding: 6px;"></button>
      </div>
    </div>
    
    <div id="inventoryContainer"></div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.getElementById('todayDate').textContent = new Date().toLocaleDateString('ko-KR');
    
    let hotels = [], roomTypes = [], inventory = {}, selected = new Set(), changes = {}, highlighted = new Set();
    let currentHotel = null; // 현재 선택된 호텔 정보
    
    async function loadHotels() {
      const res = await fetch('/api/hotels?is_active=true');
      hotels = await res.json();
      const sel = document.getElementById('hotelSelect');
      hotels.forEach(h => sel.innerHTML += `<option value="${h.id}">${h.hotel_name}</option>`);
    }
    
    async function loadData() {
      const hid = document.getElementById('hotelSelect').value;
      if (!hid) return;
      
      // 호텔 정보 가져오기 (inventory_type 확인)
      const hotelRes = await fetch(`/api/hotels/${hid}`);
      currentHotel = await hotelRes.json();
      
      const rtRes = await fetch(`/api/hotels/${hid}/room-types`);
      roomTypes = await rtRes.json();
      
      const start = document.getElementById('startMonth').value.split('-');
      const year = parseInt(start[0]), month = parseInt(start[1]);
      inventory = {};
      let latestUpdate = null;
      
      for (let m = 0; m < 6; m++) {
        const y = month + m > 12 ? year + 1 : year;
        const mo = month + m > 12 ? month + m - 12 : month + m;
        const res = await fetch(`/api/inventory?hotel_id=${hid}&year=${y}&month=${mo}`);
        const data = await res.json();
        console.log(`📥 ${y}-${mo} 데이터 ${data.length}개 로드`);
        data.forEach(inv => {
          // availability_date를 YYYY-MM-DD 형식으로 변환
          const dateOnly = inv.availability_date.split('T')[0];
          const key = `${inv.room_type_id}-${dateOnly}`;
          
          // count 방식: 숫자 그대로, status 방식: O/X로 변환
          if (currentHotel.inventory_type === 'count') {
            inventory[key] = inv.available_rooms;
          } else {
            if (inv.available_rooms === 0) {
              inventory[key] = 'X';
            } else if (inv.available_rooms > 0) {
              inventory[key] = 'O';
            } else {
              inventory[key] = '';
            }
          }
          console.log(`  → inventory["${key}"] = ${inventory[key]}`);
          
          // 가장 최근 업데이트 시간 추적
          if (inv.updated_at) {
            const updateTime = new Date(inv.updated_at);
            if (!latestUpdate || updateTime > latestUpdate) {
              latestUpdate = updateTime;
            }
          }
        });
      }
      
      // 마지막 업데이트 시간 표시
      if (latestUpdate) {
        const year = latestUpdate.getFullYear();
        const month = String(latestUpdate.getMonth() + 1).padStart(2, '0');
        const day = String(latestUpdate.getDate()).padStart(2, '0');
        const hour = String(latestUpdate.getHours()).padStart(2, '0');
        const minute = String(latestUpdate.getMinutes()).padStart(2, '0');
        
        document.getElementById('lastUpdateTime').textContent = `${year}/${month}/${day} ${hour}:${minute}`;
        document.getElementById('lastUpdateInfo').style.display = 'block';
      } else {
        document.getElementById('lastUpdateInfo').style.display = 'none';
      }
      
      // 재고 관리 방식 힌트 표시
      const hint = document.getElementById('inventoryTypeHint');
      if (currentHotel.inventory_type === 'count') {
        hint.textContent = '💡 숫자 카운팅 방식: Open 클릭 시 객실 수 입력';
      } else {
        hint.textContent = '💡 상태 표시 방식: O(오픈)/X(마감)/V(확인필요) + 노란색 표시 가능';
      }
      
      renderInventory(year, month);
    }
    
    function renderInventory(year, month) {
      const container = document.getElementById('inventoryContainer');
      let html = '';
      
      for (let m = 0; m < 6; m++) {
        const y = month + m > 12 ? year + 1 : year;
        const mo = month + m > 12 ? month + m - 12 : month + m;
        const monthName = new Date(y, mo - 1).toLocaleString('en', { month: 'short', year: '2-digit' });
        const days = new Date(y, mo, 0).getDate();
        
        html += `<div class="month-section">
          <div class="month-title">
            <input type="checkbox" class="me-2" onchange="selectMonth(${y}, ${mo})" style="transform: scale(1.2);">
            ${monthName}
          </div>
          <table class="avail-table"><thead><tr><th class="rt">Room Type</th>`;
        for (let d = 1; d <= days; d++) html += `<th>${d}</th>`;
        html += `</tr></thead><tbody>`;
        
        roomTypes.forEach(rt => {
          html += `<tr><td class="rt-cell">
            <input type="checkbox" class="me-1" onchange="selectRoomType(${rt.id}, ${y}, ${mo})" style="transform: scale(0.9);">
            ${rt.room_type_code}
          </td>`;
          for (let d = 1; d <= days; d++) {
            const date = `${y}-${String(mo).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
            const key = `${rt.id}-${date}`;
            const status = changes[key] !== undefined ? changes[key] : inventory[key];
            
            // count 방식: 숫자 표시 및 클래스 설정
            let cls = '', displayValue = '';
            if (currentHotel && currentHotel.inventory_type === 'count') {
              displayValue = status !== undefined && status !== '' ? status : '';
              if (status === 0) cls = 'cell-X';
              else if (status > 0) cls = 'cell-O';
            } else {
              // status 방식: O/X
              displayValue = status || '';
              cls = status ? `cell-${status}` : '';
            }
            
            // 노란색 강조 표시 추가
            if (highlighted.has(key)) {
              cls += ' cell-highlighted';
            }
            
            const sel = selected.has(key) ? ' cell-selected' : '';
            html += `<td class="${cls}${sel}" onclick="toggleCell('${key}')" data-key="${key}">
              <input type="checkbox" class="cell-chk" ${selected.has(key)?'checked':''} onclick="event.stopPropagation();toggleCell('${key}')">
              ${displayValue}
            </td>`;
          }
          html += `</tr>`;
        });
        
        html += `</tbody></table></div>`;
      }
      
      container.innerHTML = html;
    }
    
    function toggleCell(key) {
      if (selected.has(key)) selected.delete(key);
      else selected.add(key);
      const [year, month] = document.getElementById('startMonth').value.split('-').map(Number);
      renderInventory(year, month);
    }
    
    function selectMonth(year, month) {
      const days = new Date(year, month, 0).getDate();
      roomTypes.forEach(rt => {
        for (let d = 1; d <= days; d++) {
          const date = `${year}-${String(month).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
          const key = `${rt.id}-${date}`;
          if (event.target.checked) {
            selected.add(key);
          } else {
            selected.delete(key);
          }
        }
      });
      const [startYear, startMonth] = document.getElementById('startMonth').value.split('-').map(Number);
      renderInventory(startYear, startMonth);
    }
    
    function selectRoomType(roomTypeId, year, month) {
      const days = new Date(year, month, 0).getDate();
      for (let d = 1; d <= days; d++) {
        const date = `${year}-${String(month).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
        const key = `${roomTypeId}-${date}`;
        if (event.target.checked) {
          selected.add(key);
        } else {
          selected.delete(key);
        }
      }
      const [startYear, startMonth] = document.getElementById('startMonth').value.split('-').map(Number);
      renderInventory(startYear, startMonth);
    }
    
    function selectAllRoomType(roomTypeId) {
      const [year, month] = document.getElementById('startMonth').value.split('-').map(Number);
      for (let m = 0; m < 6; m++) {
        const y = month + m > 12 ? year + 1 : year;
        const mo = month + m > 12 ? month + m - 12 : month + m;
        const days = new Date(y, mo, 0).getDate();
        for (let d = 1; d <= days; d++) {
          const date = `${y}-${String(mo).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
          const key = `${roomTypeId}-${date}`;
          if (event.target.checked) {
            selected.add(key);
          } else {
            selected.delete(key);
          }
        }
      }
      renderInventory(year, month);
    }
    
    function selectAllCells() {
      selected.clear();
      const [year, month] = document.getElementById('startMonth').value.split('-').map(Number);
      roomTypes.forEach(rt => {
        for (let m = 0; m < 6; m++) {
          const y = month + m > 12 ? year + 1 : year;
          const mo = month + m > 12 ? month + m - 12 : month + m;
          const days = new Date(y, mo, 0).getDate();
          for (let d = 1; d <= days; d++) {
            const date = `${y}-${String(mo).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
            selected.add(`${rt.id}-${date}`);
          }
        }
      });
      renderInventory(year, month);
    }
    
    function clearSelection() {
      selected.clear();
      const [year, month] = document.getElementById('startMonth').value.split('-').map(Number);
      renderInventory(year, month);
    }
    
    function toggleHighlight() {
      if (selected.size === 0) return alert('셀을 먼저 선택하세요');
      
      // 선택된 셀들의 노란색 표시 토글
      selected.forEach(key => {
        if (highlighted.has(key)) {
          highlighted.delete(key);
        } else {
          highlighted.add(key);
        }
      });
      
      selected.clear();
      const [year, month] = document.getElementById('startMonth').value.split('-').map(Number);
      renderInventory(year, month);
    }
    
    function bulkUpdate(status) {
      if (selected.size === 0) return alert('No cells selected');
      
      // 숫자 카운팅 방식일 때는 숫자 입력
      if (currentHotel && currentHotel.inventory_type === 'count') {
        let rooms;
        if (status === 'O') {
          rooms = prompt('객실 수를 입력하세요:', '10');
          if (rooms === null) return; // 취소
          rooms = parseInt(rooms);
          if (isNaN(rooms) || rooms < 0) return alert('유효한 숫자를 입력하세요');
        } else if (status === 'X') {
          rooms = 0;
        } else if (status === 'V') {
          rooms = prompt('확인 필요 - 객실 수를 입력하세요:', '5');
          if (rooms === null) return;
          rooms = parseInt(rooms);
          if (isNaN(rooms) || rooms < 0) return alert('유효한 숫자를 입력하세요');
        }
        selected.forEach(key => changes[key] = rooms);
      } else {
        // 상태 방식: O/X/V 그대로
        selected.forEach(key => changes[key] = status);
      }
      
      selected.clear();
      const [year, month] = document.getElementById('startMonth').value.split('-').map(Number);
      renderInventory(year, month);
    }
    
    async function saveChanges() {
      if (Object.keys(changes).length === 0) return alert('No changes to save');
      const hid = document.getElementById('hotelSelect').value;
      let saved = 0;
      let failed = 0;
      
      console.log('💾 저장 시작, 변경사항:', Object.keys(changes).length);
      
      for (const [key, value] of Object.entries(changes)) {
        // key 형식: "3-2025-11-15" → rtid=3, date=2025-11-15
        const parts = key.split('-');
        const rtid = parts[0];
        const date = parts.slice(1).join('-'); // 나머지를 다시 join
        
        // count 방식: 숫자 그대로, status 방식: O/X/V를 숫자로 변환
        let rooms, notes = '';
        if (currentHotel && currentHotel.inventory_type === 'count') {
          rooms = value; // 이미 숫자
        } else {
          // O=10, X=0, V=5
          rooms = (value === 'O') ? 10 : ((value === 'X') ? 0 : 5);
        }
        
        const payload = {hotel_id: hid, room_type_id: rtid, start_date: date, end_date: date, days_of_week: [], available_rooms: rooms, notes: notes};
        console.log('📤 API 요청:', payload);
        
        try {
          const response = await fetch('/api/inventory/bulk', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(payload)
          });
          
          const result = await response.json();
          console.log('📥 API 응답:', result);
          
          if (!response.ok || !result.success) {
            console.error('❌ 저장 실패:', result);
            failed++;
          } else {
            // 저장한 내용을 inventory에 반영 (화면 업데이트용)
            inventory[key] = value;
            saved++;
          }
        } catch (error) {
          console.error('❌ API 오류:', error);
          failed++;
        }
      }
      
      alert(`저장 완료: ${saved}개 성공${failed > 0 ? `, ${failed}개 실패` : ''}`);
      changes = {};
      
      // 서버에서 데이터 다시 불러오기
      await loadData();
    }
    
    loadHotels();
  </script>
</body>
</html>
