<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>패키지 예약 관리 - 괌세이브카드 관리자</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .status-badge {
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 0.85rem;
            font-weight: bold;
        }
        .status-pending { background-color: #fff3cd; color: #856404; }
        .status-confirmed { background-color: #d4edda; color: #155724; }
        .status-cancelled { background-color: #f8d7da; color: #721c24; }
        .action-icons .btn {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
        }
        .component-icons {
            font-size: 1.2rem;
        }
        .margin-positive { color: #198754; font-weight: bold; }
        .margin-negative { color: #dc3545; font-weight: bold; }
        .last-edited-row {
            background-color: #fff3cd !important;
            border-left: 4px solid #ffc107 !important;
            transition: background-color 0.3s ease;
        }
        .last-edited-row:hover {
            background-color: #ffe69c !important;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar', { currentPage: 'package-reservations', adminUsername: adminUsername || '관리자' }) %>

    <div class="container-fluid mt-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2><i class="fas fa-box-open me-2"></i>패키지 예약 관리</h2>
                    <a href="/admin/package-inbox" class="btn btn-primary">
                        <i class="fas fa-plus me-1"></i>새 패키지 예약 등록
                    </a>
                </div>

                <!-- 필터 -->
                <div class="card mb-3">
                    <div class="card-body">
                        <div class="row g-3 mb-3">
                            <div class="col-md-2">
                                <label class="form-label">날짜 기준</label>
                                <select class="form-select" id="dateTypeFilter">
                                    <option value="departure">출발일</option>
                                    <option value="created">예약일</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">시작일</label>
                                <input type="date" class="form-control" id="startDateFilter">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">종료일</label>
                                <input type="date" class="form-control" id="endDateFilter">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">예약 채널</label>
                                <select class="form-select" id="platformFilter">
                                    <option value="">전체</option>
                                    <option value="VASCO">VASCO</option>
                                    <option value="NOL">NOL</option>
                                    <option value="롱혼스테이크">롱혼스테이크</option>
                                    <option value="직접예약">직접예약</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">상태</label>
                                <select class="form-select" id="statusFilter">
                                    <option value="all">전체</option>
                                    <option value="pending">대기</option>
                                    <option value="confirmed">확정</option>
                                    <option value="cancelled">취소</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label">검색</label>
                                <input type="text" class="form-control" id="searchInput" 
                                       placeholder="예약번호, 고객명">
                            </div>
                        </div>
                        <div class="row g-2">
                            <div class="col-md-12 text-end">
                                <button class="btn btn-primary" onclick="loadReservations()">
                                    <i class="fas fa-search me-1"></i>검색하기
                                </button>
                                <button class="btn btn-outline-secondary" onclick="resetFilters()">
                                    <i class="fas fa-redo me-1"></i>초기화
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 통계 -->
                <div class="row mb-3">
                    <div class="col-md-3">
                        <div class="card">
                            <div class="card-body">
                                <h6 class="text-muted">총 예약</h6>
                                <h3 id="stat-total">0</h3>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card">
                            <div class="card-body">
                                <h6 class="text-muted">총 거래액</h6>
                                <h3 id="stat-revenue">₩0</h3>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card">
                            <div class="card-body">
                                <h6 class="text-muted">총 매입액</h6>
                                <h3 id="stat-cost">₩0</h3>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card">
                            <div class="card-body">
                                <h6 class="text-muted">총 마진</h6>
                                <h3 id="stat-margin" class="margin-positive">₩0</h3>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 예약 목록 -->
                <div class="card">
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>출발일</th>
                                        <th>예약일</th>
                                        <th>예약채널</th>
                                        <th>상품명</th>
                                        <th>고객명</th>
                                        <th>여행기간</th>
                                        <th>인원</th>
                                        <th>구성요소</th>
                                        <th>상태</th>
                                        <th>관리</th>
                                    </tr>
                                </thead>
                                <tbody id="reservations-tbody">
                                    <tr>
                                        <td colspan="10" class="text-center">
                                            <div class="spinner-border text-primary" role="status">
                                                <span class="visually-hidden">로딩 중...</span>
                                            </div>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 상세 모달 -->
    <div class="modal fade" id="detailModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">패키지 예약 상세</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" id="modal-body-content">
                    <!-- 동적으로 채워짐 -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">닫기</button>
                    <a href="#" id="edit-link" class="btn btn-primary">수정</a>
                </div>
            </div>
        </div>
    </div>

    <!-- 예약수정 모달 -->
    <div class="modal fade" id="editModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title"><i class="fas fa-edit me-2"></i>패키지 예약 수정</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" style="max-height: 70vh; overflow-y: auto;">
                    <form id="editForm">
                        <input type="hidden" id="edit_reservation_id">
                        
                        <!-- 기본 예약 정보 -->
                        <div>
                            <h6 style="font-size: 1rem; margin-bottom: 0.75rem;">📋 기본 예약 정보</h6>
                            
                            <!-- 예약번호 표시 -->
                            <div class="row mb-2">
                                <div class="col-md-12">
                                    <div class="alert alert-info py-2 mb-2">
                                        <strong>예약번호:</strong> <span id="edit_reservation_number_display"></span>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- 한줄 요약 정보 -->
                            <div class="row mb-2">
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">예약 상태 *</label>
                                    <select class="form-select" id="edit_reservation_status" required>
                                        <option value="pending">⏳ 대기</option>
                                        <option value="confirmed">✅ 확정</option>
                                        <option value="cancelled">❌ 취소</option>
                                    </select>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">예약채널 *</label>
                                    <input type="text" class="form-control" id="edit_platform_name" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">패키지명 *</label>
                                    <input type="text" class="form-control" id="edit_package_name" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">출발일 *</label>
                                    <input type="date" class="form-control" id="edit_departure_date" onchange="calculateEditNights()" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">귀국일 *</label>
                                    <input type="date" class="form-control" id="edit_return_date" onchange="calculateEditNights()" required>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">기간</label>
                                    <input type="text" class="form-control" id="edit_period_display" readonly placeholder="자동">
                                </div>
                            </div>

                            <div class="row mb-2">
                                <div class="col-md-1">
                                    <label class="form-label" style="font-size: 0.875rem;">출국편명</label>
                                    <input type="text" class="form-control" id="edit_outbound_flight" placeholder="OZ601">
                                </div>
                                <div class="col-md-1">
                                    <label class="form-label" style="font-size: 0.875rem;">출발시간</label>
                                    <input type="time" class="form-control" id="edit_outbound_departure_time">
                                </div>
                                <div class="col-md-1">
                                    <label class="form-label" style="font-size: 0.875rem;">도착시간</label>
                                    <input type="time" class="form-control" id="edit_outbound_arrival_time">
                                </div>
                                <div class="col-md-1">
                                    <label class="form-label" style="font-size: 0.875rem;">입국편명</label>
                                    <input type="text" class="form-control" id="edit_inbound_flight" placeholder="OZ602">
                                </div>
                                <div class="col-md-1">
                                    <label class="form-label" style="font-size: 0.875rem;">출발시간</label>
                                    <input type="time" class="form-control" id="edit_inbound_departure_time">
                                </div>
                                <div class="col-md-1">
                                    <label class="form-label" style="font-size: 0.875rem;">도착시간</label>
                                    <input type="time" class="form-control" id="edit_inbound_arrival_time">
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label" style="font-size: 0.875rem;">이용호텔</label>
                                    <input type="text" class="form-control" id="edit_hotel_name" placeholder="호텔명">
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label" style="font-size: 0.875rem;">룸타입명</label>
                                    <input type="text" class="form-control" id="edit_room_type" placeholder="디럭스 오션뷰">
                                </div>
                            </div>

                            <!-- 인원 입력 -->
                            <h6 style="font-size: 0.95rem; margin-top: 1rem; margin-bottom: 0.75rem;">👥 인원 구성</h6>
                            <div class="row mb-2">
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">성인</label>
                                    <input type="number" class="form-control" id="edit_people_adult" value="0" min="0" onchange="generateEditGuestFields()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">소아</label>
                                    <input type="number" class="form-control" id="edit_people_child" value="0" min="0" onchange="generateEditGuestFields()">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">유아</label>
                                    <input type="number" class="form-control" id="edit_people_infant" value="0" min="0" onchange="generateEditGuestFields()">
                                </div>
                            </div>

                            <!-- 동적 고객 정보 필드 -->
                            <div id="edit_guests_container" class="mt-3"></div>

                            <!-- 일정 및 포함/불포함 사항 -->
                            <h6 style="font-size: 0.95rem; margin-top: 1rem; margin-bottom: 0.75rem;">📅 일정 및 포함/불포함 사항</h6>
                            <div class="row mb-2">
                                <div class="col-md-6">
                                    <label class="form-label" style="font-size: 0.875rem;">일정</label>
                                    <textarea class="form-control" id="edit_itinerary" rows="4" placeholder="1일차: 인천공항 출발 → 괌 도착&#10;2일차: 자유일정&#10;3일차: 시내관광&#10;4일차: 귀국"></textarea>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label" style="font-size: 0.875rem;">포함사항</label>
                                    <textarea class="form-control" id="edit_inclusions" rows="2" placeholder="왕복항공권, 호텔숙박, 공항픽업, 조식 등"></textarea>
                                    <label class="form-label mt-2" style="font-size: 0.875rem;">불포함사항</label>
                                    <textarea class="form-control" id="edit_exclusions" rows="2" placeholder="개인경비, 선택관광, 여행자보험 등"></textarea>
                                </div>
                            </div>
                        </div>

                        <!-- 금액 정보 -->
                        <div class="section-divider" style="border-top: 1px solid #dee2e6; margin: 20px 0; padding-top: 20px;">
                            <h6 style="font-size: 1rem; margin-bottom: 0.75rem;">💰 금액 정보</h6>
                            
                            <!-- 인원별 판매가 -->
                            <h6 style="font-size: 0.95rem; margin-bottom: 0.75rem;">👥 인원별 판매가</h6>
                            <div class="row mb-2">
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">통화 *</label>
                                    <select class="form-select" id="edit_currency">
                                        <option value="KRW">KRW</option>
                                        <option value="USD">USD</option>
                                    </select>
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">환율</label>
                                    <input type="text" class="form-control" id="edit_exchange_rate" value="1,300">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">성인 1인 요금 *</label>
                                    <input type="text" class="form-control" id="edit_price_adult" placeholder="500,000">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">소아 1인 요금</label>
                                    <input type="text" class="form-control" id="edit_price_child" placeholder="400,000">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">유아 1인 요금</label>
                                    <input type="text" class="form-control" id="edit_price_infant" placeholder="100,000">
                                </div>
                                <div class="col-md-2">
                                    <label class="form-label" style="font-size: 0.875rem;">총 판매가</label>
                                    <input type="text" class="form-control" id="edit_total_selling_price" readonly style="background-color: #e9ecef; font-weight: bold;">
                                </div>
                            </div>

                            <!-- 결제 빌링 -->
                            <h6 style="font-size: 0.95rem; margin-top: 1rem; margin-bottom: 0.75rem;">💳 결제 빌링</h6>
                            <div id="edit_billings_container"></div>
                            <button type="button" class="btn btn-sm btn-outline-success mb-3" onclick="addEditBilling()">
                                <i class="fas fa-plus me-1"></i>결제 추가
                            </button>

                            <!-- 매입 구성요소 -->
                            <h6 style="font-size: 0.95rem; margin-top: 1rem; margin-bottom: 0.75rem;">📦 매입 구성요소</h6>
                            <div id="edit_components_container"></div>
                            <button type="button" class="btn btn-sm btn-outline-primary mb-3" onclick="addEditComponent()">
                                <i class="fas fa-plus me-1"></i>구성요소 추가
                            </button>
                            
                            <!-- 특별 요청사항 -->
                            <div class="mb-2 mt-3">
                                <label class="form-label" style="font-size: 0.875rem;">특별 요청사항</label>
                                <textarea class="form-control" id="edit_special_requests" rows="2" placeholder="특별 요청사항이 있으면 입력하세요"></textarea>
                            </div>
                        </div>

                        <!-- 수정 이력 -->
                        <div class="section-divider" style="border-top: 1px solid #dee2e6; margin: 20px 0; padding-top: 20px;">
                            <h6 style="font-size: 1rem; margin-bottom: 0.75rem;">📜 수정 이력</h6>
                            <div id="modification_history_container" style="max-height: 200px; overflow-y: auto;">
                                <p class="text-muted mb-0" style="font-size: 0.875rem;">수정 이력이 없습니다.</p>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-1"></i>취소
                    </button>
                    <button type="button" class="btn btn-primary" onclick="saveReservationEdit()">
                        <i class="fas fa-save me-1"></i>저장
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- 수배서 모달 -->
    <div class="modal fade" id="assignmentModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title"><i class="fas fa-file-alt me-2"></i>수배서 관리</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <!-- 기본 예약 정보 -->
                    <div class="card mb-3">
                        <div class="card-header bg-light">
                            <h6 class="mb-0"><i class="fas fa-info-circle me-2"></i>기본 예약 정보</h6>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-3">
                                    <strong>예약번호:</strong> <span id="assign_reservation_number"></span>
                                </div>
                                <div class="col-md-3">
                                    <strong>고객명:</strong> <span id="assign_customer_name"></span>
                                </div>
                                <div class="col-md-3">
                                    <strong>출발일:</strong> <span id="assign_departure_date"></span>
                                </div>
                                <div class="col-md-3">
                                    <strong>귀국일:</strong> <span id="assign_return_date"></span>
                                </div>
                            </div>
                            <div class="row mt-2">
                                <div class="col-md-3">
                                    <strong>기간:</strong> <span id="assign_period"></span>
                                </div>
                                <div class="col-md-3">
                                    <strong>인원:</strong> <span id="assign_people"></span>
                                </div>
                                <div class="col-md-3">
                                    <strong>패키지명:</strong> <span id="assign_package_name"></span>
                                </div>
                                <div class="col-md-3">
                                    <strong>예약채널:</strong> <span id="assign_platform_name"></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 매입 구성요소별 수배서 목록 -->
                    <div class="card">
                        <div class="card-header bg-light">
                            <h6 class="mb-0"><i class="fas fa-list me-2"></i>매입 구성요소별 수배서</h6>
                        </div>
                        <div class="card-body">
                            <div id="assignment_list"></div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times me-1"></i>닫기
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        let allReservations = [];
        let lastEditedReservationId = null;

        // 예약 목록 로드
        async function loadReservations() {
            try {
                const dateType = document.getElementById('dateTypeFilter').value;
                const startDate = document.getElementById('startDateFilter').value;
                const endDate = document.getElementById('endDateFilter').value;
                const status = document.getElementById('statusFilter').value;
                const platform = document.getElementById('platformFilter').value;
                const search = document.getElementById('searchInput').value;

                const params = new URLSearchParams();
                if (dateType && startDate && endDate) {
                    params.append('dateType', dateType);
                    params.append('startDate', startDate);
                    params.append('endDate', endDate);
                }
                if (status !== 'all') params.append('status', status);
                if (platform) params.append('platform', platform);
                if (search) params.append('search', search);

                const response = await fetch(`/api/package-reservations?${params}`);
                const result = await response.json();

                if (result.success) {
                    allReservations = result.data;
                    renderReservations(result.data);
                    updateStats(result.data);
                } else {
                    alert('예약 목록 로드 실패: ' + result.message);
                }
            } catch (error) {
                console.error('예약 목록 로드 오류:', error);
                document.getElementById('reservations-tbody').innerHTML = `
                    <tr><td colspan="10" class="text-center text-danger">
                        오류가 발생했습니다: ${error.message}
                    </td></tr>
                `;
            }
        }

        // 예약 목록 렌더링
        function renderReservations(reservations) {
            const tbody = document.getElementById('reservations-tbody');

            if (reservations.length === 0) {
                tbody.innerHTML = `
                    <tr><td colspan="10" class="text-center text-muted">
                        등록된 패키지 예약이 없습니다.
                    </td></tr>
                `;
                return;
            }

            tbody.innerHTML = reservations.map(r => {
                const departureDate = new Date(r.travel_period.departure_date).toLocaleDateString('ko-KR', {year: 'numeric', month: '2-digit', day: '2-digit'}).replace(/\. /g, '-').replace('.', '');
                const returnDate = new Date(r.travel_period.return_date).toLocaleDateString('ko-KR', {year: 'numeric', month: '2-digit', day: '2-digit'}).replace(/\. /g, '-').replace('.', '');
                const createdDate = new Date(r.createdAt).toLocaleDateString('ko-KR', {year: 'numeric', month: '2-digit', day: '2-digit'}).replace(/\. /g, '-').replace('.', '');
                
                const period = `${r.travel_period.nights}박${r.travel_period.days}일`;
                const people = `성인${r.people.adult}${r.people.child > 0 ? `/소아${r.people.child}` : ''}${r.people.infant > 0 ? `/유아${r.people.infant}` : ''}`;

                const componentIcons = r.cost_components.map(c => {
                    const icons = {
                        flight: '✈️',
                        ground: '🚗',
                        hotel: '🏨',
                        tour: '🎯',
                        other: '📦'
                    };
                    return icons[c.component_type] || '📦';
                }).join(' ');

                const reservationStatus = r.reservation_status || 'pending';
                const statusClass = `status-${reservationStatus}`;
                const statusText = {
                    pending: '대기',
                    confirmed: '확정',
                    cancelled: '취소'
                }[reservationStatus] || reservationStatus;

                const isLastEdited = lastEditedReservationId === r._id;
                const rowClass = isLastEdited ? 'last-edited-row' : '';
                
                return `
                    <tr class="${rowClass}" data-reservation-id="${r._id}">
                        <td><small>${departureDate}</small></td>
                        <td><small>${createdDate}</small></td>
                        <td><small>${r.platform_name}</small></td>
                        <td>${r.package_name}</td>
                        <td>${r.customer.korean_name}</td>
                        <td><small>${period}</small></td>
                        <td><small>${people}</small></td>
                        <td class="component-icons">${componentIcons}</td>
                        <td><span class="status-badge ${statusClass}">${statusText}</span></td>
                        <td class="action-icons">
                            <button class="btn btn-sm btn-outline-primary" onclick="openEditModal('${r._id}')" title="예약수정">
                                <i class="fas fa-edit"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-info" onclick="openAssignmentModal('${r._id}')" title="수배서">
                                <i class="fas fa-file-alt"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-success" onclick="openInvoiceModal('${r._id}')" title="확정서">
                                <i class="fas fa-file-invoice"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-warning" onclick="openSettlementModal('${r._id}')" title="정산관리">
                                <i class="fas fa-calculator"></i>
                            </button>
                        </td>
                    </tr>
                `;
            }).join('');
        }

        // 통계 업데이트
        function updateStats(reservations) {
            const total = reservations.length;
            const totalRevenue = reservations.reduce((sum, r) => sum + r.settlement.total_revenue_krw, 0);
            const totalCost = reservations.reduce((sum, r) => sum + r.settlement.total_cost_krw, 0);
            const totalMargin = reservations.reduce((sum, r) => sum + r.settlement.total_margin_krw, 0);

            document.getElementById('stat-total').textContent = `${total}건`;
            document.getElementById('stat-revenue').textContent = `₩${totalRevenue.toLocaleString()}`;
            document.getElementById('stat-cost').textContent = `₩${totalCost.toLocaleString()}`;
            document.getElementById('stat-margin').textContent = `₩${totalMargin.toLocaleString()}`;
            document.getElementById('stat-margin').className = totalMargin >= 0 ? 'margin-positive' : 'margin-negative';
        }

        // 상세 보기
        async function showDetail(id) {
            try {
                const response = await fetch(`/api/package-reservations/${id}`);
                const result = await response.json();

                if (result.success) {
                    const r = result.data;
                    const departureDate = new Date(r.travel_period.departure_date).toLocaleDateString('ko-KR');
                    const returnDate = new Date(r.travel_period.return_date).toLocaleDateString('ko-KR');

                    const componentsHTML = r.cost_components.map(c => {
                        const typeNames = {
                            flight: '✈️ 항공권',
                            hotel: '🏨 호텔',
                            tour: '🎯 투어',
                            other: '📦 기타'
                        };
                        return `
                            <tr>
                                <td>${typeNames[c.component_type]}</td>
                                <td>${c.vendor_name}</td>
                                <td>${c.cost_currency === 'KRW' ? '₩' : '$'}${c.cost_amount.toLocaleString()}</td>
                                <td>₩${c.cost_krw.toLocaleString()}</td>
                                <td>${c.notes || '-'}</td>
                            </tr>
                        `;
                    }).join('');

                    document.getElementById('modal-body-content').innerHTML = `
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <h6>예약번호</h6>
                                <p><strong>${r.reservation_number}</strong></p>
                            </div>
                            <div class="col-md-6">
                                <h6>예약 채널</h6>
                                <p>${r.platform_name}</p>
                            </div>
                        </div>
                        <div class="mb-3">
                            <h6>패키지명</h6>
                            <p>${r.package_name}</p>
                        </div>
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <h6>고객 정보</h6>
                                <p>
                                    ${r.customer.korean_name} (${r.customer.english_name || '-'})<br>
                                    ${r.customer.phone || '-'}<br>
                                    ${r.customer.email || '-'}
                                </p>
                            </div>
                            <div class="col-md-6">
                                <h6>여행 기간</h6>
                                <p>
                                    ${departureDate} ~ ${returnDate}<br>
                                    ${r.travel_period.nights}박${r.travel_period.days}일
                                </p>
                            </div>
                        </div>
                        <div class="mb-3">
                            <h6>인원</h6>
                            <p>성인 ${r.people.adult}명, 소아 ${r.people.child}명, 유아 ${r.people.infant}명</p>
                        </div>
                        <div class="mb-3">
                            <h6>구성요소</h6>
                            <table class="table table-sm">
                                <thead>
                                    <tr>
                                        <th>타입</th>
                                        <th>공급업체</th>
                                        <th>매입가</th>
                                        <th>원화환산</th>
                                        <th>비고</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    ${componentsHTML}
                                </tbody>
                            </table>
                        </div>
                        <div class="mb-3">
                            <h6>정산 정보</h6>
                            <table class="table table-sm">
                                <tr>
                                    <td>총 거래액</td>
                                    <td class="text-end"><strong>₩${r.settlement.total_revenue_krw.toLocaleString()}</strong></td>
                                </tr>
                                <tr>
                                    <td>총 매입액</td>
                                    <td class="text-end">₩${r.settlement.total_cost_krw.toLocaleString()}</td>
                                </tr>
                                <tr class="table-primary">
                                    <td><strong>마진</strong></td>
                                    <td class="text-end ${r.settlement.total_margin_krw >= 0 ? 'margin-positive' : 'margin-negative'}">
                                        <strong>₩${r.settlement.total_margin_krw.toLocaleString()} (${r.settlement.margin_rate}%)</strong>
                                    </td>
                                </tr>
                                <tr>
                                    <td>부가세</td>
                                    <td class="text-end">₩${r.settlement.vat_amount.toLocaleString()}</td>
                                </tr>
                            </table>
                        </div>
                        ${r.special_requests ? `
                            <div class="mb-3">
                                <h6>특별 요청사항</h6>
                                <p>${r.special_requests}</p>
                            </div>
                        ` : ''}
                    `;

                    document.getElementById('edit-link').href = `/admin/package-inbox?id=${id}`;

                    new bootstrap.Modal(document.getElementById('detailModal')).show();
                } else {
                    alert('상세 정보 로드 실패: ' + result.message);
                }
            } catch (error) {
                console.error('상세 정보 로드 오류:', error);
                alert('상세 정보 로드 중 오류가 발생했습니다.');
            }
        }

        // 검색
        function handleSearch(event) {
            if (event.key === 'Enter') {
                loadReservations();
            }
        }

        // 필터 초기화
        function resetFilters() {
            document.getElementById('dateTypeFilter').value = 'departure';
            document.getElementById('startDateFilter').value = '';
            document.getElementById('endDateFilter').value = '';
            document.getElementById('statusFilter').value = 'all';
            document.getElementById('platformFilter').value = '';
            document.getElementById('searchInput').value = '';
            loadReservations();
        }

        // 예약수정 모달 열기
        let editComponentCount = 0;
        let editBillingCount = 0;
        
        async function openEditModal(id) {
            // 마지막 작업한 예약 ID 저장
            lastEditedReservationId = id;
            
            try {
                const response = await fetch(`/api/package-reservations/${id}`);
                const result = await response.json();

                if (result.success) {
                    const r = result.data;
                    
                    // 기본 정보
                    document.getElementById('edit_reservation_id').value = r._id;
                    document.getElementById('edit_reservation_number_display').textContent = r.reservation_number;
                    document.getElementById('edit_reservation_status').value = r.reservation_status || 'pending';
                    document.getElementById('edit_platform_name').value = r.platform_name;
                    document.getElementById('edit_package_name').value = r.package_name;
                    
                    // 여행 기간
                    document.getElementById('edit_departure_date').value = r.travel_period.departure_date.split('T')[0];
                    document.getElementById('edit_return_date').value = r.travel_period.return_date.split('T')[0];
                    document.getElementById('edit_period_display').value = `${r.travel_period.nights}박${r.travel_period.days}일`;
                    
                    // 인원 정보
                    document.getElementById('edit_people_adult').value = r.people.adult;
                    document.getElementById('edit_people_child').value = r.people.child;
                    document.getElementById('edit_people_infant').value = r.people.infant;
                    
                    // 항공편 정보
                    if (r.flight_info) {
                        document.getElementById('edit_outbound_flight').value = r.flight_info.outbound_flight || '';
                        document.getElementById('edit_outbound_departure_time').value = r.flight_info.outbound_departure_time || '';
                        document.getElementById('edit_outbound_arrival_time').value = r.flight_info.outbound_arrival_time || '';
                        document.getElementById('edit_inbound_flight').value = r.flight_info.inbound_flight || '';
                        document.getElementById('edit_inbound_departure_time').value = r.flight_info.inbound_departure_time || '';
                        document.getElementById('edit_inbound_arrival_time').value = r.flight_info.inbound_arrival_time || '';
                    }
                    
                    // 호텔 정보
                    document.getElementById('edit_hotel_name').value = r.hotel_name || '';
                    document.getElementById('edit_room_type').value = r.room_type || '';
                    
                    // 일정 및 포함/불포함
                    document.getElementById('edit_itinerary').value = r.itinerary || '';
                    document.getElementById('edit_inclusions').value = r.inclusions || '';
                    document.getElementById('edit_exclusions').value = r.exclusions || '';
                    
                    // 투숙객 정보
                    renderEditGuests(r.guests || []);
                    
                    // 금액 정보
                    if (r.pricing) {
                        document.getElementById('edit_currency').value = r.pricing.currency || 'KRW';
                        document.getElementById('edit_exchange_rate').value = (r.pricing.exchange_rate || 1300).toLocaleString();
                        document.getElementById('edit_price_adult').value = (r.pricing.price_adult || 0).toLocaleString();
                        document.getElementById('edit_price_child').value = (r.pricing.price_child || 0).toLocaleString();
                        document.getElementById('edit_price_infant').value = (r.pricing.price_infant || 0).toLocaleString();
                        document.getElementById('edit_total_selling_price').value = (r.pricing.total_selling_price || 0).toLocaleString();
                    }
                    
                    // 결제 빌링
                    renderEditBillings(r.billings || []);
                    
                    // 매입 구성요소
                    renderEditComponents(r.cost_components || []);
                    
                    // 특별 요청사항
                    document.getElementById('edit_special_requests').value = r.special_requests || '';
                    
                    // 수정 이력
                    renderModificationHistory(r.modification_history || []);
                    
                    // 모달 열기
                    new bootstrap.Modal(document.getElementById('editModal')).show();
                } else {
                    alert('예약 정보 로드 실패: ' + result.message);
                }
            } catch (error) {
                console.error('예약 정보 로드 오류:', error);
                alert('예약 정보 로드 중 오류가 발생했습니다.');
            }
        }
        
        // 투숙객 정보 렌더링 (신규 예약 페이지와 동일한 구조)
        function renderEditGuests(guests) {
            const container = document.getElementById('edit_guests_container');
            if (!guests || guests.length === 0) {
                container.innerHTML = '';
                return;
            }
            
            let html = '<h6 style="font-size: 0.95rem; margin-bottom: 0.5rem;">👤 고객 정보</h6>';
            guests.forEach((guest, index) => {
                const guestNum = index + 1;
                html += `
                    <div class="guest-card" style="border: 1px solid #dee2e6; border-radius: 6px; padding: 10px; margin-bottom: 10px; background-color: #f8f9fa;">
                        <div class="guest-header" style="font-weight: 600; color: #495057; margin-bottom: 8px; font-size: 0.9rem;">${guest.type || ''} ${guestNum}</div>
                        <div class="row">
                            <div class="col-md-1">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">한글이름 *</label>
                                <input type="text" class="form-control form-control-sm" value="${guest.korean_name || ''}" 
                                       id="edit_guest_${guestNum}_korean_name"
                                       data-guest-type="${guest.type || ''}"
                                       placeholder="홍길동" required>
                            </div>
                            <div class="col-md-2">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">영문이름</label>
                                <input type="text" class="form-control form-control-sm" value="${guest.english_name || ''}"
                                       id="edit_guest_${guestNum}_english_name"
                                       placeholder="HONG GILDONG">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">생년월일</label>
                                <input type="date" class="form-control form-control-sm" value="${guest.birth_date ? new Date(guest.birth_date).toISOString().split('T')[0] : ''}"
                                       id="edit_guest_${guestNum}_birth_date">
                            </div>
                            <div class="col-md-2">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">연락처</label>
                                <input type="text" class="form-control form-control-sm" value="${guest.phone || ''}"
                                       id="edit_guest_${guestNum}_phone"
                                       placeholder="010-1234-5678">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">이메일</label>
                                <input type="email" class="form-control form-control-sm" value="${guest.email || ''}"
                                       id="edit_guest_${guestNum}_email"
                                       placeholder="hong@email.com">
                            </div>
                            <div class="col-md-1">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">구분</label>
                                <input type="text" class="form-control form-control-sm" value="${guest.type || ''}" readonly>
                            </div>
                            <div class="col-md-1">
                                <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">성별</label>
                                <select class="form-select form-select-sm" id="edit_guest_${guestNum}_gender">
                                    <option value="">선택</option>
                                    <option value="남자" ${guest.gender === '남자' ? 'selected' : ''}>남자</option>
                                    <option value="여자" ${guest.gender === '여자' ? 'selected' : ''}>여자</option>
                                </select>
                            </div>
                        </div>
                    </div>
                `;
            });
            container.innerHTML = html;
        }
        
        // 인원 변경 시 투숙객 필드 동적 생성 (신규 예약 페이지와 동일)
        function generateEditGuestFields() {
            const adultCount = parseInt(document.getElementById('edit_people_adult').value) || 0;
            const childCount = parseInt(document.getElementById('edit_people_child').value) || 0;
            const infantCount = parseInt(document.getElementById('edit_people_infant').value) || 0;
            const totalCount = adultCount + childCount + infantCount;
            
            const container = document.getElementById('edit_guests_container');
            
            if (totalCount === 0) {
                container.innerHTML = '';
                return;
            }
            
            let html = '<h6 style="font-size: 0.95rem; margin-bottom: 0.5rem;">👤 고객 정보</h6>';
            let guestIndex = 0;
            
            // 성인
            for (let i = 0; i < adultCount; i++) {
                guestIndex++;
                html += createEditGuestCard(guestIndex, '성인', i + 1);
            }
            
            // 소아
            for (let i = 0; i < childCount; i++) {
                guestIndex++;
                html += createEditGuestCard(guestIndex, '소아', i + 1);
            }
            
            // 유아
            for (let i = 0; i < infantCount; i++) {
                guestIndex++;
                html += createEditGuestCard(guestIndex, '유아', i + 1);
            }
            
            container.innerHTML = html;
        }
        
        function createEditGuestCard(index, type, typeIndex) {
            return `
                <div class="guest-card" style="border: 1px solid #dee2e6; border-radius: 6px; padding: 10px; margin-bottom: 10px; background-color: #f8f9fa;">
                    <div class="guest-header" style="font-weight: 600; color: #495057; margin-bottom: 8px; font-size: 0.9rem;">${type} ${typeIndex}</div>
                    <div class="row">
                        <div class="col-md-1">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">한글이름 *</label>
                            <input type="text" class="form-control form-control-sm" 
                                   id="edit_guest_${index}_korean_name" 
                                   data-guest-index="${index - 1}"
                                   data-guest-type="${type}"
                                   data-field="korean_name"
                                   placeholder="홍길동" required>
                        </div>
                        <div class="col-md-2">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">영문이름</label>
                            <input type="text" class="form-control form-control-sm" 
                                   id="edit_guest_${index}_english_name"
                                   data-guest-index="${index - 1}"
                                   data-field="english_name"
                                   placeholder="HONG GILDONG">
                        </div>
                        <div class="col-md-2">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">생년월일</label>
                            <input type="date" class="form-control form-control-sm" 
                                   id="edit_guest_${index}_birth_date"
                                   data-guest-index="${index - 1}"
                                   data-field="birth_date">
                        </div>
                        <div class="col-md-2">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">연락처</label>
                            <input type="text" class="form-control form-control-sm" 
                                   id="edit_guest_${index}_phone"
                                   data-guest-index="${index - 1}"
                                   data-field="phone"
                                   placeholder="010-1234-5678">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">이메일</label>
                            <input type="email" class="form-control form-control-sm" 
                                   id="edit_guest_${index}_email"
                                   data-guest-index="${index - 1}"
                                   data-field="email"
                                   placeholder="hong@email.com">
                        </div>
                        <div class="col-md-1">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">구분</label>
                            <input type="text" class="form-control form-control-sm" 
                                   value="${type}" readonly>
                        </div>
                        <div class="col-md-1">
                            <label class="form-label" style="font-size: 0.875rem; margin-bottom: 0.25rem; font-weight: 500;">성별</label>
                            <select class="form-select form-select-sm" 
                                    id="edit_guest_${index}_gender"
                                    data-guest-index="${index - 1}"
                                    data-field="gender">
                                <option value="">선택</option>
                                <option value="남자">남자</option>
                                <option value="여자">여자</option>
                            </select>
                        </div>
                    </div>
                </div>
            `;
        }
        
        // 결제 빌링 렌더링
        function renderEditBillings(billings) {
            const container = document.getElementById('edit_billings_container');
            container.innerHTML = '';
            editBillingCount = 0;
            
            if (billings && billings.length > 0) {
                billings.forEach(billing => {
                    editBillingCount++;
                    const billingHTML = `
                        <div class="card mb-2" id="edit_billing_${editBillingCount}" style="background-color: #f8f9fa;">
                            <div class="card-body p-2">
                                <div class="row g-2">
                                    <div class="col-md-2">
                                        <select class="form-select form-select-sm">
                                            <option value="cash" ${billing.type === 'cash' ? 'selected' : ''}>💵 현금</option>
                                            <option value="card" ${billing.type === 'card' ? 'selected' : ''}>💳 카드</option>
                                            <option value="discount" ${billing.type === 'discount' ? 'selected' : ''}>🎁 할인</option>
                                        </select>
                                    </div>
                                    <div class="col-md-2">
                                        <input type="text" class="form-control form-control-sm" value="${(billing.amount || 0).toLocaleString()}" placeholder="금액">
                                    </div>
                                    <div class="col-md-2">
                                        <input type="date" class="form-control form-control-sm" value="${billing.date ? new Date(billing.date).toISOString().split('T')[0] : ''}">
                                    </div>
                                    <div class="col-md-2">
                                        <select class="form-select form-select-sm">
                                            <option value="pending" ${billing.status === 'pending' ? 'selected' : ''}>결제전</option>
                                            <option value="completed" ${billing.status === 'completed' ? 'selected' : ''}>결제완료</option>
                                        </select>
                                    </div>
                                    <div class="col-md-3">
                                        <input type="text" class="form-control form-control-sm" value="${billing.notes || ''}" placeholder="비고">
                                    </div>
                                    <div class="col-md-1">
                                        <button type="button" class="btn btn-sm btn-danger" onclick="document.getElementById('edit_billing_${editBillingCount}').remove()">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    `;
                    container.insertAdjacentHTML('beforeend', billingHTML);
                });
            }
        }
        
        // 매입 구성요소 렌더링
        function renderEditComponents(components) {
            const container = document.getElementById('edit_components_container');
            container.innerHTML = '';
            editComponentCount = 0;
            
            if (components && components.length > 0) {
                components.forEach(comp => {
                    editComponentCount++;
                    const typeIcons = {
                        flight: '✈️ 항공권',
                        hotel: '🏨 호텔',
                        tour: '🎯 투어',
                        ground: '🚗 지상비',
                        other: '📦 기타'
                    };
                    const componentHTML = `
                        <div class="card mb-2" id="edit_component_${editComponentCount}" style="background-color: #fff;">
                            <div class="card-body p-2">
                                <div class="row g-2">
                                    <div class="col-md-2">
                                        <select class="form-select form-select-sm">
                                            <option value="flight" ${comp.component_type === 'flight' ? 'selected' : ''}>✈️ 항공권</option>
                                            <option value="hotel" ${comp.component_type === 'hotel' ? 'selected' : ''}>🏨 호텔</option>
                                            <option value="tour" ${comp.component_type === 'tour' ? 'selected' : ''}>🎯 투어</option>
                                            <option value="ground" ${comp.component_type === 'ground' ? 'selected' : ''}>🚗 지상비</option>
                                            <option value="other" ${comp.component_type === 'other' ? 'selected' : ''}>📦 기타</option>
                                        </select>
                                    </div>
                                    <div class="col-md-2">
                                        <input type="text" class="form-control form-control-sm" value="${comp.vendor_name || ''}" placeholder="공급업체">
                                    </div>
                                    <div class="col-md-2">
                                        <input type="text" class="form-control form-control-sm" value="${(comp.cost_amount || 0).toLocaleString()}" placeholder="매입가">
                                    </div>
                                    <div class="col-md-1">
                                        <select class="form-select form-select-sm">
                                            <option value="KRW" ${comp.cost_currency === 'KRW' ? 'selected' : ''}>KRW</option>
                                            <option value="USD" ${comp.cost_currency === 'USD' ? 'selected' : ''}>USD</option>
                                        </select>
                                    </div>
                                    <div class="col-md-2">
                                        <input type="text" class="form-control form-control-sm" value="${(comp.cost_krw || 0).toLocaleString()}" placeholder="원화환산" readonly>
                                    </div>
                                    <div class="col-md-2">
                                        <input type="text" class="form-control form-control-sm" value="${comp.notes || ''}" placeholder="비고">
                                    </div>
                                    <div class="col-md-1">
                                        <button type="button" class="btn btn-sm btn-danger" onclick="document.getElementById('edit_component_${editComponentCount}').remove()">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    `;
                    container.insertAdjacentHTML('beforeend', componentHTML);
                });
            }
        }
        
        // 수정 이력 렌더링
        function renderModificationHistory(history) {
            const container = document.getElementById('modification_history_container');
            
            if (!history || history.length === 0) {
                container.innerHTML = '<p class="text-muted mb-0">수정 이력이 없습니다.</p>';
                return;
            }
            
            let html = '';
            history.reverse().forEach((entry, index) => {
                const date = new Date(entry.modified_at).toLocaleString('ko-KR');
                html += `
                    <div class="card mb-2 ${index === 0 ? 'border-primary' : ''}">
                        <div class="card-body p-2">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <strong style="font-size: 0.9rem;">
                                    <i class="fas fa-user-edit me-1"></i>${entry.modified_by}
                                </strong>
                                <small class="text-muted">${date}</small>
                            </div>
                            <div style="font-size: 0.85rem;">
                                <span class="badge bg-info">${entry.summary}</span>
                            </div>
                            <div class="mt-2" style="font-size: 0.8rem;">
                `;
                
                entry.changes.forEach(change => {
                    html += `
                        <div class="mb-1">
                            <span class="badge bg-secondary">${change.field_label}</span>
                            <span class="text-danger" style="text-decoration: line-through;">${change.old_value}</span>
                            <i class="fas fa-arrow-right mx-1"></i>
                            <span class="text-success"><strong>${change.new_value}</strong></span>
                        </div>
                    `;
                });
                
                html += `
                            </div>
                        </div>
                    </div>
                `;
            });
            
            container.innerHTML = html;
        }
        
        // 결제 빌링 추가
        function addEditBilling() {
            editBillingCount++;
            const container = document.getElementById('edit_billings_container');
            const billingHTML = `
                <div class="card mb-2" id="edit_billing_${editBillingCount}" style="background-color: #f8f9fa;">
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-md-2">
                                <select class="form-select form-select-sm">
                                    <option value="cash">💵 현금</option>
                                    <option value="card">💳 카드</option>
                                    <option value="discount">🎁 할인</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <input type="text" class="form-control form-control-sm" placeholder="금액">
                            </div>
                            <div class="col-md-2">
                                <input type="date" class="form-control form-control-sm">
                            </div>
                            <div class="col-md-2">
                                <select class="form-select form-select-sm">
                                    <option value="pending">결제전</option>
                                    <option value="completed">결제완료</option>
                                </select>
                            </div>
                            <div class="col-md-3">
                                <input type="text" class="form-control form-control-sm" placeholder="비고">
                            </div>
                            <div class="col-md-1">
                                <button type="button" class="btn btn-sm btn-danger" onclick="document.getElementById('edit_billing_${editBillingCount}').remove()">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            `;
            container.insertAdjacentHTML('beforeend', billingHTML);
        }
        
        // 매입 구성요소 추가
        function addEditComponent() {
            editComponentCount++;
            const container = document.getElementById('edit_components_container');
            const componentHTML = `
                <div class="card mb-2" id="edit_component_${editComponentCount}" style="background-color: #fff;">
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-md-2">
                                <select class="form-select form-select-sm">
                                    <option value="flight">✈️ 항공권</option>
                                    <option value="hotel">🏨 호텔</option>
                                    <option value="tour">🎯 투어</option>
                                    <option value="ground">🚗 지상비</option>
                                    <option value="other">📦 기타</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <input type="text" class="form-control form-control-sm" placeholder="공급업체">
                            </div>
                            <div class="col-md-2">
                                <input type="text" class="form-control form-control-sm" placeholder="매입가">
                            </div>
                            <div class="col-md-1">
                                <select class="form-select form-select-sm">
                                    <option value="KRW">KRW</option>
                                    <option value="USD">USD</option>
                                </select>
                            </div>
                            <div class="col-md-2">
                                <input type="text" class="form-control form-control-sm" placeholder="원화환산" readonly>
                            </div>
                            <div class="col-md-2">
                                <input type="text" class="form-control form-control-sm" placeholder="비고">
                            </div>
                            <div class="col-md-1">
                                <button type="button" class="btn btn-sm btn-danger" onclick="document.getElementById('edit_component_${editComponentCount}').remove()">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            `;
            container.insertAdjacentHTML('beforeend', componentHTML);
        }

        // 박수/일수 계산 (수정 모달)
        function calculateEditNights() {
            const departureDate = document.getElementById('edit_departure_date').value;
            const returnDate = document.getElementById('edit_return_date').value;
            
            if (departureDate && returnDate) {
                const departure = new Date(departureDate);
                const returnD = new Date(returnDate);
                const nights = Math.floor((returnD - departure) / (1000 * 60 * 60 * 24));
                const days = nights + 1;
                
                document.getElementById('edit_period_display').value = `${nights}박${days}일`;
            }
        }

        // 예약수정 저장
        async function saveReservationEdit() {
            const id = document.getElementById('edit_reservation_id').value;
            
            // 숫자 파싱 헬퍼
            const parseNumber = (value) => {
                if (!value) return 0;
                return parseFloat(value.toString().replace(/,/g, '')) || 0;
            };
            
            // 투숙객 정보 수집 (신규 예약 페이지와 동일한 방식)
            const adultCount = parseInt(document.getElementById('edit_people_adult').value) || 0;
            const childCount = parseInt(document.getElementById('edit_people_child').value) || 0;
            const infantCount = parseInt(document.getElementById('edit_people_infant').value) || 0;
            const totalCount = adultCount + childCount + infantCount;
            
            const guests = [];
            for (let i = 1; i <= totalCount; i++) {
                const koreanNameEl = document.getElementById(`edit_guest_${i}_korean_name`);
                if (koreanNameEl) {
                    const guest = {
                        korean_name: koreanNameEl.value || '',
                        english_name: document.getElementById(`edit_guest_${i}_english_name`)?.value || '',
                        birth_date: document.getElementById(`edit_guest_${i}_birth_date`)?.value || '',
                        phone: document.getElementById(`edit_guest_${i}_phone`)?.value || '',
                        email: document.getElementById(`edit_guest_${i}_email`)?.value || '',
                        type: koreanNameEl.dataset.guestType || '',
                        gender: document.getElementById(`edit_guest_${i}_gender`)?.value || ''
                    };
                    guests.push(guest);
                }
            }
            
            // 첫 번째 투숙객을 고객 정보로 사용 (신규 예약 페이지와 동일)
            const customer = guests.length > 0 ? {
                korean_name: guests[0].korean_name,
                english_name: guests[0].english_name,
                phone: guests[0].phone,
                email: guests[0].email
            } : {
                korean_name: '',
                english_name: '',
                phone: '',
                email: ''
            };
            
            // 결제 빌링 수집
            const billings = [];
            document.querySelectorAll('#edit_billings_container .card').forEach(card => {
                const inputs = card.querySelectorAll('input, select');
                const billing = {
                    type: inputs[0].value,
                    amount: parseNumber(inputs[1].value),
                    date: inputs[2].value || null,
                    status: inputs[3].value,
                    notes: inputs[4].value
                };
                billings.push(billing);
            });
            
            // 매입 구성요소 수집
            const cost_components = [];
            document.querySelectorAll('#edit_components_container .card').forEach(card => {
                const inputs = card.querySelectorAll('input, select');
                const component = {
                    component_type: inputs[0].value,
                    vendor_name: inputs[1].value,
                    cost_amount: parseNumber(inputs[2].value),
                    cost_currency: inputs[3].value,
                    cost_krw: parseNumber(inputs[4].value),
                    notes: inputs[5].value
                };
                cost_components.push(component);
            });
            
            // 폼 데이터 수집
            const data = {
                reservation_status: document.getElementById('edit_reservation_status').value,
                platform_name: document.getElementById('edit_platform_name').value,
                package_name: document.getElementById('edit_package_name').value,
                customer: customer,
                travel_period: {
                    departure_date: document.getElementById('edit_departure_date').value,
                    return_date: document.getElementById('edit_return_date').value,
                    nights: (() => {
                        const dep = new Date(document.getElementById('edit_departure_date').value);
                        const ret = new Date(document.getElementById('edit_return_date').value);
                        return Math.floor((ret - dep) / (1000 * 60 * 60 * 24));
                    })(),
                    days: (() => {
                        const dep = new Date(document.getElementById('edit_departure_date').value);
                        const ret = new Date(document.getElementById('edit_return_date').value);
                        return Math.floor((ret - dep) / (1000 * 60 * 60 * 24)) + 1;
                    })()
                },
                people: {
                    adult: parseInt(document.getElementById('edit_people_adult').value),
                    child: parseInt(document.getElementById('edit_people_child').value),
                    infant: parseInt(document.getElementById('edit_people_infant').value)
                },
                flight_info: {
                    outbound_flight: document.getElementById('edit_outbound_flight').value,
                    outbound_departure_time: document.getElementById('edit_outbound_departure_time').value,
                    outbound_arrival_time: document.getElementById('edit_outbound_arrival_time').value,
                    inbound_flight: document.getElementById('edit_inbound_flight').value,
                    inbound_departure_time: document.getElementById('edit_inbound_departure_time').value,
                    inbound_arrival_time: document.getElementById('edit_inbound_arrival_time').value
                },
                hotel_name: document.getElementById('edit_hotel_name').value,
                room_type: document.getElementById('edit_room_type').value,
                itinerary: document.getElementById('edit_itinerary').value,
                inclusions: document.getElementById('edit_inclusions').value,
                exclusions: document.getElementById('edit_exclusions').value,
                guests: guests.filter(g => g && Object.keys(g).length > 0),
                pricing: {
                    currency: document.getElementById('edit_currency').value,
                    exchange_rate: parseNumber(document.getElementById('edit_exchange_rate').value),
                    price_adult: parseNumber(document.getElementById('edit_price_adult').value),
                    price_child: parseNumber(document.getElementById('edit_price_child').value),
                    price_infant: parseNumber(document.getElementById('edit_price_infant').value),
                    total_selling_price: parseNumber(document.getElementById('edit_total_selling_price').value)
                },
                billings: billings,
                cost_components: cost_components,
                special_requests: document.getElementById('edit_special_requests').value
            };

            // 유효성 검사
            if (!data.platform_name || !data.package_name) {
                alert('필수 항목을 입력해주세요.');
                return;
            }

            try {
                const response = await fetch(`/api/package-reservations/${id}`, {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(data)
                });

                const result = await response.json();

                if (result.success) {
                    alert(`예약이 수정되었습니다.\n${result.changes_count || 0}개 필드가 변경되었습니다.`);
                    bootstrap.Modal.getInstance(document.getElementById('editModal')).hide();
                    
                    // 목록 새로고침 후 하이라이트된 행으로 스크롤
                    await loadReservations();
                    setTimeout(() => {
                        const highlightedRow = document.querySelector('.last-edited-row');
                        if (highlightedRow) {
                            highlightedRow.scrollIntoView({ behavior: 'smooth', block: 'center' });
                        }
                    }, 100);
                } else {
                    alert('예약 수정 실패: ' + result.message);
                }
            } catch (error) {
                console.error('예약 수정 오류:', error);
                alert('예약 수정 중 오류가 발생했습니다.');
            }
        }

        // 수배서 모달 열기
        async function openAssignmentModal(id) {
            try {
                const response = await fetch(`/api/package-reservations/${id}`);
                const result = await response.json();

                if (result.success) {
                    const r = result.data;
                    
                    // 기본 예약 정보 표시
                    document.getElementById('assign_reservation_number').textContent = r.reservation_number;
                    document.getElementById('assign_customer_name').textContent = `${r.customer.korean_name}${r.customer.english_name ? ' / ' + r.customer.english_name : ''}`;
                    document.getElementById('assign_departure_date').textContent = new Date(r.travel_period.departure_date).toLocaleDateString('ko-KR');
                    document.getElementById('assign_return_date').textContent = new Date(r.travel_period.return_date).toLocaleDateString('ko-KR');
                    document.getElementById('assign_period').textContent = `${r.travel_period.nights}박${r.travel_period.days}일`;
                    document.getElementById('assign_people').textContent = `성인${r.people.adult}${r.people.child > 0 ? `/소아${r.people.child}` : ''}${r.people.infant > 0 ? `/유아${r.people.infant}` : ''}`;
                    document.getElementById('assign_package_name').textContent = r.package_name;
                    document.getElementById('assign_platform_name').textContent = r.platform_name;
                    
                    // 매입 구성요소별 수배서 목록 생성
                    renderAssignmentList(r);
                    
                    // 모달 열기
                    const modal = new bootstrap.Modal(document.getElementById('assignmentModal'));
                    modal.show();
                } else {
                    alert('예약 정보 로드 실패: ' + result.message);
                }
            } catch (error) {
                console.error('예약 정보 로드 오류:', error);
                alert('예약 정보 로드 중 오류가 발생했습니다.');
            }
        }

        // 매입 구성요소별 수배서 목록 렌더링
        function renderAssignmentList(reservation) {
            const container = document.getElementById('assignment_list');
            
            if (!reservation.cost_components || reservation.cost_components.length === 0) {
                container.innerHTML = '<p class="text-muted">매입 구성요소가 없습니다.</p>';
                return;
            }

            const componentTypeNames = {
                flight: '✈️ 항공권',
                hotel: '🏨 호텔',
                tour: '🎯 투어',
                ground: '🚗 지상비',
                other: '📦 기타'
            };

            let html = '';
            reservation.cost_components.forEach((component, index) => {
                const typeName = componentTypeNames[component.component_type] || component.component_type;
                const costKRW = component.cost_currency === 'USD' 
                    ? (component.cost_amount * (reservation.pricing?.exchange_rate || 1300)).toLocaleString()
                    : component.cost_amount.toLocaleString();
                
                html += `
                    <div class="card mb-3" style="border-left: 4px solid #28a745;">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col-md-4">
                                    <h6 class="mb-1">${typeName} - ${component.vendor_name}</h6>
                                    <small class="text-muted">매입가: ${component.cost_amount.toLocaleString()} ${component.cost_currency} ${component.cost_currency === 'USD' ? `(₩${costKRW})` : ''}</small>
                                    ${component.notes ? `<br><small class="text-muted">비고: ${component.notes}</small>` : ''}
                                </div>
                                <div class="col-md-3">
                                    <div id="assignment_status_${index}">
                                        ${component.assignment_sent_at 
                                            ? `<span class="badge bg-success"><i class="fas fa-check-circle me-1"></i>발송완료</span>
                                               <br><small class="text-muted">${new Date(component.assignment_sent_at).toLocaleString('ko-KR')}</small>`
                                            : '<span class="badge bg-secondary">미발송</span>'
                                        }
                                        ${component.assignment_confirmed_at 
                                            ? `<br><span class="badge bg-info mt-1"><i class="fas fa-eye me-1"></i>수신확인</span>
                                               <br><small class="text-muted">${new Date(component.assignment_confirmed_at).toLocaleString('ko-KR')}</small>`
                                            : ''
                                        }
                                    </div>
                                </div>
                                <div class="col-md-5 text-end">
                                    <button class="btn btn-sm btn-success me-1" onclick="generateAssignment('${reservation._id}', ${index})" title="수배서 생성">
                                        <i class="fas fa-file-alt me-1"></i>수배서 생성
                                    </button>
                                    <button class="btn btn-sm btn-primary me-1" onclick="sendAssignmentEmail('${reservation._id}', ${index})" title="이메일 발송">
                                        <i class="fas fa-envelope me-1"></i>이메일 발송
                                    </button>
                                    <button class="btn btn-sm btn-info" onclick="copyAssignmentLink('${reservation._id}', ${index})" title="링크 복사">
                                        <i class="fas fa-link me-1"></i>링크 복사
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
            });

            container.innerHTML = html;
        }

        // 수배서 생성
        async function generateAssignment(reservationId, componentIndex) {
            try {
                const response = await fetch(`/api/package-reservations/${reservationId}/assignment/${componentIndex}`, {
                    method: 'POST'
                });
                const result = await response.json();

                if (result.success) {
                    alert('수배서가 생성되었습니다.');
                    // 모달 새로고침
                    openAssignmentModal(reservationId);
                } else {
                    alert('수배서 생성 실패: ' + result.message);
                }
            } catch (error) {
                console.error('수배서 생성 오류:', error);
                alert('수배서 생성 중 오류가 발생했습니다.');
            }
        }

        // 수배서 이메일 발송
        async function sendAssignmentEmail(reservationId, componentIndex) {
            const email = prompt('수배서를 받을 이메일 주소를 입력하세요:');
            if (!email) return;

            try {
                const response = await fetch(`/api/package-reservations/${reservationId}/assignment/${componentIndex}/email`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email })
                });
                const result = await response.json();

                if (result.success) {
                    alert(`수배서가 ${email}로 발송되었습니다.`);
                    // 모달 새로고침
                    openAssignmentModal(reservationId);
                } else {
                    alert('이메일 발송 실패: ' + result.message);
                }
            } catch (error) {
                console.error('이메일 발송 오류:', error);
                alert('이메일 발송 중 오류가 발생했습니다.');
            }
        }

        // 수배서 링크 복사
        async function copyAssignmentLink(reservationId, componentIndex) {
            try {
                const response = await fetch(`/api/package-reservations/${reservationId}/assignment/${componentIndex}/link`);
                const result = await response.json();

                if (result.success) {
                    const link = result.link;
                    await navigator.clipboard.writeText(link);
                    alert('수배서 링크가 클립보드에 복사되었습니다.\n\n' + link);
                } else {
                    alert('링크 생성 실패: ' + result.message);
                }
            } catch (error) {
                console.error('링크 복사 오류:', error);
                alert('링크 복사 중 오류가 발생했습니다.');
            }
        }

        function openInvoiceModal(id) {
            alert('확정서 모달 - 추후 구현 예정 (ID: ' + id + ')');
        }

        function openSettlementModal(id) {
            alert('정산관리 모달 - 추후 구현 예정 (ID: ' + id + ')');
        }

        // 페이지 로드 시 실행
        window.addEventListener('load', () => {
            loadReservations();
        });
    </script>
</body>
</html>
