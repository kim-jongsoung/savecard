<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>호텔 정산관리 - 괌세이브카드</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/erp-common.css">
    <style>
        /* 페이지 전용 스타일 */
        .table td { border-right: 1px solid #dee2e6; }
        .table th { border-right: 1px solid #dee2e6; }
        .table td:last-child, .table th:last-child { border-right: none; }
        .status-complete { background: #28a745 !important; color: white !important; }
        .status-incomplete { background: #ffc107 !important; color: #000 !important; }
    </style>
</head>
<body>
    <!-- 네비게이션 -->
    <%- include('../partials/navbar', { currentPage: 'hotel-settlements', adminUsername: adminUsername || '관리자' }) %>

    <div class="container-fluid mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2><i class="fas fa-calculator me-2"></i>호텔 정산관리</h2>
        </div>

        <!-- 필터링 섹션 -->
        <div class="filter-section">
            <div class="row g-2 mb-2">
                <div class="col-md-3">
                    <label class="form-label">기간</label>
                    <div class="input-group input-group-sm">
                        <input type="date" class="form-control" id="startDate">
                        <span class="input-group-text">~</span>
                        <input type="date" class="form-control" id="endDate">
                    </div>
                </div>
                <div class="col-md-2">
                    <label class="form-label">예약업체</label>
                    <select class="form-select form-select-sm" id="filterAgency">
                        <option value="">전체</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label">호텔</label>
                    <select class="form-select form-select-sm" id="filterHotel">
                        <option value="">전체</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label">투숙객명</label>
                    <input type="text" class="form-control form-control-sm" id="filterGuest" placeholder="고객명 검색">
                </div>
                <div class="col-md-1">
                    <label class="form-label">입금</label>
                    <select class="form-select form-select-sm" id="filterPaymentReceived">
                        <option value="">전체</option>
                        <option value="completed">완료</option>
                        <option value="incomplete">미완료</option>
                    </select>
                </div>
                <div class="col-md-1">
                    <label class="form-label">송금</label>
                    <select class="form-select form-select-sm" id="filterPaymentSent">
                        <option value="">전체</option>
                        <option value="completed">완료</option>
                        <option value="incomplete">미완료</option>
                    </select>
                </div>
                <div class="col-md-1 d-flex align-items-end">
                    <button class="btn btn-primary btn-sm w-100" onclick="loadSettlements()">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
            <div class="row g-2">
                <div class="col-md-12 text-end">
                    <button class="btn btn-secondary btn-sm" onclick="resetFilters()">
                        <i class="fas fa-redo me-1"></i>초기화
                    </button>
                </div>
            </div>
        </div>

        <!-- 탭 -->
        <ul class="nav nav-tabs mb-3" id="settlementTabs" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="incomplete-tab" data-bs-toggle="tab" data-bs-target="#incomplete" 
                        type="button" role="tab" onclick="switchTab('incomplete')">
                    미완료
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="complete-tab" data-bs-toggle="tab" data-bs-target="#complete" 
                        type="button" role="tab" onclick="switchTab('complete')">
                    완료
                </button>
            </li>
        </ul>

        <!-- 일괄 처리 버튼 (미완료 탭에만 표시) -->
        <div id="bulkActions" class="mb-3" style="display: none;">
            <button class="btn btn-success btn-sm" onclick="bulkPayment('received')">
                <i class="fas fa-arrow-down me-1"></i>선택 항목 일괄 입금
            </button>
            <button class="btn btn-danger btn-sm" onclick="bulkPayment('sent')">
                <i class="fas fa-arrow-up me-1"></i>선택 항목 일괄 송금
            </button>
            <button class="btn btn-warning btn-sm" onclick="bulkCancelPayment('received')">
                <i class="fas fa-undo me-1"></i>선택 항목 일괄 입금 취소
            </button>
            <button class="btn btn-info btn-sm" onclick="bulkCancelPayment('sent')">
                <i class="fas fa-undo me-1"></i>선택 항목 일괄 송금 취소
            </button>
            <button class="btn btn-outline-secondary btn-sm" onclick="clearSelection()">
                <i class="fas fa-times me-1"></i>선택 해제
            </button>
        </div>

        <!-- 탭 컨텐츠 -->
        <div class="tab-content" id="settlementTabContent">
            <!-- 미완료 탭 -->
            <div class="tab-pane fade show active" id="incomplete" role="tabpanel">
                <!-- 엑셀 다운로드 버튼 -->
                <div class="mb-3 text-end">
                    <button class="btn btn-success btn-sm" onclick="downloadIncompleteExcel()">
                        <i class="fas fa-file-excel me-1"></i>엑셀 다운로드
                    </button>
                </div>
                <!-- 미완료 통계 -->
                <div class="row mb-3" id="incompleteStats">
                    <div class="col-md-6">
                        <div class="card border-warning">
                            <div class="card-body py-2">
                                <h6 class="card-title mb-1">
                                    <i class="fas fa-arrow-down text-success"></i> 입금완료 / 송금미완료
                                </h6>
                                <h4 class="mb-0 text-warning" id="receivedOnlyTotal">₩0</h4>
                                <small class="text-muted" id="receivedOnlyCount">0건</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card border-danger">
                            <div class="card-body py-2">
                                <h6 class="card-title mb-1">
                                    <i class="fas fa-arrow-up text-danger"></i> 송금완료 / 입금미완료
                                </h6>
                                <h4 class="mb-0 text-danger" id="sentOnlyTotal">₩0</h4>
                                <small class="text-muted" id="sentOnlyCount">0건</small>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm">
                        <thead class="table-light">
                            <tr>
                                <th style="width: 40px;">
                                    <input type="checkbox" class="form-check-input" id="selectAll" onchange="toggleSelectAll()">
                                </th>
                                <th>체크인</th>
                                <th>호텔명</th>
                                <th>예약업체</th>
                                <th>투숙객명</th>
                                <th class="text-end" colspan="2">거래액</th>
                                <th class="text-end" colspan="2">호텔매입</th>
                                <th class="text-end" colspan="2">아웃호텔</th>
                                <th class="text-end" colspan="2">수배피</th>
                                <th class="text-end">마진</th>
                                <th class="text-end">부가세</th>
                                <th class="text-center">입금</th>
                                <th class="text-center">송금</th>
                            </tr>
                            <tr>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody id="incompleteList">
                            <tr>
                                <td colspan="17" class="text-center py-5">
                                    <i class="fas fa-spinner fa-spin fa-2x text-muted"></i>
                                    <p class="mt-2 text-muted">데이터를 불러오는 중...</p>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- 완료 탭 -->
            <div class="tab-pane fade" id="complete" role="tabpanel">
                <!-- 엑셀 다운로드 버튼 -->
                <div class="mb-3 text-end">
                    <button class="btn btn-success btn-sm" onclick="downloadExcel()">
                        <i class="fas fa-file-excel me-1"></i>엑셀 다운로드
                    </button>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover table-sm">
                        <thead class="table-light">
                            <tr>
                                <th>체크인</th>
                                <th>호텔명</th>
                                <th>예약업체</th>
                                <th>투숙객명</th>
                                <th class="text-end" colspan="2">거래액</th>
                                <th class="text-end" colspan="2">호텔매입</th>
                                <th class="text-end" colspan="2">아웃호텔</th>
                                <th class="text-end" colspan="2">수배피</th>
                                <th class="text-end">마진</th>
                                <th class="text-end">부가세</th>
                                <th class="text-center">입금일</th>
                                <th class="text-center">송금일</th>
                            </tr>
                            <tr>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th class="text-end" style="font-size: 0.75rem;">USD</th>
                                <th class="text-end" style="font-size: 0.75rem;">KRW</th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody id="completeList">
                            <tr>
                                <td colspan="16" class="text-center py-5">
                                    <i class="fas fa-spinner fa-spin fa-2x text-muted"></i>
                                    <p class="mt-2 text-muted">데이터를 불러오는 중...</p>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- 모달은 수배관리 페이지에서 사용 -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.sheetjs.com/xlsx-0.20.1/package/dist/xlsx.full.min.js"></script>
    <script>
        let currentTab = 'incomplete';
        let selectedIds = new Set();

        // 페이지 로드 시
        document.addEventListener('DOMContentLoaded', function() {
            // 기본 날짜 설정 (최근 3개월)
            const today = new Date();
            const threeMonthsAgo = new Date(today.getFullYear(), today.getMonth() - 3, 1);
            document.getElementById('startDate').valueAsDate = threeMonthsAgo;
            document.getElementById('endDate').valueAsDate = today;
            
            // 필터 옵션 로드
            loadFilterOptions();
            
            // 정산 목록 로드 (미완료 탭은 전체, 완료 탭은 기간 필터)
            loadSettlements();
        });

        // 필터 옵션 로드
        async function loadFilterOptions() {
            try {
                // 예약업체 목록
                console.log('🔍 예약업체 목록 API 호출...');
                const agencyRes = await fetch('/api/hotel-settlements/agencies');
                console.log('📡 예약업체 응답 상태:', agencyRes.status);
                if (!agencyRes.ok) {
                    const errorText = await agencyRes.text();
                    console.error('❌ 예약업체 목록 로드 실패:', agencyRes.status, errorText);
                    // 실패해도 계속 진행
                } else {
                    const agencies = await agencyRes.json();
                    console.log('📦 예약업체 데이터:', agencies);
                    const agencySelect = document.getElementById('filterAgency');
                    if (Array.isArray(agencies)) {
                        agencies.forEach(agency => {
                            const option = document.createElement('option');
                            option.value = agency.agency_name;
                            option.textContent = agency.agency_name;
                            agencySelect.appendChild(option);
                        });
                    }
                }

                // 호텔 목록
                const hotelRes = await fetch('/api/hotel-settlements/hotels');
                if (!hotelRes.ok) {
                    console.error('호텔 목록 로드 실패:', hotelRes.status);
                    return;
                }
                const hotels = await hotelRes.json();
                const hotelSelect = document.getElementById('filterHotel');
                if (Array.isArray(hotels)) {
                    hotels.forEach(hotel => {
                        const option = document.createElement('option');
                        option.value = hotel.hotel_name;
                        option.textContent = hotel.hotel_name;
                        hotelSelect.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('필터 옵션 로드 실패:', error);
            }
        }

        // 정산 목록 로드
        async function loadSettlements() {
            const startDate = document.getElementById('startDate').value;
            const endDate = document.getElementById('endDate').value;
            const agency = document.getElementById('filterAgency').value;
            const hotel = document.getElementById('filterHotel').value;
            const guest = document.getElementById('filterGuest').value;
            const paymentReceived = document.getElementById('filterPaymentReceived').value;
            const paymentSent = document.getElementById('filterPaymentSent').value;

            const params = new URLSearchParams({
                status: currentTab === 'incomplete' ? 'incomplete' : 'completed'
            });

            // 기간 필터 (항상 적용)
            if (startDate) params.append('start_date', startDate);
            if (endDate) params.append('end_date', endDate);

            if (agency) params.append('agency', agency);
            if (hotel) params.append('hotel', hotel);
            if (guest) params.append('guest', guest);
            if (paymentReceived) params.append('payment_received', paymentReceived);
            if (paymentSent) params.append('payment_sent', paymentSent);

            try {
                console.log('🔍 API 호출:', `/api/hotel-settlements-list?${params}`);
                const response = await fetch(`/api/hotel-settlements-list?${params}`);
                console.log('📡 응답 상태:', response.status);
                const data = await response.json();
                console.log('📦 응답 데이터:', data);

                if (data.success) {
                    renderSettlements(data.data);
                } else {
                    console.error('❌ 데이터 로드 실패:', data.message);
                    alert('데이터 로드 실패: ' + data.message);
                }
            } catch (error) {
                console.error('❌ 정산 목록 로드 실패:', error);
                alert('정산 목록을 불러오는데 실패했습니다.');
            }
        }

        // 정산 목록 렌더링
        function renderSettlements(settlements) {
            const listId = currentTab === 'incomplete' ? 'incompleteList' : 'completeList';
            const tbody = document.getElementById(listId);

            if (!settlements || settlements.length === 0) {
                tbody.innerHTML = `
                    <tr>
                        <td colspan="${currentTab === 'incomplete' ? 17 : 16}" class="text-center py-5">
                            <i class="fas fa-inbox fa-2x text-muted"></i>
                            <p class="mt-2 text-muted">정산 데이터가 없습니다</p>
                        </td>
                    </tr>
                `;
                return;
            }

            // 합계 계산용 변수
            let totalSellingUSD = 0, totalSellingKRW = 0;
            let totalCostUSD = 0, totalCostKRW = 0;
            let totalOutHotelUSD = 0, totalOutHotelKRW = 0;
            let totalAgencyFeeUSD = 0, totalAgencyFeeKRW = 0;
            let totalMarginKRW = 0, totalTax = 0;
            
            // 미완료 탭 통계용 변수
            let receivedOnlyTotal = 0, receivedOnlyCount = 0;
            let sentOnlyTotal = 0, sentOnlyCount = 0;

            const rows = settlements.map(s => {
                const sellingPriceUSD = parseFloat(s.total_selling_price || 0);
                const costPriceUSD = parseFloat(s.total_cost_price || 0);
                const outHotelCostUSD = parseFloat(s.out_hotel_cost || 0);
                const agencyFeeUSD = parseFloat(s.agency_fee || 0);
                
                const settlementRate = parseFloat(s.exchange_rate || 1470);
                const remittanceRate = parseFloat(s.remittance_rate || settlementRate);
                
                const isPaymentSent = !!s.payment_sent_date;
                const displayRate = isPaymentSent ? remittanceRate : settlementRate;
                
                // 디버깅: 환율 정보 확인
                if (isPaymentSent) {
                    console.log('💱 송금 완료 건 환율:', {
                        id: s.id,
                        settlementRate,
                        remittanceRate,
                        displayRate,
                        isPaymentSent
                    });
                }
                
                const sellingPriceKRW = Math.round(sellingPriceUSD * settlementRate);
                const costPriceKRW = Math.round(costPriceUSD * displayRate);
                const outHotelCostKRW = Math.round(outHotelCostUSD * displayRate);
                const agencyFeeKRW = Math.round(agencyFeeUSD * settlementRate);
                
                const marginKRW = sellingPriceKRW - costPriceKRW - outHotelCostKRW;
                const tax = Math.round(marginKRW * 0.1);

                // 합계 누적
                totalSellingUSD += sellingPriceUSD;
                totalSellingKRW += sellingPriceKRW;
                totalCostUSD += costPriceUSD;
                totalCostKRW += costPriceKRW;
                totalOutHotelUSD += outHotelCostUSD;
                totalOutHotelKRW += outHotelCostKRW;
                totalAgencyFeeUSD += agencyFeeUSD;
                totalAgencyFeeKRW += agencyFeeKRW;
                totalMarginKRW += marginKRW;
                totalTax += tax;
                
                // 미완료 탭 통계 계산 (거래액 기준)
                if (currentTab === 'incomplete') {
                    const hasReceived = !!s.payment_received_date;
                    const hasSent = !!s.payment_sent_date;
                    
                    if (hasReceived && !hasSent) {
                        // 입금완료, 송금미완료
                        receivedOnlyTotal += sellingPriceKRW;
                        receivedOnlyCount++;
                    } else if (!hasReceived && hasSent) {
                        // 송금완료, 입금미완료
                        sentOnlyTotal += sellingPriceKRW;
                        sentOnlyCount++;
                    }
                }

                const checkInDate = s.check_in_date ? new Date(s.check_in_date).toLocaleDateString('ko-KR') : '-';
                const paymentReceivedDate = s.payment_received_date ? new Date(s.payment_received_date).toLocaleDateString('ko-KR') : '-';
                const paymentSentDate = s.payment_sent_date ? new Date(s.payment_sent_date).toLocaleDateString('ko-KR') : '-';

                if (currentTab === 'incomplete') {
                    return `
                        <tr>
                            <td>
                                <input type="checkbox" class="form-check-input" value="${s.id}" 
                                       onchange="toggleSelection(${s.id})">
                            </td>
                            <td>${checkInDate}</td>
                            <td>${s.hotel_name || '-'}</td>
                            <td>${s.booking_agency_name || '-'}</td>
                            <td>${s.guest_name || '-'}</td>
                            <td class="text-end">$${sellingPriceUSD.toFixed(2)}</td>
                            <td class="text-end">₩${sellingPriceKRW.toLocaleString()}<br><small class="text-muted">(×${settlementRate.toFixed(2)})</small></td>
                            <td class="text-end">$${costPriceUSD.toFixed(2)}</td>
                            <td class="text-end">₩${costPriceKRW.toLocaleString()}<br><small class="text-muted">(×${displayRate.toFixed(2)})</small></td>
                            <td class="text-end">$${outHotelCostUSD.toFixed(2)}</td>
                            <td class="text-end">₩${outHotelCostKRW.toLocaleString()}<br><small class="text-muted">(×${displayRate.toFixed(2)})</small></td>
                            <td class="text-end">$${agencyFeeUSD.toFixed(2)}</td>
                            <td class="text-end">₩${agencyFeeKRW.toLocaleString()}<br><small class="text-muted">(×${settlementRate.toFixed(2)})</small></td>
                            <td class="text-end ${marginKRW >= 0 ? 'text-primary' : 'text-danger'}">₩${marginKRW.toLocaleString()}</td>
                            <td class="text-end">₩${tax.toLocaleString()}</td>
                            <td class="text-center">
                                ${s.payment_received_date 
                                    ? `<div><i class="fas fa-check text-success"></i></div><div style="font-size: 0.85rem; margin-top: 2px;">${paymentReceivedDate}</div>` 
                                    : '<i class="fas fa-times text-muted"></i>'}
                            </td>
                            <td class="text-center">
                                ${s.payment_sent_date 
                                    ? `<div><i class="fas fa-check text-success"></i></div><div style="font-size: 0.85rem; margin-top: 2px;">${paymentSentDate}</div>` 
                                    : '<i class="fas fa-times text-muted"></i>'}
                            </td>
                        </tr>
                    `;
                } else {
                    return `
                        <tr>
                            <td>${checkInDate}</td>
                            <td>${s.hotel_name || '-'}</td>
                            <td>${s.booking_agency_name || '-'}</td>
                            <td>${s.guest_name || '-'}</td>
                            <td class="text-end">$${sellingPriceUSD.toFixed(2)}</td>
                            <td class="text-end">₩${sellingPriceKRW.toLocaleString()}<br><small class="text-muted">(×${settlementRate.toFixed(2)})</small></td>
                            <td class="text-end">$${costPriceUSD.toFixed(2)}</td>
                            <td class="text-end">₩${costPriceKRW.toLocaleString()}<br><small class="text-muted">(×${displayRate.toFixed(2)})</small></td>
                            <td class="text-end">$${outHotelCostUSD.toFixed(2)}</td>
                            <td class="text-end">₩${outHotelCostKRW.toLocaleString()}<br><small class="text-muted">(×${displayRate.toFixed(2)})</small></td>
                            <td class="text-end">$${agencyFeeUSD.toFixed(2)}</td>
                            <td class="text-end">₩${agencyFeeKRW.toLocaleString()}<br><small class="text-muted">(×${settlementRate.toFixed(2)})</small></td>
                            <td class="text-end ${marginKRW >= 0 ? 'text-primary' : 'text-danger'}">₩${marginKRW.toLocaleString()}</td>
                            <td class="text-end">₩${tax.toLocaleString()}</td>
                            <td class="text-center">${paymentReceivedDate}</td>
                            <td class="text-center">${paymentSentDate}</td>
                        </tr>
                    `;
                }
            }).join('');

            // 합계 행 추가
            const totalRow = currentTab === 'incomplete' ? `
                <tr style="background-color: #fff3cd; font-weight: bold; border-top: 3px solid #ffc107;">
                    <td></td>
                    <td colspan="4" class="text-center">합계 (${settlements.length}건)</td>
                    <td class="text-end">$${totalSellingUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalSellingKRW).toLocaleString()}</td>
                    <td class="text-end">$${totalCostUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalCostKRW).toLocaleString()}</td>
                    <td class="text-end">$${totalOutHotelUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalOutHotelKRW).toLocaleString()}</td>
                    <td class="text-end">$${totalAgencyFeeUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalAgencyFeeKRW).toLocaleString()}</td>
                    <td class="text-end ${totalMarginKRW >= 0 ? 'text-primary' : 'text-danger'}">₩${Math.round(totalMarginKRW).toLocaleString()}</td>
                    <td class="text-end">₩${Math.round(totalTax).toLocaleString()}</td>
                    <td></td>
                    <td></td>
                </tr>
            ` : `
                <tr style="background-color: #d1ecf1; font-weight: bold; border-top: 3px solid #0dcaf0;">
                    <td colspan="4" class="text-center">합계 (${settlements.length}건)</td>
                    <td class="text-end">$${totalSellingUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalSellingKRW).toLocaleString()}</td>
                    <td class="text-end">$${totalCostUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalCostKRW).toLocaleString()}</td>
                    <td class="text-end">$${totalOutHotelUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalOutHotelKRW).toLocaleString()}</td>
                    <td class="text-end">$${totalAgencyFeeUSD.toFixed(2)}</td>
                    <td class="text-end">₩${Math.round(totalAgencyFeeKRW).toLocaleString()}</td>
                    <td class="text-end ${totalMarginKRW >= 0 ? 'text-primary' : 'text-danger'}">₩${Math.round(totalMarginKRW).toLocaleString()}</td>
                    <td class="text-end">₩${Math.round(totalTax).toLocaleString()}</td>
                    <td></td>
                    <td></td>
                </tr>
            `;

            tbody.innerHTML = rows + totalRow;
            
            // 미완료 탭 통계 업데이트
            if (currentTab === 'incomplete') {
                document.getElementById('receivedOnlyTotal').textContent = '₩' + Math.round(receivedOnlyTotal).toLocaleString();
                document.getElementById('receivedOnlyCount').textContent = receivedOnlyCount + '건';
                document.getElementById('sentOnlyTotal').textContent = '₩' + Math.round(sentOnlyTotal).toLocaleString();
                document.getElementById('sentOnlyCount').textContent = sentOnlyCount + '건';
            }
        }

        // 탭 전환
        function switchTab(tab) {
            currentTab = tab;
            
            // 일괄 처리 버튼은 미완료 탭에서만 표시
            document.getElementById('bulkActions').style.display = tab === 'incomplete' ? 'block' : 'none';
            
            // 미완료 통계는 미완료 탭에서만 표시
            const incompleteStats = document.getElementById('incompleteStats');
            if (incompleteStats) {
                incompleteStats.style.display = tab === 'incomplete' ? 'flex' : 'none';
            }
            
            clearSelection();
            loadSettlements();
        }

        // 체크박스 선택
        function toggleSelection(id) {
            console.log('✅ 체크박스 토글:', id, typeof id);
            
            if (selectedIds.has(id)) {
                selectedIds.delete(id);
                console.log('➖ 선택 해제:', id);
            } else {
                selectedIds.add(id);
                console.log('➕ 선택 추가:', id);
            }
            
            console.log('📋 현재 선택된 ID 목록:', Array.from(selectedIds));
            
            // 선택된 항목이 있으면 일괄 처리 버튼 표시
            const bulkActions = document.getElementById('bulkActions');
            if (selectedIds.size > 0) {
                bulkActions.style.display = 'block';
            } else {
                bulkActions.style.display = 'none';
            }
        }

        // 전체 선택
        function toggleSelectAll() {
            const checkboxes = document.querySelectorAll('#incompleteList input[type="checkbox"]');
            const selectAll = document.getElementById('selectAll').checked;
            
            checkboxes.forEach(cb => {
                cb.checked = selectAll;
                const id = parseInt(cb.value);
                if (selectAll) {
                    selectedIds.add(id);
                } else {
                    selectedIds.delete(id);
                }
            });
            
            // 일괄 처리 버튼 표시/숨김
            const bulkActions = document.getElementById('bulkActions');
            bulkActions.style.display = selectedIds.size > 0 ? 'block' : 'none';
        }

        // 선택 해제
        function clearSelection() {
            selectedIds.clear();
            document.getElementById('selectAll').checked = false;
            document.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.checked = false);
            document.getElementById('bulkActions').style.display = 'none';
        }

        // 일괄 입금/송금 처리
        async function bulkPayment(type) {
            if (selectedIds.size === 0) {
                alert('처리할 항목을 선택해주세요.');
                return;
            }

            const typeText = type === 'received' ? '입금' : '송금';
            const today = new Date().toISOString().split('T')[0];
            const date = prompt(`${typeText} 날짜를 입력하세요 (YYYY-MM-DD):`, today);
            
            if (!date) return;

            let exchangeRate = null;
            if (type === 'sent') {
                const rateInput = prompt('송금 환율을 입력하세요 (예: 1450):', '1450');
                if (!rateInput) return;
                exchangeRate = parseFloat(rateInput);
                if (isNaN(exchangeRate) || exchangeRate <= 0) {
                    alert('올바른 환율을 입력해주세요.');
                    return;
                }
            }

            if (!confirm(`선택한 ${selectedIds.size}개 항목을 ${typeText} 처리하시겠습니까?`)) {
                return;
            }

            const reservationIds = Array.from(selectedIds);
            console.log('🔍 선택된 예약 ID 목록:', reservationIds);
            console.log('🔍 전송할 데이터:', { reservation_ids: reservationIds, type, date, exchange_rate: exchangeRate });

            try {
                const response = await fetch('/api/hotel-settlements/bulk-payment', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        reservation_ids: reservationIds,
                        type: type,
                        date: date,
                        exchange_rate: exchangeRate
                    })
                });

                const result = await response.json();
                console.log('💰 일괄 처리 결과:', result);

                if (result.success) {
                    alert(`✅ ${result.count || selectedIds.size}개 항목의 ${typeText} 처리가 완료되었습니다.\n\n처리된 날짜가 표시됩니다.`);
                    clearSelection();
                    // 강제 새로고침으로 최신 데이터 로드
                    await loadSettlements();
                    console.log('🔄 데이터 새로고침 완료');
                } else {
                    alert(`❌ ${typeText} 처리 실패: ` + result.message);
                }
            } catch (error) {
                console.error('일괄 처리 실패:', error);
                alert(`일괄 ${typeText} 처리 중 오류가 발생했습니다.`);
            }
        }

        // 일괄 입금/송금 취소 처리
        async function bulkCancelPayment(type) {
            if (selectedIds.size === 0) {
                alert('취소할 항목을 선택해주세요.');
                return;
            }

            const typeText = type === 'received' ? '입금' : '송금';
            
            if (!confirm(`선택한 ${selectedIds.size}개 항목의 ${typeText}을 취소하시겠습니까?\n\n${typeText} 날짜가 삭제되고 미완료 상태로 되돌아갑니다.`)) {
                return;
            }

            const reservationIds = Array.from(selectedIds);
            console.log('🔄 선택된 예약 ID 목록:', reservationIds);
            console.log('🔄 전송할 데이터:', { reservation_ids: reservationIds, type });

            try {
                const response = await fetch('/api/hotel-settlements/bulk-cancel-payment', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        reservation_ids: reservationIds,
                        type: type
                    })
                });

                const result = await response.json();
                console.log('🔄 일괄 취소 결과:', result);

                if (result.success) {
                    alert(`✅ ${result.count || selectedIds.size}개 항목의 ${typeText}이 취소되었습니다.\n\n미완료 상태로 되돌아갑니다.`);
                    clearSelection();
                    // 강제 새로고침으로 최신 데이터 로드
                    await loadSettlements();
                    console.log('🔄 데이터 새로고침 완료');
                } else {
                    alert(`❌ ${typeText} 취소 실패: ` + result.message);
                }
            } catch (error) {
                console.error('일괄 취소 실패:', error);
                alert(`일괄 ${typeText} 취소 중 오류가 발생했습니다.`);
            }
        }

        // 필터 초기화
        function resetFilters() {
            // 기간 초기화 (최근 3개월)
            const today = new Date();
            const threeMonthsAgo = new Date(today.getFullYear(), today.getMonth() - 3, 1);
            document.getElementById('startDate').valueAsDate = threeMonthsAgo;
            document.getElementById('endDate').valueAsDate = today;
            
            document.getElementById('filterAgency').value = '';
            document.getElementById('filterHotel').value = '';
            document.getElementById('filterGuest').value = '';
            document.getElementById('filterPaymentReceived').value = '';
            document.getElementById('filterPaymentSent').value = '';
            loadSettlements();
        }

        // 예약 상세 보기 (수배관리 페이지로 이동)
        function openReservationModal(reservationId) {
            // 수배관리 페이지로 이동하면서 자동으로 모달 열기
            window.location.href = `/admin/hotel-assignments?open=${reservationId}`;
        }

        // 미정산 엑셀 다운로드
        async function downloadIncompleteExcel() {
            try {
                // 현재 필터 조건으로 미정산 데이터 가져오기
                const startDate = document.getElementById('startDate').value;
                const endDate = document.getElementById('endDate').value;
                const agency = document.getElementById('filterAgency').value;
                const hotel = document.getElementById('filterHotel').value;
                const guest = document.getElementById('filterGuest').value;
                const paymentReceived = document.getElementById('filterPaymentReceived').value;
                const paymentSent = document.getElementById('filterPaymentSent').value;

                const params = new URLSearchParams({
                    status: 'incomplete'
                });

                if (startDate) params.append('start_date', startDate);
                if (endDate) params.append('end_date', endDate);
                if (agency) params.append('agency', agency);
                if (hotel) params.append('hotel', hotel);
                if (guest) params.append('guest', guest);
                if (paymentReceived) params.append('payment_received', paymentReceived);
                if (paymentSent) params.append('payment_sent', paymentSent);

                const response = await fetch(`/api/hotel-settlements-list?${params}`);
                const data = await response.json();

                if (!data.success || !data.data || data.data.length === 0) {
                    alert('다운로드할 데이터가 없습니다.');
                    return;
                }

                // 엑셀 데이터 생성
                const excelData = data.data.map(s => {
                    const sellingPriceUSD = parseFloat(s.total_selling_price || 0);
                    const costPriceUSD = parseFloat(s.total_cost_price || 0);
                    const outHotelCostUSD = parseFloat(s.out_hotel_cost || 0);
                    const agencyFeeUSD = parseFloat(s.agency_fee || 0);
                    
                    const settlementRate = parseFloat(s.exchange_rate || 1470);
                    const remittanceRate = parseFloat(s.remittance_rate || settlementRate);
                    const displayRate = remittanceRate;
                    
                    const sellingPriceKRW = Math.round(sellingPriceUSD * settlementRate);
                    const costPriceKRW = Math.round(costPriceUSD * displayRate);
                    const outHotelCostKRW = Math.round(outHotelCostUSD * displayRate);
                    const agencyFeeKRW = Math.round(agencyFeeUSD * settlementRate);
                    const marginKRW = sellingPriceKRW - costPriceKRW - outHotelCostKRW;
                    const tax = Math.round(marginKRW * 0.1);

                    const checkInDate = s.check_in_date ? new Date(s.check_in_date).toLocaleDateString('ko-KR') : '';
                    const paymentReceivedStatus = s.payment_received_date ? '완료' : '미완료';
                    const paymentSentStatus = s.payment_sent_date ? '완료' : '미완료';

                    return {
                        '체크인일': checkInDate,
                        '호텔명': s.hotel_name || '',
                        '예약업체': s.booking_agency_name || '',
                        '투숙객명': s.guest_name || '',
                        '거래액(USD)': sellingPriceUSD.toFixed(2),
                        '거래액(KRW)': sellingPriceKRW,
                        '정산환율': settlementRate.toFixed(2),
                        '호텔매입(USD)': costPriceUSD.toFixed(2),
                        '호텔매입(KRW)': costPriceKRW,
                        '송금환율': displayRate.toFixed(2),
                        '아웃호텔(USD)': outHotelCostUSD.toFixed(2),
                        '아웃호텔(KRW)': outHotelCostKRW,
                        '수배피(USD)': agencyFeeUSD.toFixed(2),
                        '수배피(KRW)': agencyFeeKRW,
                        '마진(KRW)': marginKRW,
                        '부가세(KRW)': tax,
                        '입금상태': paymentReceivedStatus,
                        '송금상태': paymentSentStatus
                    };
                });

                // 워크북 생성
                const wb = XLSX.utils.book_new();
                const ws = XLSX.utils.json_to_sheet(excelData);

                // 컬럼 너비 설정
                ws['!cols'] = [
                    { wch: 12 }, // 체크인일
                    { wch: 20 }, // 호텔명
                    { wch: 15 }, // 예약업체
                    { wch: 12 }, // 투숙객명
                    { wch: 12 }, // 거래액(USD)
                    { wch: 15 }, // 거래액(KRW)
                    { wch: 10 }, // 정산환율
                    { wch: 12 }, // 호텔매입(USD)
                    { wch: 15 }, // 호텔매입(KRW)
                    { wch: 10 }, // 송금환율
                    { wch: 12 }, // 아웃호텔(USD)
                    { wch: 15 }, // 아웃호텔(KRW)
                    { wch: 12 }, // 수배피(USD)
                    { wch: 15 }, // 수배피(KRW)
                    { wch: 15 }, // 마진(KRW)
                    { wch: 15 }, // 부가세(KRW)
                    { wch: 10 }, // 입금상태
                    { wch: 10 }  // 송금상태
                ];

                XLSX.utils.book_append_sheet(wb, ws, '미정산');

                // 파일명 생성 (날짜 포함)
                const today = new Date();
                const fileName = `호텔미정산_${today.getFullYear()}${String(today.getMonth()+1).padStart(2,'0')}${String(today.getDate()).padStart(2,'0')}.xlsx`;

                // 다운로드
                XLSX.writeFile(wb, fileName);

                alert(`엑셀 파일이 다운로드되었습니다.\n파일명: ${fileName}`);
            } catch (error) {
                console.error('엑셀 다운로드 실패:', error);
                alert('엑셀 다운로드 중 오류가 발생했습니다.');
            }
        }

        // 정산완료 엑셀 다운로드
        async function downloadExcel() {
            try {
                // 현재 필터 조건으로 완료 데이터 가져오기
                const startDate = document.getElementById('startDate').value;
                const endDate = document.getElementById('endDate').value;
                const agency = document.getElementById('filterAgency').value;
                const hotel = document.getElementById('filterHotel').value;
                const guest = document.getElementById('filterGuest').value;
                const paymentReceived = document.getElementById('filterPaymentReceived').value;
                const paymentSent = document.getElementById('filterPaymentSent').value;

                const params = new URLSearchParams({
                    status: 'completed'
                });

                if (startDate) params.append('start_date', startDate);
                if (endDate) params.append('end_date', endDate);
                if (agency) params.append('agency', agency);
                if (hotel) params.append('hotel', hotel);
                if (guest) params.append('guest', guest);
                if (paymentReceived) params.append('payment_received', paymentReceived);
                if (paymentSent) params.append('payment_sent', paymentSent);

                const response = await fetch(`/api/hotel-settlements-list?${params}`);
                const data = await response.json();

                if (!data.success || !data.data || data.data.length === 0) {
                    alert('다운로드할 데이터가 없습니다.');
                    return;
                }

                // 엑셀 데이터 생성
                const excelData = data.data.map(s => {
                    const sellingPriceUSD = parseFloat(s.total_selling_price || 0);
                    const costPriceUSD = parseFloat(s.total_cost_price || 0);
                    const outHotelCostUSD = parseFloat(s.out_hotel_cost || 0);
                    const agencyFeeUSD = parseFloat(s.agency_fee || 0);
                    
                    const settlementRate = parseFloat(s.exchange_rate || 1470);
                    const remittanceRate = parseFloat(s.remittance_rate || settlementRate);
                    const displayRate = remittanceRate;
                    
                    const sellingPriceKRW = Math.round(sellingPriceUSD * settlementRate);
                    const costPriceKRW = Math.round(costPriceUSD * displayRate);
                    const outHotelCostKRW = Math.round(outHotelCostUSD * displayRate);
                    const agencyFeeKRW = Math.round(agencyFeeUSD * settlementRate);
                    const marginKRW = sellingPriceKRW - costPriceKRW - outHotelCostKRW;
                    const tax = Math.round(marginKRW * 0.1);

                    const checkInDate = s.check_in_date ? new Date(s.check_in_date).toLocaleDateString('ko-KR') : '';
                    const paymentReceivedDate = s.payment_received_date ? new Date(s.payment_received_date).toLocaleDateString('ko-KR') : '';
                    const paymentSentDate = s.payment_sent_date ? new Date(s.payment_sent_date).toLocaleDateString('ko-KR') : '';

                    return {
                        '체크인일': checkInDate,
                        '호텔명': s.hotel_name || '',
                        '예약업체': s.booking_agency_name || '',
                        '투숙객명': s.guest_name || '',
                        '거래액(USD)': sellingPriceUSD.toFixed(2),
                        '거래액(KRW)': sellingPriceKRW,
                        '정산환율': settlementRate.toFixed(2),
                        '호텔매입(USD)': costPriceUSD.toFixed(2),
                        '호텔매입(KRW)': costPriceKRW,
                        '송금환율': displayRate.toFixed(2),
                        '아웃호텔(USD)': outHotelCostUSD.toFixed(2),
                        '아웃호텔(KRW)': outHotelCostKRW,
                        '수배피(USD)': agencyFeeUSD.toFixed(2),
                        '수배피(KRW)': agencyFeeKRW,
                        '마진(KRW)': marginKRW,
                        '부가세(KRW)': tax,
                        '입금일': paymentReceivedDate,
                        '송금일': paymentSentDate
                    };
                });

                // 워크북 생성
                const wb = XLSX.utils.book_new();
                const ws = XLSX.utils.json_to_sheet(excelData);

                // 컬럼 너비 설정
                ws['!cols'] = [
                    { wch: 12 }, // 체크인일
                    { wch: 20 }, // 호텔명
                    { wch: 15 }, // 예약업체
                    { wch: 12 }, // 투숙객명
                    { wch: 12 }, // 거래액(USD)
                    { wch: 15 }, // 거래액(KRW)
                    { wch: 10 }, // 정산환율
                    { wch: 12 }, // 호텔매입(USD)
                    { wch: 15 }, // 호텔매입(KRW)
                    { wch: 10 }, // 송금환율
                    { wch: 12 }, // 아웃호텔(USD)
                    { wch: 15 }, // 아웃호텔(KRW)
                    { wch: 12 }, // 수배피(USD)
                    { wch: 15 }, // 수배피(KRW)
                    { wch: 15 }, // 마진(KRW)
                    { wch: 15 }, // 부가세(KRW)
                    { wch: 12 }, // 입금일
                    { wch: 12 }  // 송금일
                ];

                XLSX.utils.book_append_sheet(wb, ws, '호텔정산');

                // 파일명 생성 (날짜 포함)
                const today = new Date();
                const fileName = `호텔정산_${today.getFullYear()}${String(today.getMonth()+1).padStart(2,'0')}${String(today.getDate()).padStart(2,'0')}.xlsx`;

                // 다운로드
                XLSX.writeFile(wb, fileName);

                alert(`엑셀 파일이 다운로드되었습니다.\n파일명: ${fileName}`);
            } catch (error) {
                console.error('엑셀 다운로드 실패:', error);
                alert('엑셀 다운로드 중 오류가 발생했습니다.');
            }
        }
    </script>
</body>
</html>
