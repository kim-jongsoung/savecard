<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || '수배서' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h3><i class="bi bi-file-text me-2"></i>수배서 (안전 모드)</h3>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h5>기본 정보</h5>
                        <p><strong>예약번호:</strong> <%= assignment.reservation_number || assignment.id || '정보없음' %></p>
                        <p><strong>예약자명:</strong> <%= assignment.customer_name || '정보없음' %></p>
                        <p><strong>상품명:</strong> <%= assignment.product_name || '정보없음' %></p>
                        <p><strong>업체명:</strong> <%= assignment.vendor_name || '정보없음' %></p>
                    </div>
                    <div class="col-md-6">
                        <h5>수배 정보</h5>
                        <p><strong>수배업체:</strong> <%= assignment.assignment_vendor || '미지정' %></p>
                        <p><strong>상태:</strong> <%= assignment.status || '정보없음' %></p>
                        <p><strong>생성일:</strong> <%= assignment.created_at || '정보없음' %></p>
                    </div>
                </div>
                
                <hr>
                
                <div class="row">
                    <div class="col-12">
                        <h5>상세 정보</h5>
                        <p><strong>출발일:</strong> <%= assignment.departure_date || assignment.usage_date || '정보없음' %></p>
                        <p><strong>시간:</strong> <%= assignment.usage_time || '정보없음' %></p>
                        <p><strong>성인:</strong> <%= assignment.adult_count || 0 %>명</p>
                        <p><strong>아동:</strong> <%= assignment.child_count || 0 %>명</p>
                        <% if (assignment.people_infant) { %>
                        <p><strong>유아:</strong> <%= assignment.people_infant %>명</p>
                        <% } %>
                        <% if (assignment.total_amount) { %>
                        <p><strong>총 금액:</strong> <%= assignment.total_amount %>원</p>
                        <% } %>
                    </div>
                </div>
                
                <% if (assignment.phone_number || assignment.email) { %>
                <hr>
                <div class="row">
                    <div class="col-12">
                        <h5>연락처</h5>
                        <% if (assignment.phone_number) { %>
                        <p><strong>전화:</strong> <%= assignment.phone_number %></p>
                        <% } %>
                        <% if (assignment.email) { %>
                        <p><strong>이메일:</strong> <%= assignment.email %></p>
                        <% } %>
                    </div>
                </div>
                <% } %>
                
                <% if (assignment.special_requests) { %>
                <hr>
                <div class="row">
                    <div class="col-12">
                        <h5>특별 요청사항</h5>
                        <div class="alert alert-info">
                            <%= assignment.special_requests %>
                        </div>
                    </div>
                </div>
                <% } %>
            </div>
            <div class="card-footer text-center">
                <button onclick="window.print()" class="btn btn-primary">
                    <i class="bi bi-printer me-1"></i>인쇄
                </button>
                <button onclick="window.close()" class="btn btn-secondary">
                    <i class="bi bi-x-circle me-1"></i>닫기
                </button>
            </div>
        </div>
    </div>
</body>
</html>
