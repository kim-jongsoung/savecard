<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>괌 공항 픽업 서비스 예약 - 호텔에서 공항까지</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Select2 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet" />
  <style>
    .header-brand {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px 0;
      margin-bottom: 20px;
    }
    @media (min-width: 768px) {
      .header-brand {
        padding: 30px 0;
      }
    }
    .info-box {
      background: #f8f9fa;
      border-left: 4px solid #667eea;
      padding: 20px;
      margin-bottom: 30px;
      border-radius: 8px;
    }
    .price-badge {
      font-size: 2rem;
      font-weight: bold;
      color: #667eea;
    }
    .step-box {
      background: white;
      border: 2px solid #e9ecef;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 20px;
      transition: all 0.3s;
    }
    .step-box:hover {
      border-color: #667eea;
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.2);
    }
    .step-number {
      width: 40px;
      height: 40px;
      background: #667eea;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      font-size: 1.2rem;
    }
    .highlight {
      background: #fff3cd;
      padding: 2px 6px;
      border-radius: 4px;
      font-weight: bold;
    }
    /* Select2 커스텀 스타일 */
    .select2-container--bootstrap-5 .select2-selection {
      min-height: 38px;
      font-size: 1rem;
    }
    .select2-container--bootstrap-5 .select2-selection--single .select2-selection__rendered {
      line-height: 38px;
      padding-left: 12px;
    }
  </style>
</head>
<body>
  <div class="header-brand">
    <div class="container text-center">
      <h1 class="h3 mb-2"><i class="bi bi-airplane-engines"></i> 괌호텔에서 공항 드랍서비스</h1>
      <div class="price-badge mt-2">$10 / 1인</div>
      <p class="text-white-50 mt-1 mb-0 small">(성인/소아 동일 · 유아 무료 · 현금 결제)</p>
    </div>
  </div>

  <div class="container">
    <!-- 서비스 안내 -->
    <div class="info-box">
      <h5><i class="bi bi-info-circle"></i> 예약 안내</h5>
      <ol class="mb-0">
        <li class="mb-2"><strong>신청:</strong> 아래 양식을 작성하여 예약 신청</li>
        <li class="mb-2"><strong>카카오 친구추가:</strong> <span class="highlight">@괌공항픽업</span> 카카오톡 친구추가</li>
        <li class="mb-2"><strong>예약완료 메시지 전송:</strong> 카카오톡으로 예약 완료했다고 메시지를 먼저 보내주세요</li>
        <li class="mb-2"><strong>예약확정:</strong> 관리자가 확인 후 예약을 확정해 드립니다</li>
      </ol>
      <div class="alert alert-warning mt-3 mb-0">
        <i class="bi bi-exclamation-triangle"></i> <strong>필수!</strong> 신청 후 반드시 <strong>@괌공항픽업</strong> 카카오톡 친구추가 후, <strong>예약 완료했다고 먼저 메시지를 보내주셔야</strong> 예약이 확정됩니다.
      </div>
    </div>

    <!-- 예약 방법 안내 -->
    <div class="row mb-4">
      <div class="col-md-4">
        <div class="step-box">
          <div class="d-flex align-items-center mb-3">
            <div class="step-number me-3">1</div>
            <h5 class="mb-0">출국 날짜 확인</h5>
          </div>
          <p class="mb-0">항공권에 있는 <strong>출국 날짜</strong>를 확인하세요. 예약 날짜는 출국 날짜와 동일해야 합니다.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="step-box">
          <div class="d-flex align-items-center mb-3">
            <div class="step-number me-3">2</div>
            <h5 class="mb-0">편명 확인</h5>
          </div>
          <p class="mb-0">항공권에 있는 <strong>편명</strong>(예: KE111, OZ123)을 확인하세요.</p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="step-box">
          <div class="d-flex align-items-center mb-3">
            <div class="step-number me-3">3</div>
            <h5 class="mb-0">호텔 정보 입력</h5>
          </div>
          <p class="mb-0">현재 숙박 중인 <strong>호텔명</strong>을 정확히 입력하세요.</p>
        </div>
      </div>
    </div>

    <!-- 예약 폼 -->
    <div class="card shadow-sm">
      <div class="card-body">
        <h4 class="card-title mb-4"><i class="bi bi-calendar-check"></i> 픽업 예약하기</h4>
        
        <form id="bookingForm">
          <!-- 항공편 정보 -->
          <h5 class="mb-3"><i class="bi bi-airplane"></i> 항공편 정보</h5>
          <div class="row">
            <div class="col-md-6 mb-3">
              <label class="form-label">출국 날짜 (항공권과 동일) *</label>
              <input type="date" class="form-control" id="flightDate" required>
              <small class="text-muted">항공권에 표시된 출국 날짜를 선택하세요</small>
            </div>
            <div class="col-md-6 mb-3">
              <label class="form-label">편명 (항공권 확인) *</label>
              <select class="form-select" id="flightNumber" required>
                <option value="">편명을 선택하세요</option>
              </select>
              <small class="text-muted">목록에서 항공권의 편명을 선택하세요</small>
            </div>
          </div>

          <!-- 고객 정보 -->
          <h5 class="mt-4 mb-3"><i class="bi bi-person"></i> 예약자 정보</h5>
          <div class="row">
            <div class="col-md-6 mb-3">
              <label class="form-label">예약자명 (한글) *</label>
              <input type="text" class="form-control" id="customerName" placeholder="홍길동" required>
            </div>
            <div class="col-md-6 mb-3">
              <label class="form-label">호텔명 *</label>
              <select class="form-select" id="hotelName" required>
                <option value="">호텔을 선택하세요</option>
                <option value="Alupang Beach Tower">알루팡 비치 타워 (Alupang Beach Tower)</option>
                <option value="Bay View Hotel Guam">베이 뷰 호텔 괌 (Bay View Hotel Guam)</option>
                <option value="Crowne Plaza Resort Guam">크라운 플라자 리조트 괌 (Crowne Plaza Resort Guam)</option>
                <option value="Dusit Thani Guam Resort">두짓타니 괌 리조트 (Dusit Thani Guam Resort)</option>
                <option value="Guam Plaza Resort & Spa">괌 플라자 리조트 & 스파 (Guam Plaza Resort & Spa)</option>
                <option value="Guam Reef Hotel">괌 리프 호텔 (Guam Reef Hotel)</option>
                <option value="Hilton Guam Resort & Spa">힐튼 괌 리조트 & 스파 (Hilton Guam Resort & Spa)</option>
                <option value="Hotel Nikko Guam">호텔 니코 괌 (Hotel Nikko Guam)</option>
                <option value="Hyatt Regency Guam">하얏트 리젠시 괌 (Hyatt Regency Guam)</option>
                <option value="Lotte Hotel Guam">롯데호텔 괌 (Lotte Hotel Guam)</option>
                <option value="Onward Beach Resort">온워드 비치 리조트 (Onward Beach Resort)</option>
                <option value="Outrigger Guam Beach Resort">아웃리거 괌 비치 리조트 (Outrigger Guam Beach Resort)</option>
                <option value="Pacific Bay Hotel">퍼시픽 베이 호텔 (Pacific Bay Hotel)</option>
                <option value="Pacific Islands Club Guam">PIC 괌 (Pacific Islands Club Guam)</option>
                <option value="Royal Orchid Guam Hotel">로얄 오키드 괌 호텔 (Royal Orchid Guam Hotel)</option>
                <option value="The Westin Resort Guam">웨스틴 리조트 괌 (The Westin Resort Guam)</option>
                <option value="Tsubaki Tower">츠바키 타워 (Tsubaki Tower)</option>
              </select>
              <small class="text-muted">목록에서 투숙 중인 호텔을 선택하세요</small>
            </div>
          </div>

          <!-- 인원 정보 -->
          <h5 class="mt-4 mb-3"><i class="bi bi-people"></i> 인원 정보</h5>
          <div class="row">
            <div class="col-md-4 mb-3">
              <label class="form-label">성인 <small class="text-muted">($10/인)</small></label>
              <input type="number" class="form-control" id="adultCount" min="1" value="1" required>
            </div>
            <div class="col-md-4 mb-3">
              <label class="form-label">소아 (만 2-11세) <small class="text-muted">($10/인)</small></label>
              <input type="number" class="form-control" id="childCount" min="0" value="0">
            </div>
            <div class="col-md-4 mb-3">
              <label class="form-label">유아 (만 0-1세) <small class="text-success">무료</small></label>
              <input type="number" class="form-control" id="infantCount" min="0" value="0">
            </div>
          </div>

          <div class="mb-3">
            <label class="form-label">짐 개수 (캐리어, 큰 가방)</label>
            <input type="number" class="form-control" id="luggageCount" min="0" value="0">
          </div>

          <!-- 연락처 정보 -->
          <h5 class="mt-4 mb-3"><i class="bi bi-telephone"></i> 연락처</h5>
          <div class="mb-3">
            <label class="form-label">휴대폰 번호 *</label>
            <input type="tel" class="form-control" id="phone" placeholder="010-1234-5678" required>
            <small class="text-muted">예약 확인 및 긴급 연락용</small>
          </div>

          <div class="mb-4">
            <label class="form-label">특이사항 (선택)</label>
            <textarea class="form-control" id="memo" rows="3" placeholder="특별히 전달하실 내용이 있으시면 입력해주세요"></textarea>
          </div>

          <!-- 총 요금 표시 -->
          <div class="alert alert-success">
            <h5 class="mb-2"><i class="bi bi-calculator"></i> 예상 요금</h5>
            <div class="mb-2">
              <div>성인: <strong id="adultDisplay">1</strong>명 × $10 = <strong id="adultTotal">$10</strong></div>
              <div>소아: <strong id="childDisplay">0</strong>명 × $10 = <strong id="childTotal">$0</strong></div>
              <div class="text-muted">유아: <strong id="infantDisplay">0</strong>명 × $0 = <strong>무료</strong></div>
            </div>
            <hr>
            <div class="d-flex justify-content-between align-items-center">
              <span>총 <strong id="totalPassengers">1</strong>명</span>
              <span class="h4 mb-0"><strong id="totalPrice">$10</strong></span>
            </div>
            <small class="text-muted">* 현장에서 현금으로 결제 (카드 결제 불가)</small>
          </div>

          <button type="submit" class="btn btn-primary btn-lg w-100">
            <i class="bi bi-check-circle"></i> 예약하기
          </button>
        </form>
      </div>
    </div>

    <!-- 추가 안내사항 -->
    <div class="card mt-4 mb-5">
      <div class="card-body">
        <h5><i class="bi bi-question-circle"></i> 자주 묻는 질문</h5>
        <div class="accordion" id="faqAccordion">
          <div class="accordion-item">
            <h2 class="accordion-header">
              <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#faq1">
                픽업 시간은 어떻게 되나요?
              </button>
            </h2>
            <div id="faq1" class="accordion-collapse collapse show" data-bs-parent="#faqAccordion">
              <div class="accordion-body">
                출국 항공편 출발 시간 <strong>3시간 전</strong>에 호텔 로비에서 픽업해 드립니다. 
                예를 들어 비행기가 오후 3시에 출발한다면 낮 12시에 호텔 로비에서 만나게 됩니다.
              </div>
            </div>
          </div>
          <div class="accordion-item">
            <h2 class="accordion-header">
              <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq2">
                카카오톡 연락은 언제 오나요?
              </button>
            </h2>
            <div id="faq2" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
              <div class="accordion-body">
                예약 신청 후 <strong>@괌공항픽업</strong> 카카오톡 친구추가를 하시고, 
                <strong>먼저 예약 완료했다고 메시지를 보내주세요.</strong>
                관리자가 확인 후 예약을 확정해 드리며, 픽업 당일 아침에 정확한 픽업 시간과 차량 정보를 안내해 드립니다.
              </div>
            </div>
          </div>
          <div class="accordion-item">
            <h2 class="accordion-header">
              <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq3">
                결제는 어떻게 하나요?
              </button>
            </h2>
            <div id="faq3" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
              <div class="accordion-body">
                현장에서 <strong>현금(USD)으로만 결제</strong> 가능합니다. (신용카드 결제 불가)<br>
                요금은 <strong>성인과 소아만 1인당 $10</strong>이며, <strong>유아(만 0-1세)는 무료</strong>입니다.
              </div>
            </div>
          </div>
          <div class="accordion-item">
            <h2 class="accordion-header">
              <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq4">
                예약 취소는 어떻게 하나요?
              </button>
            </h2>
            <div id="faq4" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
              <div class="accordion-body">
                <strong>@괌공항픽업</strong> 카카오톡으로 연락주시면 취소 처리해 드립니다.
                가능한 빠르게 연락 주시면 감사하겠습니다.
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <!-- jQuery (Select2 의존성) -->
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <!-- Select2 JS -->
  <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
  <script>
    // 항공편 로드
    async function loadFlights() {
      try {
        console.log('항공편 데이터 로딩 시작...');
        const res = await fetch('/pickup/api/flights/all');
        
        if (!res.ok) {
          throw new Error(`HTTP error! status: ${res.status}`);
        }
        
        const flights = await res.json();
        console.log('항공편 데이터:', flights);
        
        const select = $('#flightNumber');
        select.html('<option value="">편명을 선택하세요</option>');
        
        let gumFlightsCount = 0;
        
        // 배열로 받은 항공편 데이터 처리
        if (Array.isArray(flights)) {
          // 괌 출발 항공편만 필터링
          flights.forEach(flight => {
            console.log(`편명 ${flight.flight_number}:`, flight);
            
            if (flight.departure_airport === 'GUM' && flight.is_active) {
              const departureTime = flight.departure_time ? flight.departure_time.substring(0, 5) : '';
              select.append(`<option value="${flight.flight_number}">${flight.flight_number} (${departureTime} 출발)</option>`);
              gumFlightsCount++;
            }
          });
        }
        
        console.log(`괌 출발 항공편 ${gumFlightsCount}개 로드 완료`);
        
        // Select2 초기화
        select.select2({
          theme: 'bootstrap-5',
          placeholder: '편명을 선택하세요',
          allowClear: true,
          width: '100%'
        });
        
        if (gumFlightsCount === 0) {
          alert('⚠️ 현재 이용 가능한 괌 출발 항공편이 없습니다.\n관리자에게 문의해주세요.');
        }
      } catch (error) {
        console.error('❌ 항공편 로드 실패:', error);
        alert('항공편 정보를 불러올 수 없습니다.\n페이지를 새로고침해주세요.');
      }
    }
    
    // 초기화
    $(document).ready(function() {
      // 호텔명 Select2 초기화
      $('#hotelName').select2({
        theme: 'bootstrap-5',
        placeholder: '호텔을 선택하세요',
        allowClear: true,
        width: '100%'
      });
      
      // 항공편 로드 및 Select2 초기화
      loadFlights();
      updatePrice();
    });

    // 인원 변경 시 총 요금 계산 (유아 무료)
    function updatePrice() {
      const adults = parseInt(document.getElementById('adultCount').value) || 0;
      const children = parseInt(document.getElementById('childCount').value) || 0;
      const infants = parseInt(document.getElementById('infantCount').value) || 0;
      
      const adultPrice = adults * 10;
      const childPrice = children * 10;
      const totalPassengers = adults + children + infants;
      const totalPrice = adultPrice + childPrice; // 유아는 무료
      
      // 각 인원별 표시
      document.getElementById('adultDisplay').textContent = adults;
      document.getElementById('adultTotal').textContent = `$${adultPrice}`;
      document.getElementById('childDisplay').textContent = children;
      document.getElementById('childTotal').textContent = `$${childPrice}`;
      document.getElementById('infantDisplay').textContent = infants;
      
      // 총 금액 표시
      document.getElementById('totalPassengers').textContent = totalPassengers;
      document.getElementById('totalPrice').textContent = `$${totalPrice}`;
    }

    // 인원 입력 필드에 이벤트 리스너 추가
    document.getElementById('adultCount').addEventListener('input', updatePrice);
    document.getElementById('childCount').addEventListener('input', updatePrice);
    document.getElementById('infantCount').addEventListener('input', updatePrice);

    // 폼 제출
    document.getElementById('bookingForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = {
        flight_date: document.getElementById('flightDate').value,
        flight_number: document.getElementById('flightNumber').value,
        customer_name: document.getElementById('customerName').value,
        hotel_name: document.getElementById('hotelName').value,
        adult_count: parseInt(document.getElementById('adultCount').value) || 0,
        child_count: parseInt(document.getElementById('childCount').value) || 0,
        infant_count: parseInt(document.getElementById('infantCount').value) || 0,
        luggage_count: parseInt(document.getElementById('luggageCount').value) || 0,
        phone: document.getElementById('phone').value,
        memo: document.getElementById('memo').value
      };

      try {
        const res = await fetch('/pickup/api/customer-booking', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(formData)
        });

        const result = await res.json();

        if (result.success) {
          alert('✅ 예약 신청이 완료되었습니다!\n\n⚠️ 다음 단계를 반드시 진행해주세요:\n1. 카카오톡에서 "@괌공항픽업" 검색\n2. 친구 추가하기\n3. 예약 완료했다고 메시지 먼저 보내기\n4. 관리자 확인 후 예약 확정\n\n감사합니다!');
          document.getElementById('bookingForm').reset();
          $('#flightNumber').val(null).trigger('change');
          $('#hotelName').val(null).trigger('change');
          updatePrice();
        } else {
          alert('❌ 예약 실패: ' + (result.error || '알 수 없는 오류'));
        }
      } catch (error) {
        console.error('예약 오류:', error);
        alert('❌ 예약 실패: ' + error.message);
      }
    });
  </script>
</body>
</html>
